// <auto-generated/>
#nullable enable
using System.Collections.Immutable;

namespace BlazorCodeFirst.Site.Content;

/// <summary>One document's navigation metadata and its build-time converted HTML.</summary>
/// <param name="Lang">The language tag this document is written in.</param>
/// <param name="Stale">True when a translation was written against an older revision
/// of its canonical document. Always false on a canonical document.</param>
public sealed record DocEntry(string Slug, string Title, int Order, string Lang, bool Stale, string Html);

/// <summary>The text the documentation shell shows a reader of one language.</summary>
/// <param name="Name">What this language calls itself, shown to a reader of another one.</param>
/// <param name="StaleNotice">The sentence a translation carries when it has fallen
/// behind, or null on the canonical language, which has nothing to fall behind.</param>
public sealed record ShellText(
    string Name,
    string IndexTitle,
    string IndexLead,
    string RailHeading,
    string LanguageLabel,
    string? StaleNotice,
    string? StaleLink);

/// <summary>The generated documentation manifest, grouped by language and ordered by
/// (Order, Slug) within each one.</summary>
public static class Docs
{
    /// <summary>The canonical language, whose documents are the top-level content files.</summary>
    public const string Canonical = "en";

    /// <summary>Every language the manifest holds documents for, canonical first.</summary>
    public static readonly ImmutableArray<string> Languages =
        ["en", "ja"];

    /// <summary>The route a language's documents are served from, with no trailing slash.</summary>
    public static string RoutePrefix(string lang) => lang switch
    {
        "ja" => "/docs/ja",
        _ => "/docs",
    };

    /// <summary>The route one document is served from.</summary>
    public static string Href(string lang, string slug) => RoutePrefix(lang) + "/" + slug;

    /// <summary>The shell text shown to a reader of one language.</summary>
    public static ShellText Shell(string lang) => lang switch
    {
        "ja" => new ShellText(
            "日本語",
            "ドキュメント",
            "ガイドの全文書を、読む順に並べています。",
            "ガイド",
            "言語",
            "この日本語版は、英語版の古い版をもとにしています。",
            "英語版を読む"),
        _ => new ShellText(
            "English",
            "Documentation",
            "Every document in the guide, in reading order.",
            "Guide",
            "Language",
            null,
            null),
    };

    public static readonly ImmutableArray<DocEntry> All =
    [
        new DocEntry(
            "getting-started",
            "Getting Started",
            10,
            "en",
            false,
            "<p>BlazorCodeFirst lets you write Blazor UI as plain C#. This page is itself rendered\nfrom Markdown, converted at build time and injected through <code>Html.Raw</code>.</p>\n<h2 id=\"installation\">Installation<a href=\"#installation\" class=\"headlink\" aria-label=\"Permalink to this section\">#</a></h2>\n<p>Add the runtime and the source generator to your project, then derive your\ncomponents from <code>BodyComponentBase</code>.</p>\n<h2 id=\"a-first-component\">A first component<a href=\"#a-first-component\" class=\"headlink\" aria-label=\"Permalink to this section\">#</a></h2>\n<div class=\"csharp\"><pre>\n<span class=\"keyword\">using</span> Microsoft.AspNetCore.Components;\n<span class=\"keyword\">using</span> BlazorCodeFirst;\n<span class=\"keyword\">using</span> <span class=\"keyword\">static</span> BlazorCodeFirst.Html;\n\n[Route(<span class=\"string\">&quot;/&quot;</span>)]\n<span class=\"keyword\">public</span> <span class=\"keyword\">partial</span> <span class=\"keyword\">class</span> Home : BodyComponentBase\n{\n    <span class=\"keyword\">protected</span> <span class=\"keyword\">override</span> View Body =&gt;\n        Div[\n            H1[<span class=\"string\">&quot;Hello&quot;</span>],\n            Span[<span class=\"string\">&quot;Welcome to BlazorCodeFirst.&quot;</span>]];\n}\n</pre></div><h2 id=\"what-the-class-has-to-be\">What the class has to be<a href=\"#what-the-class-has-to-be\" class=\"headlink\" aria-label=\"Permalink to this section\">#</a></h2>\n<p>The generator writes a <code>RenderView</code> override into the class you declared, so that class has to be one\nit can write into. Three shapes are rejected, each by its own diagnostic.</p>\n<p>The class must be <code>partial</code>, or BCF1001 is reported. Only the class that declares the override needs\nthe modifier, because nothing is generated into any of these:</p>\n<ul>\n<li>an intermediate abstract base</li>\n<li>a leaf whose base already declares the override</li>\n<li>a re-abstraction</li>\n</ul>\n<p>The class must be top-level, or BCF1005 is reported. Generated code cannot reproduce a chain of\nenclosing type declarations, including any enclosing type's type parameters, so a nested component is\nrejected rather than half emitted. Without the diagnostic the nesting would surface only as CS0534\nagainst the abstract <code>RenderView</code>. CS0534 names the missing member, never the type it is nested in.</p>\n<p>The getter must reach one returned expression, or BCF1004 is reported. Three spellings satisfy that,\nand all three translate identically:</p>\n<div class=\"csharp\"><pre>\n<span class=\"keyword\">protected</span> <span class=\"keyword\">override</span> View Body =&gt; Div[H1[<span class=\"string\">&quot;Hello&quot;</span>]];              <span class=\"comment\">// fine</span>\n<span class=\"keyword\">protected</span> <span class=\"keyword\">override</span> View Body { <span class=\"keyword\">get</span> =&gt; Div[H1[<span class=\"string\">&quot;Hello&quot;</span>]]; }      <span class=\"comment\">// fine</span>\n<span class=\"keyword\">protected</span> <span class=\"keyword\">override</span> View Body { <span class=\"keyword\">get</span> { <span class=\"keyword\">return</span> Div[H1[<span class=\"string\">&quot;Hello&quot;</span>]]; } }   <span class=\"comment\">// fine</span>\n</pre></div>\n<p>Locals and expression statements may precede that return. They are transplanted into the generated\n<code>RenderView</code> ahead of the frames, which is where a <code>ForEach</code> content block's statements already go:</p>\n<div class=\"csharp\"><pre>\n<span class=\"keyword\">protected</span> <span class=\"keyword\">override</span> View Body                                   <span class=\"comment\">// fine</span>\n{\n    <span class=\"keyword\">get</span>\n    {\n        <span class=\"keyword\">var</span> greeting = $<span class=\"string\">&quot;Hello, {_name}&quot;</span>;\n        <span class=\"keyword\">return</span> Div[H1[greeting]];\n    }\n}\n</pre></div>\n<p>What is left over still earns BCF1004:</p>\n<div class=\"csharp\"><pre>\n<span class=\"keyword\">protected</span> <span class=\"keyword\">override</span> View Body                                   <span class=\"comment\">// BCF1004: a second return</span>\n{\n    <span class=\"keyword\">get</span>\n    {\n        <span class=\"keyword\">if</span> (_name <span class=\"keyword\">is</span> <span class=\"keyword\">null</span>)\n            <span class=\"keyword\">return</span> Div[H1[<span class=\"string\">&quot;Hello&quot;</span>]];\n\n        <span class=\"keyword\">return</span> Div[H1[$<span class=\"string\">&quot;Hello, {_name}&quot;</span>]];\n    }\n}\n\n<span class=\"keyword\">protected</span> <span class=\"keyword\">override</span> View Body { <span class=\"keyword\">get</span>; } = <span class=\"keyword\">default</span>;               <span class=\"comment\">// BCF1004: an auto property has no getter body</span>\n</pre></div>\n<p>A second return and native control flow each need a sequence space of their own, and an auto property\ndeclares no getter body at all. BCF1004 blames the declaration, and that is what separates it from\nBCF1003. BCF1003 means the getter's shape was fine and something written inside it could not be\nsequenced. If the body genuinely cannot be written in this shape, override <code>RenderView</code> by hand — the\ndesign-time expression is then unused, and nothing is reported.</p>\n<p>Statements are translated, not run: the design-time expression is still inert, so mutating state inside\none is BCF3001 as it always was.</p>\n<p>A class can carry two of these faults at once — a missing <code>partial</code> and an untranslatable getter — and\nyou are told about one at a time. The <code>partial</code> check runs first, so BCF1001 comes alone, and adding\nthe modifier is what surfaces BCF1004.</p>\n<h2 id=\"where-the-surface-means-something\">Where the surface means something<a href=\"#where-the-surface-means-something\" class=\"headlink\" aria-label=\"Permalink to this section\">#</a></h2>\n<p><code>Html.Div</code>, <code>.Class(...)</code>, <code>.OnClick(...)</code> and every other factory and decoration are inert. <code>View</code> is\nan empty struct, an element helper returns nothing, and a decoration returns its receiver unchanged.\nThe generator reads the <em>syntax</em> you wrote, never the value, and it reads it in three places: a\ncomponent's <code>Body</code>, a layout's <code>Chrome</code>, and the body of a <code>[ViewPart]</code> method.</p>\n<p>The same API is callable from anywhere, and elsewhere it means nothing. Written in an event handler, a\nservice, or a helper method it still compiles, still looks like it built something, and does nothing\nat all — no output, and no handler wired up:</p>\n<div class=\"csharp\"><pre>\n<span class=\"keyword\">private</span> <span class=\"keyword\">void</span> OnSomething()\n{\n    <span class=\"comment\">// BCF3029: renders nothing, and DoThing is never called</span>\n    <span class=\"keyword\">var</span> card = Div.Class(<span class=\"string\">&quot;card&quot;</span>).OnClick(DoThing)[Span[<span class=\"string\">&quot;hello&quot;</span>]];\n}\n</pre></div>\n<p>BCF3029 names that. What marks a reading position is the design-time type it returns, so none of the\nthree is a special case. A lambda that returns a <code>View</code> — the content of an <code>If</code> or a <code>ForEach</code> — is\nread the same way. Caching a value into a field or property of a design-time type is left alone; only\na local, a discard, or an argument is reported.</p>\n<h2 id=\"values-copied-into-generated-code\">Values copied into generated code<a href=\"#values-copied-into-generated-code\" class=\"headlink\" aria-label=\"Permalink to this section\">#</a></h2>\n<p>BlazorCodeFirst copies design-time value expressions into a generated file that has no <code>using</code>\ndirectives. Resolved type names are rewritten as <code>global::</code>-qualified names. If a type is still\nunresolved and its spelling depends on the source file's lexical context, the generator reports\nBCF3015 at that type name.</p>\n<p>Fix the name, fully qualify it, move a source-generated type to a referenced project, or replace it\nwith a hand-written C# type. A reference already rooted at <code>global::</code> is preserved and left to normal\nC# resolution. Generic type arguments are checked independently.</p>\n<h2 id=\"next-steps\">Next steps<a href=\"#next-steps\" class=\"headlink\" aria-label=\"Permalink to this section\">#</a></h2>\n<ul>\n<li>Read the <a href=\"/counter\">counter sample</a> to see events, <code>If</code>, and keyed <code>ForEach</code>.</li>\n<li>Jump to <a href=\"#installation\">Installation</a> or <a href=\"#a-first-component\">A first component</a>.</li>\n<li>Learn the <a href=\"/docs/elements-and-decorations\">element vocabulary</a> and <a href=\"/docs/control-flow#if\">control flow</a>.</li>\n</ul>\n"),
        new DocEntry(
            "elements-and-decorations",
            "Elements and Decorations",
            20,
            "en",
            false,
            "<p>BlazorCodeFirst mirrors HTML directly: every element you write in a <code>Body</code> expression names the\nelement it produces. There is no intermediate widget vocabulary to learn and no runtime UI tree.\nThe source generator turns these calls into <code>RenderTreeBuilder</code> instructions at compile time.</p>\n<h2 id=\"elements\">Elements<a href=\"#elements\" class=\"headlink\" aria-label=\"Permalink to this section\">#</a></h2>\n<p>Element helpers take mixed string and element children in brackets:</p>\n<div class=\"csharp\"><pre>\n<span class=\"keyword\">using</span> BlazorCodeFirst;\n<span class=\"keyword\">using</span> <span class=\"keyword\">static</span> BlazorCodeFirst.Html;\n\n<span class=\"keyword\">protected</span> <span class=\"keyword\">override</span> View Body =&gt;\n    Article[\n        H2[<span class=\"string\">&quot;Elements&quot;</span>],\n        P[<span class=\"string\">&quot;Text and &quot;</span>, A.Href(<span class=\"string\">&quot;/docs&quot;</span>)[<span class=\"string\">&quot;links&quot;</span>], <span class=\"string\">&quot; compose in one call.&quot;</span>],\n        Ul[\n            Li[<span class=\"string\">&quot;An HTML element is a helper, named by its tag with only the first letter uppercased — Figcaption, not FigCaption, likewise Colgroup and Textarea.&quot;</span>],\n            Li[<span class=\"string\">&quot;Element covers what is not a helper: custom elements, Web Components, and the few excluded vocabularies below.&quot;</span>]]];\n</pre></div>\n<p>A dedicated helper exists for every element the HTML Living Standard lists as conforming, so a\n<code>&lt;figure&gt;</code> is one call away, attributes before children like any other element:</p>\n<div class=\"csharp\"><pre>\nFigure[\n    Img.Src(<span class=\"string\">&quot;/diagram.png&quot;</span>).Alt(<span class=\"string\">&quot;Architecture&quot;</span>),\n    Figcaption[<span class=\"string\">&quot;The compilation pipeline&quot;</span>]]\n</pre></div>\n<p><code>Element</code> is what is left over. Two things reach it. The first is custom elements and Web\nComponents, whose tag names are never a known helper. The second is the handful of standard elements\nno helper covers:</p>\n<ul>\n<li>the document and <code>&lt;head&gt;</code>-only elements: <code>html</code>, <code>head</code>, <code>body</code>, <code>title</code>, <code>base</code>, <code>meta</code>, <code>link</code></li>\n<li>raw-text elements: <code>script</code>, <code>style</code>, <code>noscript</code></li>\n<li>elements the render tree cannot give meaning to: <code>template</code>, <code>slot</code></li>\n<li><code>object</code>, whose name is ambiguous with the C# keyword</li>\n<li>the foreign vocabularies <code>svg</code> and <code>math</code>, in full</li>\n</ul>\n<div class=\"csharp\"><pre>\nElement(<span class=\"string\">&quot;my-widget&quot;</span>).Attr(<span class=\"string\">&quot;value&quot;</span>, <span class=\"string\">&quot;42&quot;</span>)   <span class=\"comment\">// custom element</span>\nElement(<span class=\"string\">&quot;svg&quot;</span>)[Element(<span class=\"string\">&quot;circle&quot;</span>)]          <span class=\"comment\">// foreign vocabulary</span>\n</pre></div>\n<p>The tag has to be a non-empty compile-time constant, a literal or a <code>const</code>, or BCF3009 is reported.\n<code>Element</code> lowers its tag to a literal <code>OpenElement</code>, and holding it to a constant is what keeps the\ncall as readable as a helper. The rule is about declarativeness rather than safety: a computed tag is\nneither an injection risk nor a sequencing problem — it just stops the element from naming itself\nwhere you wrote it.</p>\n<div class=\"csharp\"><pre>\n<span class=\"keyword\">private</span> <span class=\"keyword\">const</span> <span class=\"keyword\">string</span> Widget = <span class=\"string\">&quot;my-widget&quot;</span>;\n\nElement(Widget)                  <span class=\"comment\">// fine</span>\nElement(_kind + <span class=\"string\">&quot;-widget&quot;</span>)       <span class=\"comment\">// BCF3009</span>\nElement(<span class=\"string\">&quot;&quot;</span>)                      <span class=\"comment\">// BCF3009, the tag must be non-empty</span>\n</pre></div><h2 id=\"children\">Children<a href=\"#children\" class=\"headlink\" aria-label=\"Permalink to this section\">#</a></h2>\n<p>A bare string converts to a text node, so there is no separate <code>Text()</code> construct. An element with\nno children drops the brackets entirely, and a Blazor <code>RenderFragment</code> sits in the child list like\nany other child:</p>\n<div class=\"csharp\"><pre>\n[Parameter] <span class=\"keyword\">public</span> RenderFragment? ChildContent { <span class=\"keyword\">get</span>; <span class=\"keyword\">set</span>; }\n\n<span class=\"keyword\">protected</span> <span class=\"keyword\">override</span> View Body =&gt;\n    Div[\n        <span class=\"string\">&quot;plain text&quot;</span>,\n        Img.Src(<span class=\"string\">&quot;/logo.png&quot;</span>).Alt(<span class=\"string\">&quot;Logo&quot;</span>),\n        ChildContent];\n</pre></div>\n<p>The generator has to see each child as its own expression to give it a sequence number. Writing the\nchildren as a nested collection literal is accepted, because C# expands it to the same call:</p>\n<div class=\"csharp\"><pre>\nDiv[[<span class=\"string\">&quot;a&quot;</span>, <span class=\"string\">&quot;b&quot;</span>]]     <span class=\"comment\">// same as Div[&quot;a&quot;, &quot;b&quot;], which is the form to prefer</span>\n</pre></div>\n<p>Handing over a child list the generator cannot see through reports BCF1003. That covers a variable\nor method result passed whole (<code>Div[_kids]</code>), an explicit array (<code>Div[new View[] { … }]</code>), and any\nspread (<code>Div[[..items]]</code>). Use <a href=\"/docs/control-flow#keyed-foreach\"><code>ForEach</code></a> for repetition instead.</p>\n<h2 id=\"void-elements-take-no-children\">Void elements take no children<a href=\"#void-elements-take-no-children\" class=\"headlink\" aria-label=\"Permalink to this section\">#</a></h2>\n<p>The thirteen void elements of the HTML standard — <code>area</code>, <code>base</code>, <code>br</code>, <code>col</code>, <code>embed</code>, <code>hr</code>, <code>img</code>,\n<code>input</code>, <code>link</code>, <code>meta</code>, <code>source</code>, <code>track</code>, <code>wbr</code> — have no closing tag, so children written on one\nreport BCF3016:</p>\n<div class=\"csharp\"><pre>\nImg.Src(<span class=\"string\">&quot;/logo.png&quot;</span>)[<span class=\"string\">&quot;Logo&quot;</span>]     <span class=\"comment\">// BCF3016</span>\nElement(<span class=\"string\">&quot;img&quot;</span>)[<span class=\"string\">&quot;Logo&quot;</span>]           <span class=\"comment\">// BCF3016, same rule</span>\nImg.Src(<span class=\"string\">&quot;/logo.png&quot;</span>).Alt(<span class=\"string\">&quot;Logo&quot;</span>) <span class=\"comment\">// what to write instead</span>\n</pre></div>\n<p>The reason is that the children do not survive a round trip through HTML. Prerendering serializes a\nclosing tag the HTML parser does not accept, so the parser pushes the children out of the element and\nthey appear as its siblings; a stray <code>&lt;/br&gt;</code> is even re-read as a start tag, so <code>Br[&quot;x&quot;]</code> prerenders\nas two <code>&lt;br&gt;</code> elements. Interactive rendering has no parser in the way and puts the same children\ninside the element. One expression, two different DOM trees, and the page changes shape as hydration\ntakes over. Configure a void element with decorations and put content beside it.</p>\n<p>Both spellings are checked, the helper and <code>Element</code> with a void tag. Custom elements and unknown\ntags are not: <code>Element(&quot;img-viewer&quot;)[&quot;child&quot;]</code> is accepted, because there is no standard to read\ntheir content model out of.</p>\n<p>This is the limit of what the surface checks about HTML, and the limit is deliberate. BCF3016 is\ndecidable from the element tag by itself. Whether a particular child is allowed inside a particular\nparent is not — <code>Table[Div[&quot;x&quot;]]</code> also renders differently after hydration, and it is accepted, along\nwith attributes an element does not define (<code>Div.Href(&quot;/x&quot;)</code>). See <code>DESIGN.md</code> §4.1 for the whole\nposition.</p>\n<h2 id=\"when-a-name-collides\">When a name collides<a href=\"#when-a-name-collides\" class=\"headlink\" aria-label=\"Permalink to this section\">#</a></h2>\n<p><code>using static BlazorCodeFirst.Html;</code> imports every conforming HTML element name, and a declaration of\nyour own wins simple-name lookup over an imported one. Blazor parameters named <code>Label</code>, <code>Data</code>,\n<code>Summary</code> or <code>Source</code> are ordinary, so this happens.</p>\n<p>A member whose type is indexable makes this legal C#: the element expression silently becomes an\nindexer call on your member, and the generator reports BCF3027 to name it.</p>\n<div class=\"csharp\"><pre>\n[Parameter] <span class=\"keyword\">public</span> <span class=\"keyword\">string</span> Data { <span class=\"keyword\">get</span>; <span class=\"keyword\">set</span>; }\nDiv[Data[<span class=\"string\">&quot;Heading&quot;</span>]]                          <span class=\"comment\">// BCF3027</span>\n</pre></div>\n<p>A type, a namespace, or a method of yours takes the name the same way, and each is the same report,\nnaming what it found:</p>\n<div class=\"csharp\"><pre>\n<span class=\"keyword\">public</span> <span class=\"keyword\">sealed</span> <span class=\"keyword\">class</span> Table;                    <span class=\"comment\">// Table[&quot;x&quot;]   — BCF3027, a type</span>\n<span class=\"keyword\">namespace</span> MyApp.Article { }                   <span class=\"comment\">// Article[&quot;x&quot;] — BCF3027, a namespace</span>\n<span class=\"keyword\">private</span> <span class=\"keyword\">string</span> Summary() =&gt; <span class=\"string\">&quot;&quot;</span>;               <span class=\"comment\">// Summary[&quot;x&quot;] — BCF3027, a method</span>\n</pre></div>\n<p>C# has an error for every one of these — CS1503 on the index argument, CS0119, CS0118, CS0021 — and\nyou never see any of them. As long as the body does not translate, the component has no generated\n<code>RenderView</code>, so the compiler stops before it binds method bodies, which is where all four are found.</p>\n<p>They are fixed the same way, by qualifying the element:</p>\n<div class=\"csharp\"><pre>\nDiv[Html.Data[<span class=\"string\">&quot;Heading&quot;</span>]]\n</pre></div><h2 id=\"decorations\">Decorations<a href=\"#decorations\" class=\"headlink\" aria-label=\"Permalink to this section\">#</a></h2>\n<p>Decorations are chained onto the element they belong to, before its children, the way HTML writes\nattributes inside the tag. They collapse into the owning element's attributes rather than\nintroducing wrapper nodes:</p>\n<div class=\"csharp\"><pre>\nButton\n    .Class(<span class=\"string\">&quot;btn btn-primary&quot;</span>)\n    .Title(<span class=\"string\">&quot;Save the current document&quot;</span>)\n    .OnClick(() =&gt; Save())[<span class=\"string\">&quot;Save&quot;</span>];\n</pre></div>\n<p>Available decorations are <code>.Class</code>, <code>.Id</code>, <code>.Href</code>, <code>.Src</code>, <code>.Alt</code>, <code>.Type</code>, <code>.Title</code>, <code>.Role</code>,\n<code>.OnClick</code>, and the general-purpose escape hatches <code>.Attr(name, value)</code> and <code>.On(eventName, handler)</code>.</p>\n<p><code>.On</code> takes the full attribute name including the <code>on</code> prefix (<code>.On(&quot;onmouseenter&quot;, …)</code>); nothing is\nprefixed for you. The name given to <code>.Attr</code> or <code>.On</code> must be a non-empty compile-time constant, or\nthe generator reports BCF3011.</p>\n<p><code>.Attr</code> takes a <code>string?</code> or a <code>bool</code>. A <code>bool</code> is Blazor's conditional attribute: <code>true</code> renders the\nattribute with an empty value, which is how HTML reads <code>disabled</code>, <code>checked</code> and <code>hidden</code> as set, and\n<code>false</code> leaves the attribute out entirely. Where the attribute is always present, write it bare, as HTML\ndoes — the <code>bool</code> is for the conditional case.</p>\n<div class=\"csharp\"><pre>\nInput.Type(<span class=\"string\">&quot;checkbox&quot;</span>).Attr(<span class=\"string\">&quot;checked&quot;</span>)                    <span class=\"comment\">// &lt;input type=&quot;checkbox&quot; checked&gt;</span>\nButton.Attr(<span class=\"string\">&quot;disabled&quot;</span>, _submitting)[<span class=\"string\">&quot;Save&quot;</span>]              <span class=\"comment\">// conditional</span>\n</pre></div>\n<p>A <code>null</code> string value leaves the attribute out too, so an attribute that carries a value only sometimes\nneeds no branch around the element:</p>\n<div class=\"csharp\"><pre>\nSpan.Attr(<span class=\"string\">&quot;title&quot;</span>, _hasTip ? _tip : <span class=\"keyword\">null</span>)[<span class=\"string\">&quot;Hover me&quot;</span>]\n</pre></div>\n<p><code>null</code> and <code>&quot;&quot;</code> are different values at every stage — frames, prerendered HTML, and a re-render — so\n<code>&quot;&quot;</code> gives you <code>title=&quot;&quot;</code> and <code>null</code> gives you no <code>title</code> at all. When a re-render turns a value null,\nBlazor removes the attribute from the element already in the DOM rather than replacing the element.\nEvery decoration that takes one value accepts <code>null</code> this way: <code>.Class</code>, <code>.Href</code>, <code>.Src</code>, <code>.Alt</code>,\n<code>.Id</code>, <code>.Type</code>, <code>.Title</code>, <code>.Role</code>, and <code>.Attr</code>.</p>\n<p>There is deliberately no <code>object</code> overload. A value of any other type is formatted at render time\nunder the formatting thread's culture rather than the one your component ran under, so write it out\nyourself, where the culture is a choice you can see:</p>\n<div class=\"csharp\"><pre>\nDiv.Attr(<span class=\"string\">&quot;tabindex&quot;</span>, index.ToString(CultureInfo.InvariantCulture))\n</pre></div>\n<p>A handler written as <code>Action</code> or <code>Func&lt;Task&gt;</code> receives nothing. To read the event, give the lambda\nparameter its type, and <code>.On</code> picks up the typed overload:</p>\n<div class=\"csharp\"><pre>\nInput.Type(<span class=\"string\">&quot;text&quot;</span>).Attr(<span class=\"string\">&quot;value&quot;</span>, _name)\n     .On(<span class=\"string\">&quot;oninput&quot;</span>, (ChangeEventArgs e) =&gt; _name = e.Value?.ToString() ?? <span class=\"string\">&quot;&quot;</span>)\n</pre></div>\n<p>Unlike Razor, the argument type is not inferred from the event name, so writing it on the parameter\nis what selects the overload. <code>ChangeEventArgs</code> lives in <code>Microsoft.AspNetCore.Components</code>;\n<code>MouseEventArgs</code>, <code>KeyboardEventArgs</code> and <code>FocusEventArgs</code> live in\n<code>Microsoft.AspNetCore.Components.Web</code>, which a Blazor app already references.</p>\n<p>The type is not inferred, but it is checked. Naming one the event does not deliver reports BCF3028,\nread from the same <code>[EventHandler]</code> metadata Razor uses — so <code>.On(&quot;onclick&quot;, (KeyboardEventArgs e) =&gt; …)</code>\nstops at compile time instead of failing when the button is clicked. A base of the delivered type is\naccepted, because that is what the handler can actually receive:</p>\n<div class=\"csharp\"><pre>\nButton.On(<span class=\"string\">&quot;onclick&quot;</span>, (MouseEventArgs e) =&gt; Zoom(e.ClientX, e.ClientY))[<span class=\"string\">&quot;Zoom&quot;</span>]   <span class=\"comment\">// the delivered type</span>\nButton.On(<span class=\"string\">&quot;onclick&quot;</span>, (EventArgs e) =&gt; Save())[<span class=\"string\">&quot;Save&quot;</span>]                            <span class=\"comment\">// a base of it: fine</span>\nButton.On(<span class=\"string\">&quot;onclick&quot;</span>, (KeyboardEventArgs e) =&gt; Save())[<span class=\"string\">&quot;Save&quot;</span>]                    <span class=\"comment\">// BCF3028</span>\n</pre></div>\n<p>A type that is not an <code>EventArgs</code> at all — <code>.On(&quot;onclick&quot;, (int x) =&gt; …)</code> — is the same diagnostic; C#\nrefuses that call outright, and BCF3028 is what names the reason. An event with no <code>[EventHandler]</code>\nregistration has no mapping to check against, so a custom event you have not registered is left alone.\nRegistering one is the ordinary Blazor mechanism, and a registration in your own project is read:</p>\n<div class=\"csharp\"><pre>\n[EventHandler(<span class=\"string\">&quot;onrate&quot;</span>, <span class=\"keyword\">typeof</span>(RatingEventArgs))]\n<span class=\"keyword\">public</span> <span class=\"keyword\">static</span> <span class=\"keyword\">class</span> AppEventHandlers;\n</pre></div>\n<p>That pair — an attribute out, an event back — is what <a href=\"/docs/two-way-binding\"><code>.Bind</code></a> writes as one\ndecoration.</p>\n<p><code>class</code> is the one attribute that folds: chaining <code>.Class</code> more than once merges the values into a\nsingle <code>class</code> attribute, and <code>.Attr(&quot;class&quot;, …)</code> joins the same channel. Every other attribute and\nevent is a single binding, and binding one twice on the same element reports BCF3010. <code>style</code> is one of\nthose others: write it as <code>.Attr(&quot;style&quot;, …)</code>, and note that two of them on one element is BCF3010\nrather than a second fold.</p>\n<p>Because that channel joins its values as text, <code>class</code> is the one name that takes a string and nothing\nelse. <code>.Attr(&quot;class&quot;, flag)</code> reports BCF3023 — as does <code>.Attr(&quot;class&quot;)</code>, whose bare spelling stands for\na presence and so has no text to join either. Write a conditional class as a string, using <code>null</code> for\nthe term you want gone:</p>\n<div class=\"csharp\"><pre>\nDiv.Class(<span class=\"string\">&quot;card&quot;</span>).Class(_selected ? <span class=\"string\">&quot;is-selected&quot;</span> : <span class=\"string\">&quot;&quot;</span>)\n</pre></div>\n<p>A <code>null</code> term drops out of the join, so an element carrying one class decoration loses the attribute\nentirely when the term is null. It still leaves the separator behind when there is another term to join\nagainst: <code>Div.Class(&quot;card&quot;).Class(_selected ? &quot;is-selected&quot; : null)</code> renders <code>class=&quot;card &quot;</code> while\n<code>_selected</code> is false, which the browser reads as the single class <code>card</code>.</p>\n<p><code>.Bind(&quot;class&quot;, …)</code> is the third way to write the name and the one that does not fold, so an element\ncarrying both it and a <code>.Class</code> would be emitted with <code>class</code> twice. That reports BCF3024, whichever\norder they are written in. Bind it alone and let the getter supply the whole value.</p>\n<p>A decoration must target a single element. Applying one to <code>If</code>, <code>ForEach</code>, <code>Fragment</code>, <code>Raw</code>, or a\ncomponent result reports diagnostic BCF3008, because those constructs open no element to attach to.\nWriting the chain after the children (<code>Div[&quot;text&quot;].Class(&quot;card&quot;)</code>) reports BCF3008 for the same\nreason: the brackets have already produced a <code>View</code>.</p>\n<p>A decoration also has to be one this library declares. A misspelled name (<code>Div.Clas(&quot;card&quot;)</code>), or an\nextension method of your own that takes an element and gives one back, reports BCF3026. C# has an error\nfor the misspelling, but the same stop keeps it from you.</p>\n<h2 id=\"next\">Next<a href=\"#next\" class=\"headlink\" aria-label=\"Permalink to this section\">#</a></h2>\n<p>Read <a href=\"/docs/control-flow\">control flow</a> for conditionals and lists, or go back to\n<a href=\"/docs/getting-started#installation\">getting started</a>.</p>\n"),
        new DocEntry(
            "control-flow",
            "Control Flow",
            30,
            "en",
            false,
            "<p>Conditionals and lists are expressed with dedicated constructs so the generator can assign\ncompile-time sequence numbers to every position in the template.</p>\n<h2 id=\"if\">If<a href=\"#if\" class=\"headlink\" aria-label=\"Permalink to this section\">#</a></h2>\n<p><code>If</code> takes a condition and a content thunk, with an optional else branch:</p>\n<div class=\"csharp\"><pre>\n<span class=\"keyword\">protected</span> <span class=\"keyword\">override</span> View Body =&gt;\n    Div[\n        If(_items.Count == <span class=\"number\">0</span>,\n            () =&gt; P[<span class=\"string\">&quot;Nothing here yet.&quot;</span>],\n            () =&gt; Span[$<span class=\"string\">&quot;{_items.Count} items&quot;</span>])];\n</pre></div>\n<p>Mutually exclusive branches get disjoint sequence ranges, so switching branches never disturbs the\nsibling positions around them.</p>\n<h2 id=\"foreach-and-its-key\">ForEach and its key<a href=\"#foreach-and-its-key\" class=\"headlink\" aria-label=\"Permalink to this section\">#</a></h2>\n<p><code>ForEach</code> takes a key that identifies the item, not its position:</p>\n<div class=\"csharp\"><pre>\n<span class=\"keyword\">protected</span> <span class=\"keyword\">override</span> View Body =&gt;\n    Ul[\n        ForEach(_items,\n            key: item =&gt; item.Id,\n            content: item =&gt; Li[item.Name])];\n</pre></div>\n<p>Sequence numbers identify template positions; keys identify data instances. Passing an index as the\nkey defeats the diff, because reordering the list makes Blazor reuse the wrong element state.</p>\n<p>A key that never mentions its item at all is caught. All three of these report BCF3002:</p>\n<ul>\n<li><code>key: _ =&gt; 0</code></li>\n<li>a key read from a counter outside the lambda</li>\n<li>in a nested loop, an inner key naming only the outer item</li>\n</ul>\n<div class=\"csharp\"><pre>\nForEach(_groups, key: g =&gt; g.Id, content: g =&gt;\n    Div[ForEach(g.Items, key: i =&gt; g.Id, content: i =&gt; Span[i.Name])])   <span class=\"comment\">// BCF3002 on the inner key</span>\n</pre></div>\n<p>It is a warning rather than an error and does not stop the component being emitted, because the list\nstill renders correctly and only diffs badly. The check is also deliberately conservative: it asks\nwhether the item was referenced, not whether the value identifies anything. A key derived from the\nitem but still position-like passes it. Read BCF3002 as a floor rather than a guarantee.</p>\n<p>Both lambdas have to be inline expression lambdas. A block-bodied lambda or a method group cannot be\nsequenced statically and reports BCF3004, so wrap the call instead — <code>item =&gt; Row(item)</code> rather than\n<code>Row</code>.</p>\n<p>The content root must be a single element or component, so a <code>Fragment</code> or <code>Raw</code> root, or a\n<code>RenderFragment</code> placed as content, reports BCF3003.</p>\n<h2 id=\"declining-the-key\">Declining the key<a href=\"#declining-the-key\" class=\"headlink\" aria-label=\"Permalink to this section\">#</a></h2>\n<p>The key parameter has no default value, so a list that has no identity to key on says so:</p>\n<div class=\"csharp\"><pre>\nUl[ForEach(_columns, key: <span class=\"keyword\">null</span>, content: c =&gt; Li[c.Header])]\n</pre></div>\n<p>That is the right spelling for a static menu, a fixed set of columns, or any projection that never\nreorders. What it costs is what BCF3002 warns about: the list diffs as an index-derived key does, so\nan insertion at the front rewrites every row and each row loses its local state. Because no <code>SetKey</code>\nis emitted, BCF3002 has nothing to ask about and BCF3003 no longer applies — a <code>Fragment</code>, a <code>Raw</code>,\nor a bare <code>If</code> may root the content.</p>\n<h2 id=\"splicing-a-projection\">Splicing a projection<a href=\"#splicing-a-projection\" class=\"headlink\" aria-label=\"Permalink to this section\">#</a></h2>\n<p>A child list can also take an ordinary projection, spread into it:</p>\n<div class=\"csharp\"><pre>\nUl[[.. _columns.Select(c =&gt; Li[c.Header])]]\n</pre></div>\n<p>This is sugar for the declined-key <code>ForEach</code> above and compiles to the same <code>foreach</code>. Unlike a whole\nchild list, it mixes with siblings written around it:</p>\n<div class=\"csharp\"><pre>\nUl[[Li[<span class=\"string\">&quot;first&quot;</span>], .. _columns.Select(c =&gt; Li[c.Header]), Li[<span class=\"string\">&quot;last&quot;</span>]]]\n</pre></div>\n<p>Only <code>&lt;source&gt;.Select(&lt;inline expression lambda&gt;)</code> folds. Any other spread — a stored array of\n<code>View</code>, a method returning one — is not statically sequenceable children and reports BCF1003, the\nsame answer a stored <code>View</code> written as a single child already gets.</p>\n<h2 id=\"fragment\">Fragment<a href=\"#fragment\" class=\"headlink\" aria-label=\"Permalink to this section\">#</a></h2>\n<p><code>Fragment</code> groups children without emitting a wrapper element, the equivalent of <code>&lt;&gt;...&lt;/&gt;</code>:</p>\n<div class=\"csharp\"><pre>\nFragment(H2[<span class=\"string\">&quot;Title&quot;</span>], P[<span class=\"string\">&quot;Body&quot;</span>])\n</pre></div>\n<p>Because it opens no element it cannot be decorated (BCF3008) and cannot be a <code>ForEach</code> content root\n(BCF3003).</p>\n<h2 id=\"raw\">Raw<a href=\"#raw\" class=\"headlink\" aria-label=\"Permalink to this section\">#</a></h2>\n<p><code>Raw</code> injects a trusted HTML string verbatim, the <code>MarkupString</code> equivalent. This page itself is\nrendered that way: its Markdown is converted to HTML at build time and passed to <code>Raw</code>.</p>\n<div class=\"csharp\"><pre>\nArticle.Class(<span class=\"string\">&quot;prose&quot;</span>)[Raw(entry.Html)]\n</pre></div>\n<p><code>Raw</code> writes to the DOM without escaping, so it accepts trusted content only. Never flow user input\nor an external response through it.</p>\n<h2 id=\"next\">Next<a href=\"#next\" class=\"headlink\" aria-label=\"Permalink to this section\">#</a></h2>\n<p>See <a href=\"/docs/components-and-reuse\">components and reuse</a> for calling one component from another, or\n<a href=\"/docs/elements-and-decorations#decorations\">elements and decorations</a> for the element vocabulary.</p>\n"),
        new DocEntry(
            "components-and-reuse",
            "Components and Reuse",
            40,
            "en",
            false,
            "<p>A component is the unit of reuse. One BlazorCodeFirst component calls another with\n<code>Component&lt;T&gt;()</code>, an existing Razor or third-party component is called exactly the same way, and a\n<code>.razor</code> file can call back into a BlazorCodeFirst component as an ordinary tag. <code>[ViewPart]</code> is\na different tool for a different job, covered at the end of this page.</p>\n<h2 id=\"calling-another-blazorcodefirst-component\">Calling another BlazorCodeFirst component<a href=\"#calling-another-blazorcodefirst-component\" class=\"headlink\" aria-label=\"Permalink to this section\">#</a></h2>\n<p><code>Component&lt;T&gt;()</code> places a component into the tree. Parameters bind through <code>.Param</code>, naming the\ntarget property with a lambda:</p>\n<div class=\"csharp\"><pre>\n<span class=\"keyword\">protected</span> <span class=\"keyword\">override</span> View Body =&gt;\n    Div.Class(<span class=\"string\">&quot;dashboard&quot;</span>)[\n        Component&lt;StatusBadge&gt;()\n            .Param(b =&gt; b.Status, _status)\n            .Param(b =&gt; b.Compact, <span class=\"keyword\">true</span>)];\n</pre></div>\n<p>The generator turns each <code>.Param</code> into a static parameter setter, emitted as\n<code>AddComponentParameter</code> calls. Nothing is reflected over and no expression tree is compiled at\nruntime, which is what keeps the path trimming- and AOT-safe.</p>\n<p>That is also why the shape is fenced in by diagnostics. Every channel that names a parameter with a\nselector lambda answers to them — <code>.Param</code>, <code>.Template</code>, and the component <code>.Bind</code> alike, not\n<code>.Param</code> alone:</p>\n<ul>\n<li>The selector must be a plain property selection. A cast, a method call, or a member of a\ncaptured variable reports BCF3005, because none of those name a property the generator can emit\na setter for.</li>\n<li>The target must be a settable <code>[Parameter]</code> property, or BCF3006 is reported. Blazor would\notherwise throw at runtime, so the rejection is moved to compile time.</li>\n<li>Binding the same property twice reports BCF3007, whichever channels the two bindings came from.\nBlazor applies only the last value, so the earlier binding would silently die.</li>\n</ul>\n<h2 id=\"passing-child-content\">Passing child content<a href=\"#passing-child-content\" class=\"headlink\" aria-label=\"Permalink to this section\">#</a></h2>\n<p>Nested children bind to <code>ChildContent</code>, mirroring Razor's rule that nested content becomes\n<code>ChildContent</code> and nothing else:</p>\n<div class=\"csharp\"><pre>\n<span class=\"keyword\">protected</span> <span class=\"keyword\">override</span> View Body =&gt;\n    Component&lt;Card&gt;()[\n        H2[<span class=\"string\">&quot;Heading&quot;</span>],\n        P[<span class=\"string\">&quot;Body text&quot;</span>]];\n</pre></div>\n<p>This requires <code>Card</code> to have a settable <code>[Parameter]</code> named <code>ChildContent</code> of a fragment type;\notherwise BCF3013 is reported. A <code>RenderFragment&lt;TContext&gt;</code> counts: the brackets bind it with the\ncontext discarded, because there is no name inside brackets to read a context through. A generic\nfragment under any <em>other</em> name is named with <code>.Template</code> instead — see\n<a href=\"#generic-fragment-parameters\">Generic fragment parameters</a> below.</p>\n<p>Other non-generic <code>RenderFragment</code> parameters (such as <code>Footer</code> or <code>Header</code>) bind through\n<code>.Param(c =&gt; c.Footer, content)</code>, naming the parameter explicitly:</p>\n<div class=\"csharp\"><pre>\n<span class=\"keyword\">protected</span> <span class=\"keyword\">override</span> View Body =&gt;\n    Component&lt;Card&gt;()\n        .Param(c =&gt; c.Title, <span class=\"string\">&quot;Card title&quot;</span>)\n        .Param(c =&gt; c.Footer, Span[<span class=\"string\">&quot;Footer note&quot;</span>])[\n            H2[<span class=\"string\">&quot;Heading&quot;</span>],\n            P[<span class=\"string\">&quot;Body text&quot;</span>]];\n</pre></div>\n<p>Naming <code>ChildContent</code> through <code>.Param</code> is also legal. That is verbose, but it matches Razor's\nattribute form (<code>&lt;Card&gt;&lt;ChildContent&gt;...&lt;/ChildContent&gt;&lt;/Card&gt;</code>). Binding the same parameter\nthrough both channels reports BCF3007.</p>\n<p>A real <code>RenderFragment</code> value (as opposed to a BlazorCodeFirst <code>View</code> expression) still binds\nthrough the generic <code>.Param&lt;TValue&gt;</code> overload and is emitted verbatim.</p>\n<p>Which overload runs is decided by the target parameter's type: a <code>RenderFragment?</code> parameter takes\nthe content overload, everything else takes the generic one. So content aimed at a parameter that is\nnot a <code>RenderFragment</code> lands on the generic overload, which emits its value verbatim — and the\nruntime value of a design-time expression is an empty marker. That reports BCF3014:</p>\n<div class=\"csharp\"><pre>\n[Parameter] <span class=\"keyword\">public</span> <span class=\"keyword\">object</span>? Payload { <span class=\"keyword\">get</span>; <span class=\"keyword\">set</span>; }\n\nComponent&lt;Card&gt;().Param(c =&gt; c.Payload, Div[<span class=\"string\">&quot;x&quot;</span>])   <span class=\"comment\">// BCF3014</span>\n</pre></div>\n<p>Without the diagnostic the failure is either invisible or late. An <code>object</code>-typed parameter accepts\nthe marker with no exception at all and renders wrong output; a typed parameter throws an invalid\ncast while Blazor applies parameters. <code>View</code>, <code>ElementView</code>, <code>ComponentView&lt;T&gt;</code> and <code>SlotView</code> are\nall reported the same way. To pass content, give the receiving component a <code>RenderFragment</code>\nparameter.</p>\n<p>For unresolved type names inside parameter values, see\n<a href=\"/docs/getting-started#values-copied-into-generated-code\">Values copied into generated code</a>.</p>\n<h2 id=\"generic-fragment-parameters\">Generic fragment parameters<a href=\"#generic-fragment-parameters\" class=\"headlink\" aria-label=\"Permalink to this section\">#</a></h2>\n<p>A <code>RenderFragment&lt;TContext&gt;</code> parameter takes a <em>template</em>: the component invokes it once per context\nvalue it wants rendered. <code>EditForm.ChildContent</code> is the one most authors meet first — it is a\n<code>RenderFragment&lt;EditContext&gt;</code>.</p>\n<p><code>.Template</code> names such a parameter. It has two spellings, and which one you want depends only on\nwhether the content reads the context. A generic fragment under a name other than <code>ChildContent</code>,\nsuch as a grid's <code>RowTemplate</code>, always needs <code>.Template</code>, because the brackets never reach it.</p>\n<p>For a <code>ChildContent</code> whose content ignores the context, the brackets are the spelling — the form\nshown above, which emits exactly what <code>.Template(form =&gt; form.ChildContent, content)</code> emits:</p>\n<div class=\"csharp\"><pre>\n<span class=\"keyword\">protected</span> <span class=\"keyword\">override</span> View Body =&gt;\n    Component&lt;EditForm&gt;()\n        .Param(form =&gt; form.Model, _model)[\n            Component&lt;NameFields&gt;().Param(fields =&gt; fields.Value, _model)];\n</pre></div>\n<p>Or name it, with a lambda from the context to content:</p>\n<div class=\"csharp\"><pre>\n<span class=\"keyword\">protected</span> <span class=\"keyword\">override</span> View Body =&gt;\n    Component&lt;EditForm&gt;()\n        .Param(form =&gt; form.Model, _model)\n        .Template(form =&gt; form.ChildContent, editContext =&gt;\n            Fragment(\n                Span[editContext.IsModified() ? <span class=\"string\">&quot;Unsaved changes&quot;</span> : <span class=\"string\">&quot;No changes&quot;</span>],\n                Component&lt;NameFields&gt;().Param(fields =&gt; fields.Value, _model)));\n</pre></div>\n<p>That second example needs one caveat, or the badge will never change. <code>IsModified()</code> is read when the\ntemplate runs, and nothing in the <code>EditForm</code> / <code>CascadingValue</code> chain re-renders on <code>OnFieldChanged</code>.\nTyping into a field does notify the <code>EditContext</code>, but with nothing subscribed to that notification\nthe component holding the template never re-renders, so the badge keeps the text it was first\nrendered with. This is Blazor's render propagation, not a limit on\nthe context the template receives: the template is handed the live <code>EditContext</code> every time it runs.</p>\n<p>So if a template reads context state that changes, the component owning the form has to re-render\nitself. Construct the <code>EditContext</code> rather than letting <code>Model</code> create it, subscribe to\n<code>OnFieldChanged</code>, call <code>StateHasChanged</code>, and unsubscribe in <code>Dispose</code>:</p>\n<div class=\"csharp\"><pre>\n<span class=\"keyword\">public</span> ContextReadingForm()\n{\n    _editContext = <span class=\"keyword\">new</span> EditContext(_model);\n    _editContext.OnFieldChanged += OnFieldChanged;\n}\n\n<span class=\"keyword\">private</span> <span class=\"keyword\">void</span> OnFieldChanged(<span class=\"keyword\">object</span>? sender, FieldChangedEventArgs e) =&gt; StateHasChanged();\n\n<span class=\"keyword\">public</span> <span class=\"keyword\">void</span> Dispose() =&gt; _editContext.OnFieldChanged -= OnFieldChanged;\n\n<span class=\"comment\">// ...then pass .Param(form =&gt; form.EditContext, _editContext) instead of .Param(form =&gt; form.Model, …)</span>\n</pre></div>\n<p>A template that ignores its context, or reads only state that does not change while the form is open,\nneeds none of this.</p>\n<p>The generator writes the <code>RenderFragment&lt;TContext&gt;</code> lambda for you, so the content inside is ordinary\nBlazorCodeFirst and its sequence numbers continue the surrounding ones. The context parameter's name\nis yours to choose; the generated code uses a name of its own and rewrites the places you referred to\nit, so a field that happens to share the name is not disturbed.</p>\n<p>The second argument must be an inline lambda. A method group, or a delegate held in a variable or\nfield, reports <strong>BCF3022</strong>: what gets copied into the generated code is the lambda's body syntax, and\na delegate whose declaration is elsewhere has no body to copy.</p>\n<p>If you already hold a <code>RenderFragment&lt;TContext&gt;</code> <em>value</em>, pass it through the scalar\n<code>.Param</code> instead. Both channels reach the same parameter, but they differ in delegate identity, and\nthat difference is visible:</p>\n<div class=\"csharp\"><pre>\n<span class=\"comment\">// Built once, in the constructor. The parameter reference stays stable across renders.</span>\n<span class=\"keyword\">private</span> <span class=\"keyword\">readonly</span> RenderFragment&lt;EditContext&gt; _fields;\n</pre></div>\n<p><code>.Template</code> content that reads state captures it, so the lambda becomes a new delegate on every\nrender. The receiving component sees a changed parameter and re-renders the template. A cached\ndelegate passed through <code>.Param</code> does not change, so it does not. Reach for the cached form only when\nyou want that stability; <code>.Template</code> is otherwise the shorter and safer spelling, because it cannot\nbe forgotten the way caching can.</p>\n<h2 id=\"calling-an-existing-razor-or-third-party-component\">Calling an existing Razor or third-party component<a href=\"#calling-an-existing-razor-or-third-party-component\" class=\"headlink\" aria-label=\"Permalink to this section\">#</a></h2>\n<p>The syntax does not change. A component written in <code>.razor</code>, or one from a package such as\nMudBlazor or QuickGrid, is placed with the same <code>Component&lt;T&gt;()</code>:</p>\n<div class=\"csharp\"><pre>\n<span class=\"keyword\">protected</span> <span class=\"keyword\">override</span> View Body =&gt;\n    Div[\n        Span[<span class=\"string\">&quot;Data Grid&quot;</span>],\n        Component&lt;MudDataGrid&lt;Order&gt;&gt;()\n            .Param(g =&gt; g.Items, _orders)\n            .Param(g =&gt; g.Dense, <span class=\"keyword\">true</span>)];\n</pre></div>\n<p>One restriction applies, and it is the first wall most authors hit. The type argument falls into\nthe generated code as a literal <code>OpenComponent&lt;T&gt;</code>, so it has to resolve while the generator runs.\nThe Razor compiler is itself a source generator, and source generators cannot observe each other's\noutput. A <code>.razor</code> component declared in the <em>same project</em> therefore does not exist yet when\nBlazorCodeFirst's generator runs, and naming it reports <strong>BCF3012</strong>.</p>\n<p>There are two ways around it:</p>\n<ul>\n<li>Move the <code>.razor</code> component into a referenced project or a package. Its type then comes from\nmetadata and resolves normally.</li>\n<li>Hand-write the component in C#. A hand-written component is ordinary source, so it always\nresolves — including in the same project.</li>\n</ul>\n<p>A typo or a missing <code>using</code> produces the same BCF3012, alongside CS0246 at the same position.</p>\n<h2 id=\"using-a-blazorcodefirst-component-from-razor\">Using a BlazorCodeFirst component from Razor<a href=\"#using-a-blazorcodefirst-component-from-razor\" class=\"headlink\" aria-label=\"Permalink to this section\">#</a></h2>\n<p>The other direction has no such restriction. A BlazorCodeFirst component is a plain Blazor\ncomponent — <code>BodyComponentBase</code> derives from <code>ComponentBase</code> — so a <code>.razor</code> file names it as an\nordinary tag:</p>\n<pre><code class=\"language-razor\">@* ExistingPage.razor *@\n&lt;div class=&quot;legacy-layout&quot;&gt;\n    &lt;StatusBadge Status=&quot;@currentStatus&quot; /&gt;\n&lt;/div&gt;\n</code></pre>\n<div class=\"csharp\"><pre>\n<span class=\"keyword\">public</span> <span class=\"keyword\">partial</span> <span class=\"keyword\">class</span> StatusBadge : BodyComponentBase\n{\n    [Parameter] <span class=\"keyword\">public</span> Status Status { <span class=\"keyword\">get</span>; <span class=\"keyword\">set</span>; } = <span class=\"keyword\">default</span>!;\n\n    <span class=\"keyword\">protected</span> <span class=\"keyword\">override</span> View Body =&gt;\n        Span.Class(Status.IsHealthy ? <span class=\"string\">&quot;badge badge-ok&quot;</span> : <span class=\"string\">&quot;badge badge-alert&quot;</span>)[Status.Label];\n}\n</pre></div>\n<p>This works in the same project, and it is worth understanding why the asymmetry with BCF3012\nexists. What Razor has to resolve here is the <em>class name</em>, and that class declaration is source\nyou wrote. The generator only fills in <code>RenderView</code> inside it, and Razor never needs to see that.\nIn the BCF3012 direction, the type itself is generated output, which is a different problem.</p>\n<p>This site does it: <code>App.razor</code> names <code>NotFoundPage</code>, a BlazorCodeFirst component declared in a\nplain <code>.cs</code> file in the same project.</p>\n<h2 id=\"splitting-without-a-component-viewpart\">Splitting without a component: <code>[ViewPart]</code><a href=\"#splitting-without-a-component-viewpart\" class=\"headlink\" aria-label=\"Permalink to this section\">#</a></h2>\n<p>Not every part of a <code>Body</code> expression deserves a component. A <code>[ViewPart]</code> method is a piece of\nUI that the generator expands <em>into the caller</em> rather than rendering through a component\nboundary:</p>\n<div class=\"csharp\"><pre>\n<span class=\"keyword\">protected</span> <span class=\"keyword\">override</span> View Body =&gt;\n    Div[\n        AppHeader(<span class=\"string\">&quot;My Application&quot;</span>),\n        BodyContent()];\n\n[ViewPart]\n<span class=\"keyword\">private</span> <span class=\"keyword\">static</span> View AppHeader(<span class=\"keyword\">string</span> title) =&gt;\n    Div.Class(<span class=\"string\">&quot;app-header&quot;</span>)[\n        Span[title]];\n</pre></div>\n<p>The caller's generated <code>RenderView</code> contains the header's frames directly. There is no component\ninstance, no parameters, no lifecycle, and no diffing boundary — it is as if you had written the\nmarkup inline.</p>\n<h3 id=\"naming-a-component-call\">Naming a component call<a href=\"#naming-a-component-call\" class=\"headlink\" aria-label=\"Permalink to this section\">#</a></h3>\n<p>A part's body is ordinary design-time syntax, so it can be a component call as readily as an element.\nThat gives a component invocation a name of its own, and the call site stops mentioning\n<code>Component&lt;T&gt;()</code> or <code>.Param</code> at all:</p>\n<div class=\"csharp\"><pre>\n<span class=\"keyword\">public</span> <span class=\"keyword\">static</span> <span class=\"keyword\">class</span> Widgets\n{\n    [ViewPart]\n    <span class=\"keyword\">public</span> <span class=\"keyword\">static</span> View Badge(<span class=\"keyword\">string</span> label, <span class=\"keyword\">bool</span> compact = <span class=\"keyword\">false</span>) =&gt;\n        Component&lt;StatusBadge&gt;()\n            .Param(b =&gt; b.Label, label)\n            .Param(b =&gt; b.Compact, compact);\n}\n\n<span class=\"keyword\">protected</span> <span class=\"keyword\">override</span> View Body =&gt;\n    Div[\n        Widgets.Badge(<span class=\"string\">&quot;hello&quot;</span>),\n        Widgets.Badge(<span class=\"string\">&quot;x&quot;</span>, compact: <span class=\"keyword\">true</span>)];\n</pre></div>\n<p>Named and optional arguments work, because the call site is an ordinary C# call. The expansion is\nstill an expansion: the caller's generated <code>RenderView</code> opens <code>StatusBadge</code> directly at each call, so\nthe rendered tree is the one writing <code>Component&lt;StatusBadge&gt;()</code> twice would have produced.</p>\n<h3 id=\"wrapping-content\">Wrapping content<a href=\"#wrapping-content\" class=\"headlink\" aria-label=\"Permalink to this section\">#</a></h3>\n<p>A part that wraps content the caller supplies returns <code>SlotView</code> instead of <code>View</code>, and writes\n<code>Slot</code> where that content belongs. The caller supplies it in brackets, exactly as it supplies an\nelement's children:</p>\n<div class=\"csharp\"><pre>\n<span class=\"keyword\">protected</span> <span class=\"keyword\">override</span> View Body =&gt;\n    Div[\n        Card(<span class=\"string\">&quot;Profile&quot;</span>)[P[<span class=\"string\">&quot;Body text&quot;</span>]],\n        Section.Class(<span class=\"string\">&quot;body&quot;</span>)[P[<span class=\"string\">&quot;…&quot;</span>]]];\n\n[ViewPart]\n<span class=\"keyword\">private</span> <span class=\"keyword\">static</span> SlotView Card(<span class=\"keyword\">string</span> title) =&gt;\n    Div.Class(<span class=\"string\">&quot;card&quot;</span>)[\n        H2[title],\n        Slot];\n</pre></div>\n<p>That is the point of the spelling: a part you factored out reads the same way a built-in element\nreads. <code>Card(&quot;Profile&quot;)[…]</code> sits beside <code>Section.Class(&quot;body&quot;)[…]</code> without announcing that one of\nthem is yours.</p>\n<p>The brackets are not optional, and nothing enforces that but C#. <code>SlotView</code> has no conversion to\n<code>View</code>, so <code>Div[Card(&quot;Profile&quot;)]</code> — the brackets forgotten — is a compile error rather than a card\nthat renders silently empty. The same property rules out a decoration (<code>Card(&quot;t&quot;).Class(&quot;x&quot;)</code>, which\nfinds no extension method) and the positional spelling (<code>Card(&quot;t&quot;, P[&quot;x&quot;])</code>, which has no parameter\nto bind to).</p>\n<p>A second slot is an ordinary <code>View</code> parameter:</p>\n<div class=\"csharp\"><pre>\n<span class=\"keyword\">protected</span> <span class=\"keyword\">override</span> View Body =&gt;\n    Panel(H2[<span class=\"string\">&quot;Title&quot;</span>])[\n        P[<span class=\"string\">&quot;Body text&quot;</span>]];\n\n[ViewPart]\n<span class=\"keyword\">private</span> <span class=\"keyword\">static</span> SlotView Panel(View header) =&gt;\n    Div.Class(<span class=\"string\">&quot;panel&quot;</span>)[\n        Div.Class(<span class=\"string\">&quot;panel-head&quot;</span>)[header],\n        Div.Class(<span class=\"string\">&quot;panel-body&quot;</span>)[Slot]];\n</pre></div>\n<p>Named channels first, the main content in brackets — the shape <code>Div.Class(&quot;card&quot;)[…]</code> and\n<code>Component&lt;T&gt;().Template(…)[…]</code> already have on this surface.</p>\n<p>Two rules are worth knowing. A <code>SlotView</code> part must name <code>Slot</code> <strong>exactly once</strong>: naming it twice\nwould emit the caller's content twice from one bracket, and never naming it would discard content the\ncaller was required to supply. Either reports <strong>BCF3025</strong>, as does a <code>Slot</code> written anywhere that\nreceives no caller content — a component's own <code>Body</code>, or a part returning <code>View</code>.</p>\n<p>A <code>View</code> parameter, by contrast, may be referenced any number of times, because it is an ordinary\nparameter. Nothing is captured or shared: each reference expands the caller's expression again, so an\nargument with side effects runs once per reference. That is the same behaviour a Blazor\n<code>RenderFragment</code> invoked twice has.</p>\n<p>Both kinds are content, though, and content has no value: it becomes frames, not an expression. So a\nslot can only be <em>placed</em> as a child. Reading one where a value is expected — as a <code>ForEach</code> key, or\ninside an attribute value — reports <strong>BCF1002</strong>.</p>\n<p>That is the whole trade-off, dimension by dimension:</p>\n<table>\n<thead>\n<tr>\n<th></th>\n<th><code>[ViewPart]</code></th>\n<th>A component</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>State and lifecycle</td>\n<td>none; it is a method</td>\n<td>its own, as any Blazor component has</td>\n</tr>\n<tr>\n<td>Re-rendering</td>\n<td>with the caller, having no boundary of its own</td>\n<td>on its own, at its own diffing boundary</td>\n</tr>\n<tr>\n<td>What the caller's frames hold</td>\n<td>the part's frames, expanded in place</td>\n<td>one frame that opens the component</td>\n</tr>\n<tr>\n<td>Arguments</td>\n<td>by-value parameters, named and optional</td>\n<td><code>[Parameter]</code> properties, set through <code>.Param</code></td>\n</tr>\n<tr>\n<td>From another assembly</td>\n<td>not available (BCF1002)</td>\n<td>available</td>\n</tr>\n</tbody>\n</table>\n<p>A <code>[ViewPart]</code> has to satisfy a declaration contract the generator can expand, or it reports\n<strong>BCF1002</strong>. The method must be:</p>\n<ul>\n<li>static</li>\n<li>non-generic, and declared in a non-generic type</li>\n<li>reaching one returned expression, with locals and expression statements allowed ahead of it — the\nsame shape a <code>Body</code> getter takes</li>\n<li>returning <code>View</code>, or <code>SlotView</code> to take content</li>\n</ul>\n<p>Its parameters must be ordinary by-value parameters whose types can be named from generated code.\n<code>params</code>, by-reference parameters,\nand <code>ElementView</code> parameters are all rejected — a childless element is passed as content by\nwriting <code>Div[…]</code> or <code>Fragment(Div)</code>, both of which are <code>View</code>s. A <code>View</code> parameter is a content slot,\nso it requires the <code>SlotView</code> return type; on a part returning <code>View</code> it is BCF1002, and it may\nnever be optional.</p>\n<p>It also must not be an extension member — neither a <code>this</code> parameter nor a member of an <code>extension</code>\nblock. A call is written as a plain call (<code>AppHeader(&quot;My Application&quot;)</code>), the way this surface writes\nevery call that is not a decoration on an element. The fluent spelling would put something that is\nnot a decoration in the position the surface reserves for one. Its receiver could only ever be some\nother type's value, which would turn <code>[ViewPart]</code> into a way to grow <em>that</em> type's API rather than to\nsplit up a <code>Body</code>.</p>\n<p>BCF1002 also fires at the <em>call site</em>, and one of its conditions is worth stating plainly:</p>\n<p><strong>a <code>[ViewPart]</code> cannot cross an assembly boundary.</strong> Expanding a call needs the declaration's\nsource syntax, and the generator collects declarations from the compilation it is running in. IL\ncarries no body syntax, so a <code>[ViewPart]</code> in a referenced project or a package always reports\nBCF1002 where it is called. The same diagnostic covers a recursive expansion cycle, and a body\nthat reaches a <code>private</code> or <code>protected</code> member the expansion site cannot see.</p>\n<p>If you need the part in another project, make it a component and use it through <code>Component&lt;T&gt;()</code>.</p>\n<h2 id=\"next\">Next<a href=\"#next\" class=\"headlink\" aria-label=\"Permalink to this section\">#</a></h2>\n<p>See <a href=\"/docs/layouts\">layouts</a> for wrapping routed pages in shared chrome,\n<a href=\"/docs/two-way-binding#binding-a-component-parameter\">two-way binding</a> for <code>.Bind</code>, the other way a\nparameter is supplied, or <a href=\"/docs/control-flow\">control flow</a> for <code>If</code> and keyed <code>ForEach</code>.</p>\n"),
        new DocEntry(
            "layouts",
            "Layouts",
            50,
            "en",
            false,
            "<p>A layout wraps the routed page with shared chrome: headers, navigation, footers. BlazorCodeFirst\nlayouts are written the same way as components: derive from <code>ChromeLayoutBase</code>, declare a\ndesign-time UI expression, and let the source generator produce the rendering.</p>\n<h2 id=\"chrome-and-body\">Chrome and Body<a href=\"#chrome-and-body\" class=\"headlink\" aria-label=\"Permalink to this section\">#</a></h2>\n<p><code>ChromeLayoutBase</code> derives from Blazor's <code>LayoutComponentBase</code>, so it already has a <code>Body</code>\nparameter holding the routed page. The chrome the layout itself draws goes in a separate\noverridden property, <code>Chrome</code>:</p>\n<div class=\"csharp\"><pre>\n<span class=\"keyword\">using</span> BlazorCodeFirst;\n<span class=\"keyword\">using</span> <span class=\"keyword\">static</span> BlazorCodeFirst.Html;\n\n<span class=\"keyword\">public</span> <span class=\"keyword\">partial</span> <span class=\"keyword\">class</span> MainLayout : ChromeLayoutBase\n{\n    <span class=\"keyword\">protected</span> <span class=\"keyword\">override</span> View Chrome =&gt;\n        Div.Class(<span class=\"string\">&quot;shell&quot;</span>)[\n            Header[H1[<span class=\"string\">&quot;My App&quot;</span>]],\n            Main.Class(<span class=\"string\">&quot;content&quot;</span>)[Body],\n            Footer[<span class=\"string\">&quot;&#169; 2026&quot;</span>]];\n}\n</pre></div>\n<p><code>Main[Body]</code> here is exactly <code>&lt;main&gt;@Body&lt;/main&gt;</code> in Razor, the routed page dropped in as\nelement content.</p>\n<h2 id=\"why-chrome-not-body\">Why Chrome, not Body<a href=\"#why-chrome-not-body\" class=\"headlink\" aria-label=\"Permalink to this section\">#</a></h2>\n<p>Blazor requires a layout's routed content to be exposed through a parameter named exactly\n<code>Body</code>, and C# cannot declare two members with the same name on one type. So <code>Body</code> keeps\nits Razor meaning, the page being wrapped, and the layout's own design-time expression is\nnamed <code>Chrome</code> instead.</p>\n<h2 id=\"nesting-layouts\">Nesting layouts<a href=\"#nesting-layouts\" class=\"headlink\" aria-label=\"Permalink to this section\">#</a></h2>\n<p>A layout can itself sit inside another layout. Put <code>[Layout]</code> on the layout type, exactly as you\nwould on a page:</p>\n<div class=\"csharp\"><pre>\n<span class=\"keyword\">using</span> BlazorCodeFirst;\n<span class=\"keyword\">using</span> Microsoft.AspNetCore.Components;\n<span class=\"keyword\">using</span> <span class=\"keyword\">static</span> BlazorCodeFirst.Html;\n\n[Layout(<span class=\"keyword\">typeof</span>(SiteLayout))]\n<span class=\"keyword\">public</span> <span class=\"keyword\">partial</span> <span class=\"keyword\">class</span> DocsLayout : ChromeLayoutBase\n{\n    <span class=\"keyword\">protected</span> <span class=\"keyword\">override</span> View Chrome =&gt; Div.Class(<span class=\"string\">&quot;docs&quot;</span>)[Aside[TableOfContents()], Main[Body]];\n}\n</pre></div>\n<p>Nesting is resolved by Blazor, not by BlazorCodeFirst: <code>LayoutView</code> reads the attribute off the layout\ntype and wraps it in its own layout, and a BlazorCodeFirst layout is an ordinary <code>LayoutComponentBase</code>\ndescendant. Each level's <code>Body</code> holds the level below it. <code>SiteLayout</code>'s <code>Body</code> is the rendered\n<code>DocsLayout</code>, whose own <code>Body</code> is the routed page.</p>\n<h2 id=\"renderfragment-becomes-content-directly\">RenderFragment becomes content directly<a href=\"#renderfragment-becomes-content-directly\" class=\"headlink\" aria-label=\"Permalink to this section\">#</a></h2>\n<p><code>Body</code> is a plain Blazor <code>RenderFragment?</code>, not a BlazorCodeFirst type, yet <code>Main[Body]</code> above\ncompiles without dedicated syntax. <code>View</code> has an implicit conversion from <code>RenderFragment?</code>,\nso any fragment can appear wherever element content is expected. The conversion is from the\nnon-generic <code>RenderFragment</code> only, and a <code>RenderFragment&lt;T&gt;</code> does not convert. Like <code>Fragment</code>\nand <code>Raw</code>, a <code>RenderFragment</code> opens no keyable frame, so it cannot be a <code>ForEach</code> content root and\ncannot carry decorations.</p>\n<p>The same mechanism lets a BlazorCodeFirst component render children passed in from Razor. A component\nwith <code>[Parameter] public RenderFragment? ChildContent</code> uses it exactly like <code>Body</code>:</p>\n<div class=\"csharp\"><pre>\n<span class=\"keyword\">using</span> BlazorCodeFirst;\n<span class=\"keyword\">using</span> Microsoft.AspNetCore.Components;\n<span class=\"keyword\">using</span> <span class=\"keyword\">static</span> BlazorCodeFirst.Html;\n\n<span class=\"keyword\">public</span> <span class=\"keyword\">partial</span> <span class=\"keyword\">class</span> Card : BodyComponentBase\n{\n    [Parameter]\n    <span class=\"keyword\">public</span> RenderFragment? ChildContent { <span class=\"keyword\">get</span>; <span class=\"keyword\">set</span>; }\n\n    <span class=\"keyword\">protected</span> <span class=\"keyword\">override</span> View Body =&gt; Div.Class(<span class=\"string\">&quot;card&quot;</span>)[ChildContent];\n}\n</pre></div>\n<p>Passing content the other way, from BlazorCodeFirst code into a Razor or hand-written component,\nuses <code>Component&lt;T&gt;()</code>. See\n<a href=\"/docs/components-and-reuse#passing-child-content\">passing child content</a>.</p>\n<h2 id=\"reads-are-allowed-mutation-is-not\">Reads are allowed, mutation is not<a href=\"#reads-are-allowed-mutation-is-not\" class=\"headlink\" aria-label=\"Permalink to this section\">#</a></h2>\n<p>Both <code>Chrome</code> and <code>Body</code> may read component state, since projecting state to UI is their whole\npurpose, but neither may mutate it. <code>Body</code> here means the <code>BodyComponentBase</code> one, not the layout's\nrouted-content parameter. Mutating state inside either reports BCF3001, the same diagnostic that\napplies to a regular component's <code>Body</code>.</p>\n<h2 id=\"next\">Next<a href=\"#next\" class=\"headlink\" aria-label=\"Permalink to this section\">#</a></h2>\n<p>See <a href=\"/docs/components-and-reuse\">components and reuse</a> for calling one component from another, or\n<a href=\"/docs/elements-and-decorations\">elements and decorations</a> for the element vocabulary used above.</p>\n"),
        new DocEntry(
            "two-way-binding",
            "Two-Way Binding",
            60,
            "en",
            false,
            "<p>A two-way binding is one decoration that writes a value out to the DOM and reads the user's edit back\ninto your state. <code>.Bind</code> is Razor's <code>@bind</code>, spelled so that everything it needs is an argument you can\nsee. The generator lowers it to an attribute frame, an event frame carrying Blazor's own\n<code>CreateBinder</code>, and — when the bound attribute is <code>value</code> or <code>checked</code> — the DOM resynchronization\nthat keeps the element honest. Nothing generated compiles an expression tree at runtime, and nothing\ngenerated reflects; binding an enum reaches one reflective lookup inside the framework's own\nconverter.</p>\n<h2 id=\"binding-an-element\">Binding an element<a href=\"#binding-an-element\" class=\"headlink\" aria-label=\"Permalink to this section\">#</a></h2>\n<p>Name the attribute that carries the value, the event that reports a change, and a lambda that reads\nthe current value:</p>\n<div class=\"csharp\"><pre>\n<span class=\"keyword\">public</span> <span class=\"keyword\">partial</span> <span class=\"keyword\">class</span> NameField : BodyComponentBase\n{\n    <span class=\"keyword\">private</span> <span class=\"keyword\">string</span> _name = <span class=\"string\">&quot;&quot;</span>;\n\n    <span class=\"keyword\">protected</span> <span class=\"keyword\">override</span> View Body =&gt;\n        Div[\n            Input.Type(<span class=\"string\">&quot;text&quot;</span>).Bind(<span class=\"string\">&quot;value&quot;</span>, <span class=\"string\">&quot;oninput&quot;</span>, () =&gt; _name),\n            P[$<span class=\"string\">&quot;Hello, {_name}&quot;</span>]];\n}\n</pre></div>\n<p>The lambda is read in both directions. Its body becomes the attribute's value, and — in this\ngetter-only form — the same body becomes the left-hand side of the assignment the generator writes\nfor you. So the target has to be assignable: a field, a property with a setter, or a path through\neither (<code>_form.Name</code>, <code>Model.Items[0].Title</code>, <code>_dict[&quot;k&quot;]</code>). A computed expression such as\n<code>() =&gt; _name.ToUpper()</code> reports BCF3018, and the way to write that is the explicit setter below.</p>\n<p>Use <code>&quot;oninput&quot;</code> to bind on every keystroke and <code>&quot;onchange&quot;</code> to bind when the element loses focus.\nThose are the two you will usually want, but no list restricts the pair. Three rules do:</p>\n<ul>\n<li>both names have to be non-empty compile-time constants (BCF3011)</li>\n<li>the event name has to start with <code>on</code> (BCF3019)</li>\n<li>neither may already be bound on the same element by another decoration (BCF3010)</li>\n</ul>\n<p>Nothing checks the names against HTML.</p>\n<h2 id=\"why-you-write-both-names\">Why you write both names<a href=\"#why-you-write-both-names\" class=\"headlink\" aria-label=\"Permalink to this section\">#</a></h2>\n<p>Razor infers the attribute from the markup: it reads the literal <code>type=&quot;checkbox&quot;</code> out of your <code>.razor</code>\nfile and binds <code>checked</code> instead of <code>value</code>. This surface has no literal to read. The tag is a string\nand <code>type</code> is an expression — <code>Input.Type(kind)</code> is an ordinary C# call whose value may not be known\nuntil it runs — so there is nothing an inference could be checked against.</p>\n<p>Defaulting to <code>value</code> would then produce the one failure worth going out of the way to avoid: a\ncheckbox bound to the wrong attribute, silently, with no diagnostic to tell you. So the rule across\nthe whole surface is <strong>infer only what you can verify</strong>. The element side cannot verify, so it does\nnot infer, and you write two short strings instead.</p>\n<p>The half of the mistake that <em>is</em> checkable is caught: an event name that does not start with <code>on</code>\nreports BCF3019, so swapping the two arguments stops at compile time rather than adding a dead\nattribute.</p>\n<p>The component side of <code>.Bind</code> does infer names, because the same rule allows it there — see\n<a href=\"#binding-a-component-parameter\">binding a component parameter</a>.</p>\n<h2 id=\"checkboxes-bind-checked\">Checkboxes bind <code>checked</code><a href=\"#checkboxes-bind-checked\" class=\"headlink\" aria-label=\"Permalink to this section\">#</a></h2>\n<p>A checkbox binds a <code>bool</code> to the <code>checked</code> attribute, on <code>onchange</code>:</p>\n<div class=\"csharp\"><pre>\n<span class=\"keyword\">private</span> <span class=\"keyword\">bool</span> _agreed;\n\n<span class=\"keyword\">protected</span> <span class=\"keyword\">override</span> View Body =&gt;\n    Label[\n        Input.Type(<span class=\"string\">&quot;checkbox&quot;</span>).Bind(<span class=\"string\">&quot;checked&quot;</span>, <span class=\"string\">&quot;onchange&quot;</span>, () =&gt; _agreed),\n        <span class=\"string\">&quot; I agree&quot;</span>];\n</pre></div>\n<p><code>bool</code> is HTML's boolean-attribute form, the same one\n<a href=\"/docs/elements-and-decorations#decorations\"><code>.Attr</code> takes</a>: <code>true</code> renders the attribute with an empty\nvalue, <code>false</code> leaves it out entirely.</p>\n<h2 id=\"normalizing-with-an-explicit-setter\">Normalizing with an explicit setter<a href=\"#normalizing-with-an-explicit-setter\" class=\"headlink\" aria-label=\"Permalink to this section\">#</a></h2>\n<p>A fourth argument replaces the generated assignment with your own setter, which is where validation,\nnormalization, or any work that has to happen on each edit belongs:</p>\n<div class=\"csharp\"><pre>\n<span class=\"keyword\">private</span> <span class=\"keyword\">string</span> _name = <span class=\"string\">&quot;&quot;</span>;\n\n<span class=\"keyword\">protected</span> <span class=\"keyword\">override</span> View Body =&gt;\n    Input.Type(<span class=\"string\">&quot;text&quot;</span>).Bind(<span class=\"string\">&quot;value&quot;</span>, <span class=\"string\">&quot;oninput&quot;</span>, () =&gt; _name, v =&gt; _name = v.Trim());\n</pre></div>\n<p>This covers what Razor splits across <code>@bind:get</code> / <code>@bind:set</code> and <code>@bind:after</code>: the setter <em>is</em> the\nwrite, so anything you would have run afterwards goes in the same lambda. Note that the write is now\nyours to perform — nothing assigns for you once you supply a setter. A method group works too\n(<code>SetName</code>), the lambda may have a block body, and an <code>async</code> variant is available by returning\n<code>Task</code>:</p>\n<div class=\"csharp\"><pre>\nInput.Type(<span class=\"string\">&quot;text&quot;</span>).Bind(<span class=\"string\">&quot;value&quot;</span>, <span class=\"string\">&quot;oninput&quot;</span>, () =&gt; _query, <span class=\"keyword\">async</span> v =&gt;\n{\n    _query = v;\n    <span class=\"keyword\">await</span> SearchAsync(v);\n});\n</pre></div>\n<p>Only the getter-only form needs an assignable getter. With a setter, the getter is only ever read, so\nany expression will do.</p>\n<p>A normalizing setter creates a divergence: the element shows what was typed, while your field holds\nthe trimmed value. Ordinary diffing writes nothing, because the render tree has not changed since the\nlast render. On a <code>value</code> or <code>checked</code> binding, <code>.Bind</code> registers that attribute for DOM\nresynchronization to close the gap, so the element is corrected to show the normalized value. You get\nthis without asking for it.</p>\n<p>Those two names are the whole of it. Blazor's client sends back a form element's own <code>value</code> — or\n<code>checked</code> for a checkbox — and nothing else, so those are the only two names the generator registers.\nA binding to any other attribute registers nothing; <code>.Bind(&quot;hue&quot;, &quot;onhuechange&quot;, () =&gt; _hue, Normalize)</code>\non a custom element is the usual shape. The setter still runs and the new value still reaches the DOM\nby ordinary diffing on the next render. What is missing is the repair above, for the case where\nnormalizing leaves the render tree unchanged and the element goes on showing what was typed.</p>\n<p>The setter receives <code>&quot;&quot;</code> for an emptied text input, never <code>null</code> — which is why it takes a\nnon-nullable <code>string</code>. Writing to your own state from the setter is allowed, even though writing to\nstate anywhere else in a <code>Body</code> reports BCF3001: a setter is a deferred handler, like an <code>.OnClick</code>\nlambda, and does not run while the tree is being built.</p>\n<h2 id=\"numbers-dates-and-enums\">Numbers, dates, and enums<a href=\"#numbers-dates-and-enums\" class=\"headlink\" aria-label=\"Permalink to this section\">#</a></h2>\n<p>Any type binds, as long as you write the culture. It is the last argument and it cannot be omitted:</p>\n<div class=\"csharp\"><pre>\n<span class=\"keyword\">private</span> <span class=\"keyword\">int</span> _age;\n\n<span class=\"keyword\">protected</span> <span class=\"keyword\">override</span> View Body =&gt;\n    Input.Type(<span class=\"string\">&quot;number&quot;</span>).Bind(<span class=\"string\">&quot;value&quot;</span>, <span class=\"string\">&quot;oninput&quot;</span>, () =&gt; _age, CultureInfo.InvariantCulture);\n</pre></div>\n<p>The culture formats the value on the way out and parses it on the way back, through Blazor's own\n<code>BindConverter</code>. Numbers, dates, times, <code>Guid</code>, enums, and every nullable form of those all work,\nbecause the conversion is the framework's rather than this library's.</p>\n<p>It is an argument rather than a default because the alternative is a culture chosen out of your sight.\nRazor picks one from the element's literal <code>type</code> — the literal this surface does not read, for the\nsame reason it does not infer the attribute name. So the choice moves to where you can see it.</p>\n<h3 id=\"write-the-invariant-culture-for-number-and-date\">Write the invariant culture for <code>number</code> and <code>date</code><a href=\"#write-the-invariant-culture-for-number-and-date\" class=\"headlink\" aria-label=\"Permalink to this section\">#</a></h3>\n<p><code>&lt;input type=&quot;number&quot;&gt;</code> and <code>&lt;input type=&quot;date&quot;&gt;</code> are defined in terms of a fixed format, not the\nuser's locale. Binding either under <code>CultureInfo.CurrentCulture</code> produces a value the element rejects\nas soon as the current locale writes decimals with a comma.</p>\n<p><strong>This is not diagnosed.</strong> The check would need to read <code>type</code>, and <code>type</code> is an expression here; a\nrule that fired only when you happened to write a literal would catch the same mistake in one spelling\nand miss it in another. Write <code>CultureInfo.InvariantCulture</code> for those two, and use the current\nculture for text the user reads as prose.</p>\n<h3 id=\"a-value-that-will-not-parse-is-put-back\">A value that will not parse is put back<a href=\"#a-value-that-will-not-parse-is-put-back\" class=\"headlink\" aria-label=\"Permalink to this section\">#</a></h3>\n<p>If the converter cannot read what was typed, your setter is never called and both your field and the\nelement return to the previous value. That is Blazor's behaviour, and <code>.Bind</code> reaches it through the\nsame DOM resynchronization described above.</p>\n<p>It has a consequence worth choosing deliberately. On <code>&quot;oninput&quot;</code> the reversion runs on every\nkeystroke, so typing <code>4.5</code> into an <code>int</code> binding leaves you with <code>45</code> — the <code>.</code> is rejected and\nremoved before the <code>5</code> arrives. The empty string does not parse either, so the field cannot be\ncleared: emptying it puts the previous value straight back. For numeric input that is usually not\nwhat you want:</p>\n<div class=\"csharp\"><pre>\n<span class=\"comment\">// Reverts on blur, so a half-typed number survives being typed.</span>\nInput.Type(<span class=\"string\">&quot;number&quot;</span>).Bind(<span class=\"string\">&quot;value&quot;</span>, <span class=\"string\">&quot;onchange&quot;</span>, () =&gt; _amount, CultureInfo.InvariantCulture);\n</pre></div>\n<p><code>&quot;oninput&quot;</code> is still right when every intermediate value is meaningful — a range slider, or a text\nfield whose type accepts anything the user can type.</p>\n<h3 id=\"dates-need-a-format\">Dates need a format<a href=\"#dates-need-a-format\" class=\"headlink\" aria-label=\"Permalink to this section\">#</a></h3>\n<p>A date input requires <code>yyyy-MM-dd</code>, and this surface cannot supply that from <code>type</code> the way Razor\ndoes. Write it as the argument before the culture:</p>\n<div class=\"csharp\"><pre>\n<span class=\"keyword\">private</span> DateOnly _due = <span class=\"keyword\">new</span>(<span class=\"number\">2026</span>, <span class=\"number\">8</span>, <span class=\"number\">14</span>);\n\n<span class=\"keyword\">protected</span> <span class=\"keyword\">override</span> View Body =&gt;\n    Input.Type(<span class=\"string\">&quot;date&quot;</span>).Bind(\n        <span class=\"string\">&quot;value&quot;</span>, <span class=\"string\">&quot;oninput&quot;</span>, () =&gt; _due, <span class=\"string\">&quot;yyyy-MM-dd&quot;</span>, CultureInfo.InvariantCulture);\n</pre></div>\n<p>A format is accepted for <code>DateTime</code>, <code>DateTimeOffset</code>, <code>DateOnly</code>, <code>TimeOnly</code> and their nullable\nforms, and for nothing else — those are the types the framework declares a format-taking converter\nfor. Writing one for an <code>int</code> reports BCF3031. To format a number, format it in the getter and parse\nit in an explicit setter instead.</p>\n<h3 id=\"if-you-publish-trimmed\">If you publish trimmed<a href=\"#if-you-publish-trimmed\" class=\"headlink\" aria-label=\"Permalink to this section\">#</a></h3>\n<p>Binding any value type roots Blazor's <code>BindConverter</code> whole, including converters for types you never\nbind. Measured on a trimmed self-contained publish, that is about 10 KB of\n<code>Microsoft.AspNetCore.Components.dll</code>. An app that binds only <code>string</code> and <code>bool</code> does not pay it. It\nis a one-off, not per binding.</p>\n<h3 id=\"doing-the-conversion-yourself\">Doing the conversion yourself<a href=\"#doing-the-conversion-yourself\" class=\"headlink\" aria-label=\"Permalink to this section\">#</a></h3>\n<p>The explicit form still works, and is what you want when the value needs validating rather than\nconverting:</p>\n<div class=\"csharp\"><pre>\n<span class=\"keyword\">private</span> <span class=\"keyword\">decimal</span> _amount;\n\n<span class=\"keyword\">protected</span> <span class=\"keyword\">override</span> View Body =&gt;\n    Input.Type(<span class=\"string\">&quot;text&quot;</span>).Bind(\n        <span class=\"string\">&quot;value&quot;</span>, <span class=\"string\">&quot;onchange&quot;</span>,\n        () =&gt; _amount.ToString(CultureInfo.InvariantCulture),\n        v =&gt; _amount = <span class=\"keyword\">decimal</span>.TryParse(v, NumberStyles.Number, CultureInfo.InvariantCulture, <span class=\"keyword\">out</span> <span class=\"keyword\">var</span> d)\n            ? d\n            : _amount);\n</pre></div>\n<p>Use <code>TryParse</code> rather than <code>Parse</code> here. An exception thrown from a setter is not the framework's\nrejection path; it faults the render.</p>\n<h2 id=\"binding-a-component-parameter\">Binding a component parameter<a href=\"#binding-a-component-parameter\" class=\"headlink\" aria-label=\"Permalink to this section\">#</a></h2>\n<p>On a component the names are derived rather than written, because there the derivation can be checked.\n<code>.Bind</code> selects the parameter with a lambda and the generator appends <code>{Name}Changed</code>, plus\n<code>{Name}Expression</code> when the component declares it:</p>\n<div class=\"csharp\"><pre>\n<span class=\"keyword\">using</span> Microsoft.AspNetCore.Components.Forms;\n\n<span class=\"keyword\">private</span> <span class=\"keyword\">readonly</span> NameModel _model = <span class=\"keyword\">new</span>();\n\n<span class=\"keyword\">protected</span> <span class=\"keyword\">override</span> View Body =&gt;\n    Component&lt;InputText&gt;().Bind(c =&gt; c.Value, () =&gt; _model.Name);\n</pre></div>\n<p>That single call supplies <code>Value</code>, <code>ValueChanged</code>, and <code>ValueExpression</code>. Every derived name is looked\nup on the component type, so a missing or mistyped <code>{Name}Changed</code> reports BCF3020 rather than binding\nnothing. This is what makes the asymmetry with the element side a rule and not an inconsistency.</p>\n<p><code>{Name}Expression</code> is the reason the target is written as a getter lambda rather than passed by\nreference. A component under an <code>EditForm</code> resolves a <code>FieldIdentifier</code> from that expression, and the\nidentifier is what ties the input to a property of your model, so validation messages land on the right\nfield. No other spelling could supply it.</p>\n<p>The <code>EditForm</code> around it is written the same way. <code>EditForm.ChildContent</code> is a\n<code>RenderFragment&lt;EditContext&gt;</code>, and the brackets supply it with the context discarded, so the content\nbelow needs nothing else. Content that reads the <code>EditContext</code> is named with <code>.Template</code> instead:</p>\n<div class=\"csharp\"><pre>\n<span class=\"keyword\">protected</span> <span class=\"keyword\">override</span> View Body =&gt;\n    Component&lt;EditForm&gt;()\n        .Param(form =&gt; form.Model, _model)[\n            Component&lt;NameFields&gt;().Param(fields =&gt; fields.Value, _model)];\n</pre></div>\n<p>The bound inputs may sit directly in the brackets or in their own component; the binding above, and\nthe <code>ValueExpression</code> it supplies, are unaffected either way. See\n<a href=\"/docs/components-and-reuse#generic-fragment-parameters\">generic fragment parameters</a> for the spelling\nthat reads the <code>EditContext</code>, and for when a cached delegate through <code>.Param</code> is the better choice.</p>\n<p>An explicit setter and an <code>async</code> setter are available here too, with the same meaning as on an\nelement. <code>TValue</code> takes no culture and no format: the value goes to a parameter rather than to the\nDOM, so nothing formats or parses it on the way and there is no choice to write down.</p>\n<p>Remember that <code>Component&lt;T&gt;()</code> cannot name a <code>.razor</code> component declared in the same project\n(<a href=\"/docs/components-and-reuse#calling-an-existing-razor-or-third-party-component\">BCF3012</a>). Framework\ncomponents such as <code>InputText</code>, and hand-written C# components, always resolve.</p>\n<h2 id=\"what-is-checked\">What is checked<a href=\"#what-is-checked\" class=\"headlink\" aria-label=\"Permalink to this section\">#</a></h2>\n<ul>\n<li><strong>BCF3017</strong> — the getter is not an inline lambda with an expression body, such as a block-bodied\nlambda or a method group. Its body has to be extractable, because it is copied into both the\nattribute value and the binder. The setter has no such restriction; it is only handed over.</li>\n<li><strong>BCF3018</strong> — the getter-only form's getter body is not assignable. Calls and operators, get-only\nproperties, <code>readonly</code> fields, and locals or <code>ForEach</code> iteration variables themselves are rejected;\na <em>member</em> of an iteration variable (<code>o.Title</code>) is fine, since writing it changes the underlying\nitem. Write an explicit setter instead.</li>\n<li><strong>BCF3019</strong> — the event name does not start with <code>on</code>. Such a name would be added as a plain\nattribute and the handler would never fire, so this is what catches the two names swapped.</li>\n<li><strong>BCF3020</strong> — the component's <code>{Name}Changed</code> parameter is missing or is not an\n<code>EventCallback&lt;TValue&gt;</code>.</li>\n<li><strong>BCF3024</strong> — the attribute name is <code>class</code> and the element also carries a <code>.Class</code> or an\n<code>.Attr(&quot;class&quot;, …)</code>. Those fold into one attribute and the binding does not join them, so the\nelement would carry <code>class</code> twice.</li>\n<li><strong>BCF3031</strong> — a format is written for a type the framework declares no format-taking converter for.\nOnly <code>DateTime</code>, <code>DateTimeOffset</code>, <code>DateOnly</code>, <code>TimeOnly</code> and their nullable forms accept one.</li>\n</ul>\n<p>An element may carry more than one <code>.Bind</code>. If two of them share an attribute name or an event name,\nthat is BCF3010, the same duplicate any two decorations would report. DOM resynchronization — the\nrepair that puts a normalized value back over what the user typed — applies to <code>value</code> and <code>checked</code>\nonly, because those are the only two the browser sends back with the event.</p>\n<h2 id=\"next\">Next<a href=\"#next\" class=\"headlink\" aria-label=\"Permalink to this section\">#</a></h2>\n<p>See <a href=\"/docs/elements-and-decorations#decorations\">elements and decorations</a> for the one-way <code>.Attr</code> and\n<code>.On</code> this is built from, or <a href=\"/docs/components-and-reuse\">components and reuse</a> for <code>.Param</code> and the\nrest of the component surface.</p>\n"),
        new DocEntry(
            "getting-started",
            "はじめに",
            10,
            "ja",
            false,
            "<p>BlazorCodeFirst を使うと、Blazor の UI をそのままの C# で書けます。このページ自体も Markdown\nから作られていて、ビルド時に HTML へ変換したものを <code>Html.Raw</code> で流し込んでいます。</p>\n<h2 id=\"導入\">導入<a href=\"#%E5%B0%8E%E5%85%A5\" class=\"headlink\" aria-label=\"Permalink to this section\">#</a></h2>\n<p>ランタイムとソースジェネレーターをプロジェクトに追加し、コンポーネントを <code>BodyComponentBase</code>\nから派生させます。</p>\n<h2 id=\"最初のコンポーネント\">最初のコンポーネント<a href=\"#%E6%9C%80%E5%88%9D%E3%81%AE%E3%82%B3%E3%83%B3%E3%83%9D%E3%83%BC%E3%83%8D%E3%83%B3%E3%83%88\" class=\"headlink\" aria-label=\"Permalink to this section\">#</a></h2>\n<div class=\"csharp\"><pre>\n<span class=\"keyword\">using</span> Microsoft.AspNetCore.Components;\n<span class=\"keyword\">using</span> BlazorCodeFirst;\n<span class=\"keyword\">using</span> <span class=\"keyword\">static</span> BlazorCodeFirst.Html;\n\n[Route(<span class=\"string\">&quot;/&quot;</span>)]\n<span class=\"keyword\">public</span> <span class=\"keyword\">partial</span> <span class=\"keyword\">class</span> Home : BodyComponentBase\n{\n    <span class=\"keyword\">protected</span> <span class=\"keyword\">override</span> View Body =&gt;\n        Div[\n            H1[<span class=\"string\">&quot;Hello&quot;</span>],\n            Span[<span class=\"string\">&quot;Welcome to BlazorCodeFirst.&quot;</span>]];\n}\n</pre></div><h2 id=\"クラスに求められる形\">クラスに求められる形<a href=\"#%E3%82%AF%E3%83%A9%E3%82%B9%E3%81%AB%E6%B1%82%E3%82%81%E3%82%89%E3%82%8C%E3%82%8B%E5%BD%A2\" class=\"headlink\" aria-label=\"Permalink to this section\">#</a></h2>\n<p>ジェネレーターは、宣言されたクラスの中に <code>RenderView</code> のオーバーライドを書き込みます。だから\nそのクラスは、書き込める形でなければなりません。受け付けない形が3つあり、それぞれに専用の\n診断があります。</p>\n<p>クラスは <code>partial</code> である必要があります。でなければ BCF1001 を報告します。修飾子が要るのは、\nオーバーライドを宣言するクラスだけです。次の3つには何も生成されないので、修飾子は要りません。</p>\n<ul>\n<li>間に挟まる抽象基底クラス</li>\n<li>基底が既にオーバーライドを宣言している末端のクラス</li>\n<li>再抽象化したクラス</li>\n</ul>\n<p>クラスはトップレベルである必要があります。でなければ BCF1005 を報告します。生成コードは、\n外側の型宣言の連なりを、その型引数まで含めて再現できません。だから入れ子のコンポーネントは、\n中途半端に生成せず拒否します。この診断がなければ、入れ子であることは抽象メンバー <code>RenderView</code>\nに対する CS0534 としてしか現れません。CS0534 が名指しするのは足りないメンバーだけです。どの型\nの入れ子になっているかは教えてくれません。</p>\n<p>ゲッターは1つの <code>return</code> へ到達する必要があります。でなければ BCF1004 を報告します。書き方は3\n通りあり、どれも同じものに変換されます。</p>\n<div class=\"csharp\"><pre>\n<span class=\"keyword\">protected</span> <span class=\"keyword\">override</span> View Body =&gt; Div[H1[<span class=\"string\">&quot;Hello&quot;</span>]];              <span class=\"comment\">// これでよい</span>\n<span class=\"keyword\">protected</span> <span class=\"keyword\">override</span> View Body { <span class=\"keyword\">get</span> =&gt; Div[H1[<span class=\"string\">&quot;Hello&quot;</span>]]; }      <span class=\"comment\">// これでもよい</span>\n<span class=\"keyword\">protected</span> <span class=\"keyword\">override</span> View Body { <span class=\"keyword\">get</span> { <span class=\"keyword\">return</span> Div[H1[<span class=\"string\">&quot;Hello&quot;</span>]]; } }   <span class=\"comment\">// これでもよい</span>\n</pre></div>\n<p>その <code>return</code> の手前には、ローカル変数の宣言と式文を置けます。書いた文は生成された\n<code>RenderView</code> のフレーム発行の手前へ移植されます。<code>ForEach</code> のコンテンツブロックに書いた文が\n落ちる場所と同じです。</p>\n<div class=\"csharp\"><pre>\n<span class=\"keyword\">protected</span> <span class=\"keyword\">override</span> View Body                                   <span class=\"comment\">// これでよい</span>\n{\n    <span class=\"keyword\">get</span>\n    {\n        <span class=\"keyword\">var</span> greeting = $<span class=\"string\">&quot;Hello, {_name}&quot;</span>;\n        <span class=\"keyword\">return</span> Div[H1[greeting]];\n    }\n}\n</pre></div>\n<p>残った形は BCF1004 のままです。</p>\n<div class=\"csharp\"><pre>\n<span class=\"keyword\">protected</span> <span class=\"keyword\">override</span> View Body                                   <span class=\"comment\">// BCF1004: 2つ目の return がある</span>\n{\n    <span class=\"keyword\">get</span>\n    {\n        <span class=\"keyword\">if</span> (_name <span class=\"keyword\">is</span> <span class=\"keyword\">null</span>)\n            <span class=\"keyword\">return</span> Div[H1[<span class=\"string\">&quot;Hello&quot;</span>]];\n\n        <span class=\"keyword\">return</span> Div[H1[$<span class=\"string\">&quot;Hello, {_name}&quot;</span>]];\n    }\n}\n\n<span class=\"keyword\">protected</span> <span class=\"keyword\">override</span> View Body { <span class=\"keyword\">get</span>; } = <span class=\"keyword\">default</span>;               <span class=\"comment\">// BCF1004: 自動プロパティにはゲッターの本体がない</span>\n</pre></div>\n<p>2つ目の <code>return</code> とネイティブの制御構文は、それぞれ専用のシーケンス空間を要します。自動プロパ\nティには、そもそもゲッターの本体がありません。BCF1004 が咎めるのは宣言そのものです。ここが\nBCF1003 との違いです。BCF1003 は、ゲッターの形は問題なく、その中に書かれた何かを順序付けでき\nなかった、という意味です。本体がどうしてもこの形にならないなら、<code>RenderView</code> を手で書いてくだ\nさい。そのとき設計時の式は使われなくなり、何も報告されません。</p>\n<p>文は翻訳されるだけで、実行はされません。設計時の式が不活性であることは変わらないので、その中\nで状態を書き換えれば、これまでどおり BCF3001 です。</p>\n<p>1つのクラスが2つの誤りを同時に抱えることはあります。<code>partial</code> が抜けていて、かつゲッターが\n変換できない、という具合に。それでも知らされるのは一度に1つです。<code>partial</code> の検査が先に走る\nので、まず BCF1001 だけが出ます。修飾子を足して、はじめて BCF1004 が現れます。</p>\n<h2 id=\"この-api-が意味を持つ場所\">この API が意味を持つ場所<a href=\"#%E3%81%93%E3%81%AE-api-%E3%81%8C%E6%84%8F%E5%91%B3%E3%82%92%E6%8C%81%E3%81%A4%E5%A0%B4%E6%89%80\" class=\"headlink\" aria-label=\"Permalink to this section\">#</a></h2>\n<p><code>Html.Div</code>、<code>.Class(...)</code>、<code>.OnClick(...)</code> をはじめ、要素のファクトリと装飾は、すべて中身が\nありません。<code>View</code> は空の構造体で、要素ヘルパーは何も返さず、装飾はレシーバーをそのまま返す\nだけです。ジェネレーターが読むのは書かれた <em>構文</em> であって、値ではありません。読む場所も3つ\nしかありません。コンポーネントの <code>Body</code>、レイアウトの <code>Chrome</code>、そして <code>[ViewPart]</code> メソッドの\n本体です。</p>\n<p>同じ API はどこからでも呼べて、その3つ以外では何の意味も持ちません。イベントハンドラーの中で\nも、サービスの中でも、ヘルパーメソッドの中でも書けます。コンパイルは通り、何かを組み立てたよ\nうに見えます。そして何も起きません。出力もされず、ハンドラーも繋がりません。</p>\n<div class=\"csharp\"><pre>\n<span class=\"keyword\">private</span> <span class=\"keyword\">void</span> OnSomething()\n{\n    <span class=\"comment\">// BCF3029: 何も描画されず、DoThing も呼ばれない</span>\n    <span class=\"keyword\">var</span> card = Div.Class(<span class=\"string\">&quot;card&quot;</span>).OnClick(DoThing)[Span[<span class=\"string\">&quot;hello&quot;</span>]];\n}\n</pre></div>\n<p>これを名指しするのが BCF3029 です。読まれる3つの場所を分けているのは、返り値の設計時型です。\nだからどれも特別扱いではありません。<code>If</code> や <code>ForEach</code> の内容のように <code>View</code> を返すラムダも、\n同じように読まれます。設計時型の値をフィールドやプロパティに取っておくのは咎めません。報告\nするのは、ローカル変数、破棄、引数の場合だけです。</p>\n<h2 id=\"生成コードに写される値\">生成コードに写される値<a href=\"#%E7%94%9F%E6%88%90%E3%82%B3%E3%83%BC%E3%83%89%E3%81%AB%E5%86%99%E3%81%95%E3%82%8C%E3%82%8B%E5%80%A4\" class=\"headlink\" aria-label=\"Permalink to this section\">#</a></h2>\n<p>BlazorCodeFirst は、設計時の値の式を、<code>using</code> を一切持たない生成ファイルへ写します。解決済み\nの型名は <code>global::</code> 修飾に書き換えます。解決できないままで、かつその綴りが元ファイルの字句的\nな文脈に依存している型があれば、その型名の位置で BCF3015 を報告します。</p>\n<p>名前を直すか、完全修飾するか、ソース生成された型を参照プロジェクトへ移すか、手書きの C# の型\nに置き換えてください。既に <code>global::</code> から始まっている参照はそのまま残し、通常の C# の解決に\n任せます。ジェネリックの型引数は、それぞれ独立に検査します。</p>\n<h2 id=\"次に読むもの\">次に読むもの<a href=\"#%E6%AC%A1%E3%81%AB%E8%AA%AD%E3%82%80%E3%82%82%E3%81%AE\" class=\"headlink\" aria-label=\"Permalink to this section\">#</a></h2>\n<ul>\n<li><a href=\"/counter\">カウンターの実例</a>で、イベント、<code>If</code>、キー付き <code>ForEach</code> の動きを見る。</li>\n<li><a href=\"#%E5%B0%8E%E5%85%A5\">導入</a>や<a href=\"#%E6%9C%80%E5%88%9D%E3%81%AE%E3%82%B3%E3%83%B3%E3%83%9D%E3%83%BC%E3%83%8D%E3%83%B3%E3%83%88\">最初のコンポーネント</a>へ戻る。</li>\n<li><a href=\"/docs/ja/elements-and-decorations\">要素と装飾</a>で要素の語彙を、<a href=\"/docs/ja/control-flow#if\">制御構文</a>で\n条件分岐とリストを学ぶ。</li>\n</ul>\n"),
        new DocEntry(
            "elements-and-decorations",
            "要素と装飾",
            20,
            "ja",
            false,
            "<p>BlazorCodeFirst は HTML を直に写します。<code>Body</code> の式に書いた要素の名前が、そのまま出力される要素\nの名前です。あいだに挟まるウィジェットの語彙を覚える必要はなく、実行時の UI ツリーもありません。\nソースジェネレーターが、これらの呼び出しをコンパイル時に <code>RenderTreeBuilder</code> の命令へ変換します。</p>\n<h2 id=\"要素\">要素<a href=\"#%E8%A6%81%E7%B4%A0\" class=\"headlink\" aria-label=\"Permalink to this section\">#</a></h2>\n<p>要素のヘルパーは、文字列と要素を混ぜた子を角括弧で受け取ります。</p>\n<div class=\"csharp\"><pre>\n<span class=\"keyword\">using</span> BlazorCodeFirst;\n<span class=\"keyword\">using</span> <span class=\"keyword\">static</span> BlazorCodeFirst.Html;\n\n<span class=\"keyword\">protected</span> <span class=\"keyword\">override</span> View Body =&gt;\n    Article[\n        H2[<span class=\"string\">&quot;要素&quot;</span>],\n        P[<span class=\"string\">&quot;テキストと &quot;</span>, A.Href(<span class=\"string\">&quot;/docs&quot;</span>)[<span class=\"string\">&quot;リンク&quot;</span>], <span class=\"string\">&quot; は1回の呼び出しで並びます。&quot;</span>],\n        Ul[\n            Li[<span class=\"string\">&quot;HTML の要素にはヘルパーがあります。名前はタグの先頭1文字だけを大文字にした綴りで、FigCaption ではなく Figcaption、同じく Colgroup と Textarea です。&quot;</span>],\n            Li[<span class=\"string\">&quot;ヘルパーの無いものを Element が受け持ちます。カスタム要素、Web Components、そして下に挙げるいくつかの語彙です。&quot;</span>]]];\n</pre></div>\n<p>HTML Living Standard が適合と認める要素には、すべて専用のヘルパーがあります。だから <code>&lt;figure&gt;</code>\nも呼び出しひとつです。属性は子より前、ほかの要素と同じです。</p>\n<div class=\"csharp\"><pre>\nFigure[\n    Img.Src(<span class=\"string\">&quot;/diagram.png&quot;</span>).Alt(<span class=\"string\">&quot;Architecture&quot;</span>),\n    Figcaption[<span class=\"string\">&quot;The compilation pipeline&quot;</span>]]\n</pre></div>\n<p><code>Element</code> は、そこから漏れたものを受け持ちます。届くのは2種類です。ひとつはカスタム要素と Web\nComponents で、そのタグ名にヘルパーはありません。もうひとつは、ヘルパーが用意されていない数少\nない標準要素です。</p>\n<ul>\n<li>文書そのものと <code>&lt;head&gt;</code> の中だけに現れる要素。\n<code>html</code>、<code>head</code>、<code>body</code>、<code>title</code>、<code>base</code>、<code>meta</code>、<code>link</code></li>\n<li>生テキスト要素。<code>script</code>、<code>style</code>、<code>noscript</code></li>\n<li>レンダーツリーが意味を与えられない要素。<code>template</code>、<code>slot</code></li>\n<li><code>object</code>。C# のキーワードと綴りがぶつかります</li>\n<li>別の語彙である <code>svg</code> と <code>math</code>。その配下すべて</li>\n</ul>\n<div class=\"csharp\"><pre>\nElement(<span class=\"string\">&quot;my-widget&quot;</span>).Attr(<span class=\"string\">&quot;value&quot;</span>, <span class=\"string\">&quot;42&quot;</span>)   <span class=\"comment\">// カスタム要素</span>\nElement(<span class=\"string\">&quot;svg&quot;</span>)[Element(<span class=\"string\">&quot;circle&quot;</span>)]          <span class=\"comment\">// 別の語彙</span>\n</pre></div>\n<p>タグは空でないコンパイル時定数、つまりリテラルか <code>const</code> である必要があります。でなければ\nBCF3009 を報告します。<code>Element</code> はタグをリテラルの <code>OpenElement</code> に落とします。定数に縛ることで、\nその呼び出しはヘルパーと同じくらい読めるものになります。この規則が守っているのは安全性ではなく、\n宣言的であることです。計算されたタグは、インジェクションの危険でも順序付けの問題でもありません。\nただ、書いた場所で要素が自分の名前を名乗らなくなるだけです。</p>\n<div class=\"csharp\"><pre>\n<span class=\"keyword\">private</span> <span class=\"keyword\">const</span> <span class=\"keyword\">string</span> Widget = <span class=\"string\">&quot;my-widget&quot;</span>;\n\nElement(Widget)                  <span class=\"comment\">// これでよい</span>\nElement(_kind + <span class=\"string\">&quot;-widget&quot;</span>)       <span class=\"comment\">// BCF3009</span>\nElement(<span class=\"string\">&quot;&quot;</span>)                      <span class=\"comment\">// BCF3009。タグは空にできない</span>\n</pre></div><h2 id=\"子に置けるもの\">子に置けるもの<a href=\"#%E5%AD%90%E3%81%AB%E7%BD%AE%E3%81%91%E3%82%8B%E3%82%82%E3%81%AE\" class=\"headlink\" aria-label=\"Permalink to this section\">#</a></h2>\n<p>文字列をそのまま書くと、テキストノードになります。だから <code>Text()</code> のような構文は別に用意して\nいません。子の無い要素は角括弧ごと省きます。Blazor の <code>RenderFragment</code> は、ほかの子と同じように\n子の並びへ置けます。</p>\n<div class=\"csharp\"><pre>\n[Parameter] <span class=\"keyword\">public</span> RenderFragment? ChildContent { <span class=\"keyword\">get</span>; <span class=\"keyword\">set</span>; }\n\n<span class=\"keyword\">protected</span> <span class=\"keyword\">override</span> View Body =&gt;\n    Div[\n        <span class=\"string\">&quot;plain text&quot;</span>,\n        Img.Src(<span class=\"string\">&quot;/logo.png&quot;</span>).Alt(<span class=\"string\">&quot;Logo&quot;</span>),\n        ChildContent];\n</pre></div>\n<p>ジェネレーターは、子のひとつひとつを独立した式として見られないと、シーケンス番号を振れません。\n子をコレクション式で入れ子に書くのは受け付けます。C# がそれを同じ呼び出しへ展開するからです。</p>\n<div class=\"csharp\"><pre>\nDiv[[<span class=\"string\">&quot;a&quot;</span>, <span class=\"string\">&quot;b&quot;</span>]]     <span class=\"comment\">// Div[&quot;a&quot;, &quot;b&quot;] と同じ。書くならこちら</span>\n</pre></div>\n<p>ジェネレーターが中を見通せない子の並びを渡すと、BCF1003 を報告します。当てはまるのは3つです。</p>\n<ul>\n<li>変数やメソッドの結果をまるごと渡す（<code>Div[_kids]</code>）</li>\n<li>明示的な配列（<code>Div[new View[] { … }]</code>）</li>\n<li>あらゆるスプレッド（<code>Div[[..items]]</code>）</li>\n</ul>\n<p>繰り返しには <a href=\"/docs/ja/control-flow#%E3%82%AD%E3%83%BC%E4%BB%98%E3%81%8D-foreach\"><code>ForEach</code></a> を使ってください。</p>\n<h2 id=\"空要素は子を取らない\">空要素は子を取らない<a href=\"#%E7%A9%BA%E8%A6%81%E7%B4%A0%E3%81%AF%E5%AD%90%E3%82%92%E5%8F%96%E3%82%89%E3%81%AA%E3%81%84\" class=\"headlink\" aria-label=\"Permalink to this section\">#</a></h2>\n<p>HTML 標準の空要素は13個あります。<code>area</code>、<code>base</code>、<code>br</code>、<code>col</code>、<code>embed</code>、<code>hr</code>、<code>img</code>、<code>input</code>、\n<code>link</code>、<code>meta</code>、<code>source</code>、<code>track</code>、<code>wbr</code>。どれも閉じタグを持たないので、子を書くと BCF3016 を\n報告します。</p>\n<div class=\"csharp\"><pre>\nImg.Src(<span class=\"string\">&quot;/logo.png&quot;</span>)[<span class=\"string\">&quot;Logo&quot;</span>]     <span class=\"comment\">// BCF3016</span>\nElement(<span class=\"string\">&quot;img&quot;</span>)[<span class=\"string\">&quot;Logo&quot;</span>]           <span class=\"comment\">// BCF3016。同じ規則</span>\nImg.Src(<span class=\"string\">&quot;/logo.png&quot;</span>).Alt(<span class=\"string\">&quot;Logo&quot;</span>) <span class=\"comment\">// こう書く</span>\n</pre></div>\n<p>理由は、その子が HTML を往復して生き残らないからです。プリレンダリングは、HTML パーサーが受け\n付けない閉じタグを書き出します。するとパーサーは子を要素の外へ押し出し、子は兄弟として現れます。\n行き場のない <code>&lt;/br&gt;</code> は開始タグとして読み直されるので、<code>Br[&quot;x&quot;]</code> は <code>&lt;br&gt;</code> 2つとしてプリレンダ\nリングされます。対話的なレンダリングには間にパーサーがいないので、同じ子は要素の中に入ります。\n式は1つ、DOM ツリーは2通り。ハイドレーションが引き継ぐところで、ページの形が変わります。空\n要素は装飾で設定して、中身はその隣に置いてください。</p>\n<p>検査するのは両方の書き方です。ヘルパーと、空要素のタグを渡した <code>Element</code>。カスタム要素と未知の\nタグは検査しません。<code>Element(&quot;img-viewer&quot;)[&quot;child&quot;]</code> は受け付けます。その内容モデルを読み取れる\n標準がないからです。</p>\n<p>この API が HTML について検査するのは、ここまでです。この線引きは意図したものです。BCF3016 は\n要素のタグだけで判定できます。ある子がある親の中に置けるかどうかは、そうはいきません。\n<code>Table[Div[&quot;x&quot;]]</code> もハイドレーションの後で表示が変わりますが、受け付けます。要素が定義していない\n属性（<code>Div.Href(&quot;/x&quot;)</code>）も同じです。立場の全体は <code>DESIGN.md</code> §4.1 にあります。</p>\n<h2 id=\"名前がぶつかったとき\">名前がぶつかったとき<a href=\"#%E5%90%8D%E5%89%8D%E3%81%8C%E3%81%B6%E3%81%A4%E3%81%8B%E3%81%A3%E3%81%9F%E3%81%A8%E3%81%8D\" class=\"headlink\" aria-label=\"Permalink to this section\">#</a></h2>\n<p><code>using static BlazorCodeFirst.Html;</code> は、適合する HTML 要素の名前をすべて取り込みます。そして\n単純名の解決では、自分で宣言した名前が取り込まれた名前に勝ちます。Blazor のパラメーターを\n<code>Label</code>、<code>Data</code>、<code>Summary</code>、<code>Source</code> と名付けるのはありふれているので、これは起こります。</p>\n<p>型がインデックス可能なメンバーだと、これは正しい C# になってしまいます。要素の式が、黙ってその\nメンバーへのインデクサー呼び出しに変わるからです。ジェネレーターはそれを BCF3027 で名指しします。</p>\n<div class=\"csharp\"><pre>\n[Parameter] <span class=\"keyword\">public</span> <span class=\"keyword\">string</span> Data { <span class=\"keyword\">get</span>; <span class=\"keyword\">set</span>; }\nDiv[Data[<span class=\"string\">&quot;Heading&quot;</span>]]                          <span class=\"comment\">// BCF3027</span>\n</pre></div>\n<p>自分の型、名前空間、メソッドも同じように名前を奪います。報告はどれも同じで、見つけたものを名指し\nします。</p>\n<div class=\"csharp\"><pre>\n<span class=\"keyword\">public</span> <span class=\"keyword\">sealed</span> <span class=\"keyword\">class</span> Table;                    <span class=\"comment\">// Table[&quot;x&quot;]   — BCF3027、型</span>\n<span class=\"keyword\">namespace</span> MyApp.Article { }                   <span class=\"comment\">// Article[&quot;x&quot;] — BCF3027、名前空間</span>\n<span class=\"keyword\">private</span> <span class=\"keyword\">string</span> Summary() =&gt; <span class=\"string\">&quot;&quot;</span>;               <span class=\"comment\">// Summary[&quot;x&quot;] — BCF3027、メソッド</span>\n</pre></div>\n<p>C# には、どれにもエラーがあります。インデックス引数の CS1503、そして CS0119、CS0118、CS0021。\nそのどれも目に入りません。本体が変換できないあいだ、コンポーネントには生成された <code>RenderView</code>\nがありません。だからコンパイラーはメソッド本体を束縛する前で止まります。4つのエラーは、その\n束縛の段で見つかります。</p>\n<p>直し方はどれも同じで、要素を修飾します。</p>\n<div class=\"csharp\"><pre>\nDiv[Html.Data[<span class=\"string\">&quot;Heading&quot;</span>]]\n</pre></div><h2 id=\"装飾\">装飾<a href=\"#%E8%A3%85%E9%A3%BE\" class=\"headlink\" aria-label=\"Permalink to this section\">#</a></h2>\n<p>装飾は、それが属する要素に、子より前に繋げて書きます。HTML が属性をタグの中に書くのと同じ並び\nです。装飾はラッパーのノードを作らず、持ち主の要素の属性へ畳み込まれます。</p>\n<div class=\"csharp\"><pre>\nButton\n    .Class(<span class=\"string\">&quot;btn btn-primary&quot;</span>)\n    .Title(<span class=\"string\">&quot;Save the current document&quot;</span>)\n    .OnClick(() =&gt; Save())[<span class=\"string\">&quot;Save&quot;</span>];\n</pre></div>\n<p>使える装飾は <code>.Class</code>、<code>.Id</code>、<code>.Href</code>、<code>.Src</code>、<code>.Alt</code>、<code>.Type</code>、<code>.Title</code>、<code>.Role</code>、<code>.OnClick</code>。\nそれに、汎用の逃げ道として <code>.Attr(name, value)</code> と <code>.On(eventName, handler)</code> があります。</p>\n<p><code>.On</code> には <code>on</code> を含んだ属性名をそのまま渡します（<code>.On(&quot;onmouseenter&quot;, …)</code>）。こちらで前に何かを\n付けたりはしません。<code>.Attr</code> や <code>.On</code> に渡す名前は、空でないコンパイル時定数である必要があります。\nでなければ BCF3011 を報告します。</p>\n<p><code>.Attr</code> は <code>string?</code> か <code>bool</code> を取ります。<code>bool</code> は Blazor の条件付き属性です。<code>true</code> なら値の\n空な属性として出力します。HTML が <code>disabled</code>、<code>checked</code>、<code>hidden</code> を立っていると読むのは、この形\nです。<code>false</code> なら属性ごと出しません。いつも付く属性は、HTML と同じように値なしで書いてください。\n<code>bool</code> は、付いたり付かなかったりする場合のためにあります。</p>\n<div class=\"csharp\"><pre>\nInput.Type(<span class=\"string\">&quot;checkbox&quot;</span>).Attr(<span class=\"string\">&quot;checked&quot;</span>)                    <span class=\"comment\">// &lt;input type=&quot;checkbox&quot; checked&gt;</span>\nButton.Attr(<span class=\"string\">&quot;disabled&quot;</span>, _submitting)[<span class=\"string\">&quot;Save&quot;</span>]              <span class=\"comment\">// 条件付き</span>\n</pre></div>\n<p>文字列の <code>null</code> も属性を出しません。だから、値が付くこともあれば付かないこともある属性のために、\n要素を分岐で囲む必要はありません。</p>\n<div class=\"csharp\"><pre>\nSpan.Attr(<span class=\"string\">&quot;title&quot;</span>, _hasTip ? _tip : <span class=\"keyword\">null</span>)[<span class=\"string\">&quot;Hover me&quot;</span>]\n</pre></div>\n<p><code>null</code> と <code>&quot;&quot;</code> は、どの段階でも別の値です。フレームでも、プリレンダリングされた HTML でも、再\nレンダリングでも。<code>&quot;&quot;</code> は <code>title=&quot;&quot;</code> になり、<code>null</code> では <code>title</code> そのものが出ません。再レンダリ\nングで値が null になったとき、Blazor は要素を差し替えるのではなく、すでに DOM にある要素から属性\nを取り除きます。値を1つ取る装飾は、どれもこの意味で <code>null</code> を受け付けます。<code>.Class</code>、<code>.Href</code>、\n<code>.Src</code>、<code>.Alt</code>、<code>.Id</code>、<code>.Type</code>、<code>.Title</code>、<code>.Role</code>、そして <code>.Attr</code> です。</p>\n<p><code>object</code> のオーバーロードは、あえて用意していません。ほかの型の値は、レンダリングの時点で、書式\n化を行うスレッドのカルチャーによって文字列になります。コンポーネントが動いたときのカルチャーで\nはありません。だから自分で書き出してください。そうすれば、どのカルチャーを選んだのかが目に\n見えます。</p>\n<div class=\"csharp\"><pre>\nDiv.Attr(<span class=\"string\">&quot;tabindex&quot;</span>, index.ToString(CultureInfo.InvariantCulture))\n</pre></div>\n<p><code>Action</code> や <code>Func&lt;Task&gt;</code> として書いたハンドラーは、何も受け取りません。イベントを読むには、ラムダ\nの引数に型を書きます。すると <code>.On</code> が型付きのオーバーロードを選びます。</p>\n<div class=\"csharp\"><pre>\nInput.Type(<span class=\"string\">&quot;text&quot;</span>).Attr(<span class=\"string\">&quot;value&quot;</span>, _name)\n     .On(<span class=\"string\">&quot;oninput&quot;</span>, (ChangeEventArgs e) =&gt; _name = e.Value?.ToString() ?? <span class=\"string\">&quot;&quot;</span>)\n</pre></div>\n<p>Razor と違って、引数の型はイベント名から推論されません。オーバーロードを選ぶのは、引数に書いた型\nです。<code>ChangeEventArgs</code> は <code>Microsoft.AspNetCore.Components</code> にあります。<code>MouseEventArgs</code>、\n<code>KeyboardEventArgs</code>、<code>FocusEventArgs</code> は <code>Microsoft.AspNetCore.Components.Web</code> にあります。\nこちらも、Blazor のアプリがすでに参照している名前空間です。</p>\n<p>型は推論されませんが、検査はされます。そのイベントが渡さない型を書くと BCF3028 を報告します。\n判断のもとは、Razor が使うのと同じ <code>[EventHandler]</code> のメタデータです。おかげで\n<code>.On(&quot;onclick&quot;, (KeyboardEventArgs e) =&gt; …)</code> は、ボタンが押されてから失敗するのではなく、\nコンパイル時に止まります。渡される型の基底クラスは受け付けます。ハンドラーが実際に受け取れるのが\nそれだからです。</p>\n<div class=\"csharp\"><pre>\nButton.On(<span class=\"string\">&quot;onclick&quot;</span>, (MouseEventArgs e) =&gt; Zoom(e.ClientX, e.ClientY))[<span class=\"string\">&quot;Zoom&quot;</span>]   <span class=\"comment\">// 渡される型</span>\nButton.On(<span class=\"string\">&quot;onclick&quot;</span>, (EventArgs e) =&gt; Save())[<span class=\"string\">&quot;Save&quot;</span>]                            <span class=\"comment\">// その基底。これでよい</span>\nButton.On(<span class=\"string\">&quot;onclick&quot;</span>, (KeyboardEventArgs e) =&gt; Save())[<span class=\"string\">&quot;Save&quot;</span>]                    <span class=\"comment\">// BCF3028</span>\n</pre></div>\n<p>そもそも <code>EventArgs</code> ですらない型（<code>.On(&quot;onclick&quot;, (int x) =&gt; …)</code>）も、同じ診断です。C# はその\n呼び出しを端から拒みますが、理由を名指しするのが BCF3028 です。<code>[EventHandler]</code> の登録が無い\nイベントには、照合する対応表がありません。だから登録していないカスタムイベントには何も言いません。\n登録は Blazor のふつうの仕組みで、自分のプロジェクトでの登録も読みます。</p>\n<div class=\"csharp\"><pre>\n[EventHandler(<span class=\"string\">&quot;onrate&quot;</span>, <span class=\"keyword\">typeof</span>(RatingEventArgs))]\n<span class=\"keyword\">public</span> <span class=\"keyword\">static</span> <span class=\"keyword\">class</span> AppEventHandlers;\n</pre></div>\n<p>属性を出して、イベントを受け取る。この対をひとつの装飾で書くのが\n<a href=\"/docs/ja/two-way-binding\"><code>.Bind</code></a>です。</p>\n<p><code>class</code> は、畳み込まれる唯一の属性です。<code>.Class</code> を2回以上繋げると、値は1つの <code>class</code> 属性に\nまとまります。<code>.Attr(&quot;class&quot;, …)</code> も同じところへ合流します。ほかの属性とイベントはすべて単一の\n束縛で、同じ要素に2度束縛すると BCF3010 を報告します。<code>style</code> もそのひとつです。<code>.Attr(&quot;style&quot;, …)</code>\nと書き、同じ要素に2つ置くと、畳み込まれずに BCF3010 になります。</p>\n<p>この合流は値を文字列として繋ぐので、<code>class</code> は文字列しか取らない唯一の名前です。\n<code>.Attr(&quot;class&quot;, flag)</code> は BCF3023 を報告します。<code>.Attr(&quot;class&quot;)</code> も同じです。値を書かない形は\n属性が立っていることを表すだけで、繋ぐ文字列を持ちません。条件付きのクラスは文字列で書き、消し\nたい項には <code>null</code> を渡してください。</p>\n<div class=\"csharp\"><pre>\nDiv.Class(<span class=\"string\">&quot;card&quot;</span>).Class(_selected ? <span class=\"string\">&quot;is-selected&quot;</span> : <span class=\"string\">&quot;&quot;</span>)\n</pre></div>\n<p><code>null</code> の項は合流から落ちます。だから class の装飾を1つだけ持つ要素は、その項が null のとき属性\nごと消えます。繋ぐ相手がもう1つあるときは、区切りだけが残ります。<code>_selected</code> が false のあいだ、\n<code>Div.Class(&quot;card&quot;).Class(_selected ? &quot;is-selected&quot; : null)</code> は <code>class=&quot;card &quot;</code> と出力されます。\nブラウザーはこれを <code>card</code> 1つのクラスとして読みます。</p>\n<p><code>.Bind(&quot;class&quot;, …)</code> は、この名前を書く3つ目の方法で、畳み込まれない唯一の方法です。だから\n<code>.Class</code> と併せて書いた要素は、<code>class</code> を2つ持って出力されることになります。これは BCF3024 を\n報告します。書いた順序は問いません。束縛だけを書いて、値の全体はゲッターに作らせてください。</p>\n<p>装飾は、単一の要素を相手にしなければなりません。<code>If</code>、<code>ForEach</code>、<code>Fragment</code>、<code>Raw</code>、コンポーネント\nの結果に付けると BCF3008 を報告します。それらは、付ける先の要素を開かないからです。子の後ろに\n繋げて書いた場合（<code>Div[&quot;text&quot;].Class(&quot;card&quot;)</code>）も、同じ理由で BCF3008 です。角括弧は、もう\n<code>View</code> を作り終えています。</p>\n<p>装飾は、このライブラリが宣言しているものである必要もあります。綴り違い（<code>Div.Clas(&quot;card&quot;)</code>）や、\n要素を取って要素を返す自作の拡張メソッドは BCF3026 を報告します。綴り違いには C# のエラーが\nありますが、さきほどと同じ停止によって、それも目に入りません。</p>\n<h2 id=\"次に読むもの\">次に読むもの<a href=\"#%E6%AC%A1%E3%81%AB%E8%AA%AD%E3%82%80%E3%82%82%E3%81%AE\" class=\"headlink\" aria-label=\"Permalink to this section\">#</a></h2>\n<ul>\n<li><a href=\"/docs/ja/getting-started#%E5%B0%8E%E5%85%A5\">はじめに</a>に戻る。</li>\n<li>条件分岐とリストは<a href=\"/docs/ja/control-flow\">制御構文</a>へ。</li>\n</ul>\n"),
        new DocEntry(
            "control-flow",
            "制御構文",
            30,
            "ja",
            false,
            "<p>条件分岐とリストには、専用の構文があります。テンプレートのどの位置にも、ジェネレーターがコン\nパイル時にシーケンス番号を振れるようにするためです。</p>\n<h2 id=\"if\">If<a href=\"#if\" class=\"headlink\" aria-label=\"Permalink to this section\">#</a></h2>\n<p><code>If</code> は、条件と中身のサンクを取ります。else の分岐は省けます。</p>\n<div class=\"csharp\"><pre>\n<span class=\"keyword\">protected</span> <span class=\"keyword\">override</span> View Body =&gt;\n    Div[\n        If(_items.Count == <span class=\"number\">0</span>,\n            () =&gt; P[<span class=\"string\">&quot;Nothing here yet.&quot;</span>],\n            () =&gt; Span[$<span class=\"string\">&quot;{_items.Count} items&quot;</span>])];\n</pre></div>\n<p>互いに排他な分岐には、重ならないシーケンス番号の範囲を割り当てます。だから分岐が切り替わっても、\n周りにある兄弟の位置は動きません。</p>\n<h2 id=\"foreach-とそのキー\">ForEach とそのキー<a href=\"#foreach-%E3%81%A8%E3%81%9D%E3%81%AE%E3%82%AD%E3%83%BC\" class=\"headlink\" aria-label=\"Permalink to this section\">#</a></h2>\n<p><code>ForEach</code> は、位置ではなく要素そのものを見分けるキーを取ります。</p>\n<div class=\"csharp\"><pre>\n<span class=\"keyword\">protected</span> <span class=\"keyword\">override</span> View Body =&gt;\n    Ul[\n        ForEach(_items,\n            key: item =&gt; item.Id,\n            content: item =&gt; Li[item.Name])];\n</pre></div>\n<p>シーケンス番号はテンプレートの位置を、キーはデータの実体を見分けます。インデックスをキーに渡す\nと、差分の計算が働きません。並び替えたときに、Blazor が別の要素の状態を使い回してしまうからです。</p>\n<p>自分の要素にまったく触れていないキーは、見つけて報告します。次の3つは、どれも BCF3002 です。</p>\n<ul>\n<li><code>key: _ =&gt; 0</code></li>\n<li>ラムダの外にあるカウンターから読んだキー</li>\n<li>入れ子のループで、外側の要素しか名指ししていない内側のキー</li>\n</ul>\n<div class=\"csharp\"><pre>\nForEach(_groups, key: g =&gt; g.Id, content: g =&gt;\n    Div[ForEach(g.Items, key: i =&gt; g.Id, content: i =&gt; Span[i.Name])])   <span class=\"comment\">// 内側のキーで BCF3002</span>\n</pre></div>\n<p>これはエラーではなく警告で、コンポーネントの出力は止めません。リストの表示そのものは正しく、\n差分の計算だけが下手になるからです。検査もあえて控えめにしてあります。見ているのは、要素を参照\nしたかという点です。その値に見分ける力があるかは、問いません。要素から作ってはいるものの位置に\n近い、というキーはこの検査を通ります。BCF3002 は、保証ではなく下限として読んでください。</p>\n<p>どちらのラムダも、その場に書いた式のラムダである必要があります。本体が文のラムダ、あるいは\nメソッドグループは静的に順序付けできず、BCF3004 を報告します。呼び出しで包んでください。<code>Row</code>\nではなく <code>item =&gt; Row(item)</code> と書きます。</p>\n<p>中身の根は、単一の要素かコンポーネントである必要があります。だから根が <code>Fragment</code> や <code>Raw</code> の\n場合、中身に <code>RenderFragment</code> を置いた場合は、BCF3003 を報告します。</p>\n<h2 id=\"キーを降りる\">キーを降りる<a href=\"#%E3%82%AD%E3%83%BC%E3%82%92%E9%99%8D%E3%82%8A%E3%82%8B\" class=\"headlink\" aria-label=\"Permalink to this section\">#</a></h2>\n<p>キーの引数に既定値はありません。だから見分ける材料を持たないリストは、そう書きます。</p>\n<div class=\"csharp\"><pre>\nUl[ForEach(_columns, key: <span class=\"keyword\">null</span>, content: c =&gt; Li[c.Header])]\n</pre></div>\n<p>固定のメニュー、決まった列の集合、並び替わらない射影は、この綴りが正解です。代わりに払うのは、\nBCF3002 が警告しているものと同じです。差分の計算はインデックスをキーにしたときと同じになり、\n先頭に1件挿入すると全行が書き直され、行ごとの状態が失われます。<code>SetKey</code> を出さないので、BCF3002\nは問う対象を持たず、BCF3003 も適用されません。中身の根には <code>Fragment</code> も <code>Raw</code> も裸の <code>If</code> も\n置けます。</p>\n<h2 id=\"射影を差し込む\">射影を差し込む<a href=\"#%E5%B0%84%E5%BD%B1%E3%82%92%E5%B7%AE%E3%81%97%E8%BE%BC%E3%82%80\" class=\"headlink\" aria-label=\"Permalink to this section\">#</a></h2>\n<p>子のリストには、ふつうの射影をスプレッドで差し込むこともできます。</p>\n<div class=\"csharp\"><pre>\nUl[[.. _columns.Select(c =&gt; Li[c.Header])]]\n</pre></div>\n<p>これは上のキーを降りた <code>ForEach</code> の糖衣で、同じ <code>foreach</code> になります。子のリスト全体を渡す形と\n違って、周りに書いた兄弟と混ざります。</p>\n<div class=\"csharp\"><pre>\nUl[[Li[<span class=\"string\">&quot;先頭&quot;</span>], .. _columns.Select(c =&gt; Li[c.Header]), Li[<span class=\"string\">&quot;末尾&quot;</span>]]]\n</pre></div>\n<p>畳めるのは <code>&lt;source&gt;.Select(&lt;その場に書いた式のラムダ&gt;)</code> だけです。それ以外のスプレッド、たとえば\n保存した <code>View</code> の配列や、それを返すメソッドは、静的に順序付けできる子ではないので BCF1003 を\n報告します。保存した <code>View</code> を子として1つ書いたときと同じ答えです。</p>\n<h2 id=\"fragment\">Fragment<a href=\"#fragment\" class=\"headlink\" aria-label=\"Permalink to this section\">#</a></h2>\n<p><code>Fragment</code> は、ラッパーの要素を出さずに子をまとめます。<code>&lt;&gt;...&lt;/&gt;</code> にあたるものです。</p>\n<div class=\"csharp\"><pre>\nFragment(H2[<span class=\"string\">&quot;Title&quot;</span>], P[<span class=\"string\">&quot;Body&quot;</span>])\n</pre></div>\n<p>要素を開かないので、装飾は付けられず（BCF3008）、<code>ForEach</code> の中身の根にもできません（BCF3003）。</p>\n<h2 id=\"raw\">Raw<a href=\"#raw\" class=\"headlink\" aria-label=\"Permalink to this section\">#</a></h2>\n<p><code>Raw</code> は、信頼できる HTML の文字列をそのまま流し込みます。<code>MarkupString</code> にあたるものです。この\nページ自体もそうやって表示しています。Markdown をビルド時に HTML へ変換し、<code>Raw</code> に渡しています。</p>\n<div class=\"csharp\"><pre>\nArticle.Class(<span class=\"string\">&quot;prose&quot;</span>)[Raw(entry.Html)]\n</pre></div>\n<p><code>Raw</code> はエスケープせずに DOM へ書き込むので、受け付けてよいのは信頼できる中身だけです。利用者の\n入力や、外部から返ってきた応答を通さないでください。</p>\n<h2 id=\"次に読むもの\">次に読むもの<a href=\"#%E6%AC%A1%E3%81%AB%E8%AA%AD%E3%82%80%E3%82%82%E3%81%AE\" class=\"headlink\" aria-label=\"Permalink to this section\">#</a></h2>\n<ul>\n<li>要素の語彙は<a href=\"/docs/ja/elements-and-decorations#%E8%A3%85%E9%A3%BE\">要素と装飾</a>へ。</li>\n<li>コンポーネントから別のコンポーネントを呼ぶ方法は<a href=\"/docs/ja/components-and-reuse\">コンポーネントと再利用</a>へ。</li>\n</ul>\n"),
        new DocEntry(
            "components-and-reuse",
            "コンポーネントと再利用",
            40,
            "ja",
            false,
            "<p>再利用の単位はコンポーネントです。BlazorCodeFirst のコンポーネントから別のコンポーネントを呼ぶ\nには <code>Component&lt;T&gt;()</code> を使います。既存の Razor コンポーネントも、サードパーティのコンポーネント\nも、まったく同じ呼び方です。逆に <code>.razor</code> のファイルからは、BlazorCodeFirst のコンポーネントを\nふつうのタグとして名指しできます。<code>[ViewPart]</code> は別の仕事のための別の道具で、このページの終わり\nで扱います。</p>\n<h2 id=\"blazorcodefirst-のコンポーネントを呼ぶ\">BlazorCodeFirst のコンポーネントを呼ぶ<a href=\"#blazorcodefirst-%E3%81%AE%E3%82%B3%E3%83%B3%E3%83%9D%E3%83%BC%E3%83%8D%E3%83%B3%E3%83%88%E3%82%92%E5%91%BC%E3%81%B6\" class=\"headlink\" aria-label=\"Permalink to this section\">#</a></h2>\n<p><code>Component&lt;T&gt;()</code> は、コンポーネントをツリーに置きます。パラメーターは <code>.Param</code> で束縛し、相手の\nプロパティはラムダで名指しします。</p>\n<div class=\"csharp\"><pre>\n<span class=\"keyword\">protected</span> <span class=\"keyword\">override</span> View Body =&gt;\n    Div.Class(<span class=\"string\">&quot;dashboard&quot;</span>)[\n        Component&lt;StatusBadge&gt;()\n            .Param(b =&gt; b.Status, _status)\n            .Param(b =&gt; b.Compact, <span class=\"keyword\">true</span>)];\n</pre></div>\n<p>ジェネレーターは <code>.Param</code> のひとつひとつを静的なパラメーターの設定に変え、\n<code>AddComponentParameter</code> の呼び出しとして書き出します。リフレクションは使わず、実行時に式ツリーを\nコンパイルすることもありません。これが、この経路をトリミングと AOT に対して安全に保っています。</p>\n<p>書ける形が診断で囲ってあるのも、それが理由です。セレクターのラムダでパラメーターを名指しする\n経路は、どれもこの診断に従います。<code>.Param</code> だけではありません。<code>.Template</code> も、コンポーネントに\n対する <code>.Bind</code> も同じです。</p>\n<ul>\n<li>セレクターは、プロパティを選ぶだけの式である必要があります。キャスト、メソッド呼び出し、捕捉\nした変数のメンバーは BCF3005 を報告します。どれも、ジェネレーターが設定を書き出せるプロパティ\nを名指ししていないからです。</li>\n<li>相手は、設定できる <code>[Parameter]</code> のプロパティである必要があります。でなければ BCF3006 を報告\nします。そうでなければ Blazor が実行時に例外を投げるので、拒否をコンパイル時へ前倒ししています。</li>\n<li>同じプロパティを2度束縛すると BCF3007 を報告します。2つの束縛がどの経路から来たかは問いません。\nBlazor は最後の値しか適用しないので、先の束縛は黙って死にます。</li>\n</ul>\n<h2 id=\"子の内容を渡す\">子の内容を渡す<a href=\"#%E5%AD%90%E3%81%AE%E5%86%85%E5%AE%B9%E3%82%92%E6%B8%A1%E3%81%99\" class=\"headlink\" aria-label=\"Permalink to this section\">#</a></h2>\n<p>入れ子に書いた子は <code>ChildContent</code> に束縛されます。入れ子の内容は <code>ChildContent</code> にしかならない、\nという Razor の規則をそのまま写しています。</p>\n<div class=\"csharp\"><pre>\n<span class=\"keyword\">protected</span> <span class=\"keyword\">override</span> View Body =&gt;\n    Component&lt;Card&gt;()[\n        H2[<span class=\"string\">&quot;Heading&quot;</span>],\n        P[<span class=\"string\">&quot;Body text&quot;</span>]];\n</pre></div>\n<p>これには <code>Card</code> の側に、フラグメント型で設定できる <code>[Parameter] ChildContent</code> が必要です。無ければ\nBCF3013 を報告します。<code>RenderFragment&lt;TContext&gt;</code> も対象です。角括弧はコンテキストを捨てて束縛し\nます。角括弧の中には、コンテキストを読むための名前が無いからです。<code>ChildContent</code> 以外の名前を持つ\nジェネリックなフラグメントは <code>.Template</code> で名指しします。下の\n<a href=\"#%E3%82%B8%E3%82%A7%E3%83%8D%E3%83%AA%E3%83%83%E3%82%AF%E3%81%AA%E3%83%95%E3%83%A9%E3%82%B0%E3%83%A1%E3%83%B3%E3%83%88%E3%81%AE%E3%83%91%E3%83%A9%E3%83%A1%E3%83%BC%E3%82%BF%E3%83%BC\">ジェネリックなフラグメントのパラメーター</a>を見て\nください。</p>\n<p>ジェネリックでないほかの <code>RenderFragment</code> パラメーターは、<code>Footer</code> や <code>Header</code> などです。これらは\n<code>.Param(c =&gt; c.Footer, content)</code> と書き、パラメーターを明示して束縛します。</p>\n<div class=\"csharp\"><pre>\n<span class=\"keyword\">protected</span> <span class=\"keyword\">override</span> View Body =&gt;\n    Component&lt;Card&gt;()\n        .Param(c =&gt; c.Title, <span class=\"string\">&quot;Card title&quot;</span>)\n        .Param(c =&gt; c.Footer, Span[<span class=\"string\">&quot;Footer note&quot;</span>])[\n            H2[<span class=\"string\">&quot;Heading&quot;</span>],\n            P[<span class=\"string\">&quot;Body text&quot;</span>]];\n</pre></div>\n<p><code>ChildContent</code> を <code>.Param</code> で名指しするのも正しい書き方です。冗長ですが、Razor の属性の形\n（<code>&lt;Card&gt;&lt;ChildContent&gt;...&lt;/ChildContent&gt;&lt;/Card&gt;</code>）に対応します。同じパラメーターを両方の経路\nから束縛すると BCF3007 です。</p>\n<p>本物の <code>RenderFragment</code> の値は、BlazorCodeFirst の <code>View</code> の式とは違います。ジェネリックな\n<code>.Param&lt;TValue&gt;</code> のオーバーロードで束縛し、そのまま書き出されます。</p>\n<p>どちらのオーバーロードが走るかは、相手のパラメーターの型で決まります。<code>RenderFragment?</code> の\nパラメーターなら内容のオーバーロード、それ以外ならジェネリックなほうです。だから\n<code>RenderFragment</code> でないパラメーターに向けた内容は、ジェネリックなオーバーロードに落ちます。\nそこでは値がそのまま書き出されますが、設計時の式の実行時の値は空の目印にすぎません。これが\nBCF3014 です。</p>\n<div class=\"csharp\"><pre>\n[Parameter] <span class=\"keyword\">public</span> <span class=\"keyword\">object</span>? Payload { <span class=\"keyword\">get</span>; <span class=\"keyword\">set</span>; }\n\nComponent&lt;Card&gt;().Param(c =&gt; c.Payload, Div[<span class=\"string\">&quot;x&quot;</span>])   <span class=\"comment\">// BCF3014</span>\n</pre></div>\n<p>この診断が無ければ、失敗は見えないか、遅れて出てきます。<code>object</code> 型のパラメーターは目印を例外\nも出さずに受け取り、間違った出力を描きます。型の付いたパラメーターは、Blazor がパラメーターを\n適用している最中に不正なキャストで例外を投げます。<code>View</code>、<code>ElementView</code>、<code>ComponentView&lt;T&gt;</code>、\n<code>SlotView</code> は、どれも同じように報告します。内容を渡したいなら、受け取る側のコンポーネントに\n<code>RenderFragment</code> のパラメーターを持たせてください。</p>\n<p>パラメーターの値の中で型名が解決できない場合は、\n<a href=\"/docs/ja/getting-started#%E7%94%9F%E6%88%90%E3%82%B3%E3%83%BC%E3%83%89%E3%81%AB%E5%86%99%E3%81%95%E3%82%8C%E3%82%8B%E5%80%A4\">生成コードに写される値</a>を見てください。</p>\n<h2 id=\"ジェネリックなフラグメントのパラメーター\">ジェネリックなフラグメントのパラメーター<a href=\"#%E3%82%B8%E3%82%A7%E3%83%8D%E3%83%AA%E3%83%83%E3%82%AF%E3%81%AA%E3%83%95%E3%83%A9%E3%82%B0%E3%83%A1%E3%83%B3%E3%83%88%E3%81%AE%E3%83%91%E3%83%A9%E3%83%A1%E3%83%BC%E3%82%BF%E3%83%BC\" class=\"headlink\" aria-label=\"Permalink to this section\">#</a></h2>\n<p><code>RenderFragment&lt;TContext&gt;</code> のパラメーターが取るのは <em>テンプレート</em> です。コンポーネントは、描き\nたいコンテキストの値ごとに、それを1回ずつ呼び出します。最初に出会うのはたいてい\n<code>EditForm.ChildContent</code> でしょう。これは <code>RenderFragment&lt;EditContext&gt;</code> です。</p>\n<p>こうしたパラメーターを名指しするのが <code>.Template</code> です。書き方は2通りあり、どちらを使うかは、\n内容がコンテキストを読むかどうかだけで決まります。グリッドの <code>RowTemplate</code> のように\n<code>ChildContent</code> 以外の名前を持つものには、常に <code>.Template</code> が要ります。角括弧はそこへ届きません。</p>\n<p>コンテキストを読まない <code>ChildContent</code> は角括弧で書きます。上に出した形であり、\n<code>.Template(form =&gt; form.ChildContent, content)</code> と同じものを発行します。</p>\n<div class=\"csharp\"><pre>\n<span class=\"keyword\">protected</span> <span class=\"keyword\">override</span> View Body =&gt;\n    Component&lt;EditForm&gt;()\n        .Param(form =&gt; form.Model, _model)[\n            Component&lt;NameFields&gt;().Param(fields =&gt; fields.Value, _model)];\n</pre></div>\n<p>使うなら、コンテキストから内容へのラムダで名指しします。</p>\n<div class=\"csharp\"><pre>\n<span class=\"keyword\">protected</span> <span class=\"keyword\">override</span> View Body =&gt;\n    Component&lt;EditForm&gt;()\n        .Param(form =&gt; form.Model, _model)\n        .Template(form =&gt; form.ChildContent, editContext =&gt;\n            Fragment(\n                Span[editContext.IsModified() ? <span class=\"string\">&quot;Unsaved changes&quot;</span> : <span class=\"string\">&quot;No changes&quot;</span>],\n                Component&lt;NameFields&gt;().Param(fields =&gt; fields.Value, _model)));\n</pre></div>\n<p>2つ目の例には、注意が1つ要ります。でないとバッジは一度も変わりません。<code>IsModified()</code> は\nテンプレートが走るときに読まれます。しかし <code>EditForm</code> と <code>CascadingValue</code> の連なりには、\n<code>OnFieldChanged</code> で再レンダリングするものがありません。フィールドに入力すれば <code>EditContext</code> に\n通知は飛びます。ただ、その通知を誰も購読していないので、テンプレートを持つコンポーネントは\n再レンダリングされず、バッジは最初に描かれた文字のままです。これは Blazor のレンダリングの\n伝わり方の話であって、テンプレートが受け取るコンテキストの制限ではありません。テンプレートには、\n走るたびに生きた <code>EditContext</code> が渡されます。</p>\n<p>だからテンプレートが変化するコンテキストの状態を読むなら、再レンダリングはフォームを持つ\nコンポーネント自身の仕事になります。<code>Model</code> に作らせるのではなく、<code>EditContext</code> を自分で組み立て\nます。そして <code>OnFieldChanged</code> を購読し、<code>StateHasChanged</code> を呼び、<code>Dispose</code> で購読を解きます。</p>\n<div class=\"csharp\"><pre>\n<span class=\"keyword\">public</span> ContextReadingForm()\n{\n    _editContext = <span class=\"keyword\">new</span> EditContext(_model);\n    _editContext.OnFieldChanged += OnFieldChanged;\n}\n\n<span class=\"keyword\">private</span> <span class=\"keyword\">void</span> OnFieldChanged(<span class=\"keyword\">object</span>? sender, FieldChangedEventArgs e) =&gt; StateHasChanged();\n\n<span class=\"keyword\">public</span> <span class=\"keyword\">void</span> Dispose() =&gt; _editContext.OnFieldChanged -= OnFieldChanged;\n\n<span class=\"comment\">// ...そのうえで .Param(form =&gt; form.Model, …) ではなく .Param(form =&gt; form.EditContext, _editContext) を渡す</span>\n</pre></div>\n<p>コンテキストを使わないテンプレートや、フォームが開いているあいだ変わらない状態しか読まない\nテンプレートに、この用意は要りません。</p>\n<p><code>RenderFragment&lt;TContext&gt;</code> のラムダはジェネレーターが書くので、その中身はふつうの\nBlazorCodeFirst で、シーケンス番号も周りから続きます。コンテキストの引数の名前は自由に決められ\nます。生成コードは自前の名前を使い、参照していた箇所を書き換えるので、たまたま同じ名前の\nフィールドがあっても壊れません。</p>\n<p>2つ目の引数は、その場に書いたラムダである必要があります。メソッドグループや、変数やフィールド\nに持っているデリゲートは <strong>BCF3022</strong> を報告します。生成コードに写されるのはラムダの本体の構文で、\n宣言が別の場所にあるデリゲートには写す本体がないからです。</p>\n<p>すでに <code>RenderFragment&lt;TContext&gt;</code> の <em>値</em> を持っているなら、スカラーの <code>.Param</code> で渡してくだ\nさい。どちらの経路も同じパラメーターに届きますが、デリゲートの同一性が違い、その違いは見える\nところに出ます。</p>\n<div class=\"csharp\"><pre>\n<span class=\"comment\">// コンストラクターで一度だけ組み立てる。パラメーターの参照はレンダリングをまたいで変わらない。</span>\n<span class=\"keyword\">private</span> <span class=\"keyword\">readonly</span> RenderFragment&lt;EditContext&gt; _fields;\n</pre></div>\n<p>状態を読む <code>.Template</code> の内容は、その状態を捕捉します。だからラムダは、レンダリングのたびに\n新しいデリゲートへ変わります。受け取る側のコンポーネントはパラメーターが変わったと見て、テンプ\nレートを描き直します。<code>.Param</code> で渡したキャッシュ済みのデリゲートは変わらないので、描き直しま\nせん。キャッシュする形に手を伸ばすのは、その安定が欲しいときだけにしてください。ほかの場面では\n<code>.Template</code> のほうが短く、安全です。キャッシュのように忘れることがないからです。</p>\n<h2 id=\"既存の-razor-コンポーネントやサードパーティのコンポーネントを呼ぶ\">既存の Razor コンポーネントやサードパーティのコンポーネントを呼ぶ<a href=\"#%E6%97%A2%E5%AD%98%E3%81%AE-razor-%E3%82%B3%E3%83%B3%E3%83%9D%E3%83%BC%E3%83%8D%E3%83%B3%E3%83%88%E3%82%84%E3%82%B5%E3%83%BC%E3%83%89%E3%83%91%E3%83%BC%E3%83%86%E3%82%A3%E3%81%AE%E3%82%B3%E3%83%B3%E3%83%9D%E3%83%BC%E3%83%8D%E3%83%B3%E3%83%88%E3%82%92%E5%91%BC%E3%81%B6\" class=\"headlink\" aria-label=\"Permalink to this section\">#</a></h2>\n<p>書き方は変わりません。<code>.razor</code> で書いたコンポーネントも、MudBlazor や QuickGrid のような\nパッケージのコンポーネントも、同じ <code>Component&lt;T&gt;()</code> で置きます。</p>\n<div class=\"csharp\"><pre>\n<span class=\"keyword\">protected</span> <span class=\"keyword\">override</span> View Body =&gt;\n    Div[\n        Span[<span class=\"string\">&quot;Data Grid&quot;</span>],\n        Component&lt;MudDataGrid&lt;Order&gt;&gt;()\n            .Param(g =&gt; g.Items, _orders)\n            .Param(g =&gt; g.Dense, <span class=\"keyword\">true</span>)];\n</pre></div>\n<p>制限が1つあり、たいていの人が最初にぶつかる壁がこれです。型引数は <code>OpenComponent&lt;T&gt;</code> として\nそのまま生成コードに落ちるので、ジェネレーターが走る時点で解決できなければなりません。Razor の\nコンパイラー自身がソースジェネレーターで、ソースジェネレーターは互いの出力を見られません。だから\n<em>同じプロジェクト</em> で宣言した <code>.razor</code> のコンポーネントは、BlazorCodeFirst のジェネレーターが\n走る時点ではまだ存在しません。名指しすると <strong>BCF3012</strong> を報告します。</p>\n<p>回避の道は2つです。</p>\n<ul>\n<li><code>.razor</code> のコンポーネントを、参照しているプロジェクトかパッケージへ移す。型はメタデータから\n来るようになり、ふつうに解決します。</li>\n<li>コンポーネントを C# で手書きする。手書きのコンポーネントはふつうのソースなので、同じ\nプロジェクトの中でも必ず解決します。</li>\n</ul>\n<p>綴り違いや <code>using</code> の書き忘れも同じ BCF3012 になり、同じ位置に CS0246 が並びます。</p>\n<h2 id=\"razor-から-blazorcodefirst-のコンポーネントを使う\">Razor から BlazorCodeFirst のコンポーネントを使う<a href=\"#razor-%E3%81%8B%E3%82%89-blazorcodefirst-%E3%81%AE%E3%82%B3%E3%83%B3%E3%83%9D%E3%83%BC%E3%83%8D%E3%83%B3%E3%83%88%E3%82%92%E4%BD%BF%E3%81%86\" class=\"headlink\" aria-label=\"Permalink to this section\">#</a></h2>\n<p>逆向きには、この制限がありません。BlazorCodeFirst のコンポーネントは、ただの Blazor の\nコンポーネントです。<code>BodyComponentBase</code> は <code>ComponentBase</code> から派生しています。だから <code>.razor</code>\nのファイルは、これをふつうのタグとして名指しできます。</p>\n<pre><code class=\"language-razor\">@* ExistingPage.razor *@\n&lt;div class=&quot;legacy-layout&quot;&gt;\n    &lt;StatusBadge Status=&quot;@currentStatus&quot; /&gt;\n&lt;/div&gt;\n</code></pre>\n<div class=\"csharp\"><pre>\n<span class=\"keyword\">public</span> <span class=\"keyword\">partial</span> <span class=\"keyword\">class</span> StatusBadge : BodyComponentBase\n{\n    [Parameter] <span class=\"keyword\">public</span> Status Status { <span class=\"keyword\">get</span>; <span class=\"keyword\">set</span>; } = <span class=\"keyword\">default</span>!;\n\n    <span class=\"keyword\">protected</span> <span class=\"keyword\">override</span> View Body =&gt;\n        Span.Class(Status.IsHealthy ? <span class=\"string\">&quot;badge badge-ok&quot;</span> : <span class=\"string\">&quot;badge badge-alert&quot;</span>)[Status.Label];\n}\n</pre></div>\n<p>これは同じプロジェクトでも動きます。BCF3012 との非対称がどこから来るのかは、知っておく価値が\nあります。ここで Razor が解決しなければならないのは <em>クラス名</em> で、そのクラスの宣言は手で書いた\nソースです。ジェネレーターがその中に書き入れるのは <code>RenderView</code> だけで、Razor はそれを見る必要が\nありません。BCF3012 の向きでは、型そのものが生成された出力です。これは別の問題です。</p>\n<p>このサイトがそうしています。<code>App.razor</code> が <code>NotFoundPage</code> を名指ししています。これは同じ\nプロジェクトのふつうの <code>.cs</code> ファイルで宣言した、BlazorCodeFirst のコンポーネントです。</p>\n<h2 id=\"コンポーネントを作らずに分ける-viewpart\">コンポーネントを作らずに分ける: <code>[ViewPart]</code><a href=\"#%E3%82%B3%E3%83%B3%E3%83%9D%E3%83%BC%E3%83%8D%E3%83%B3%E3%83%88%E3%82%92%E4%BD%9C%E3%82%89%E3%81%9A%E3%81%AB%E5%88%86%E3%81%91%E3%82%8B-viewpart\" class=\"headlink\" aria-label=\"Permalink to this section\">#</a></h2>\n<p><code>Body</code> の式のどの部分にも、コンポーネントが要るわけではありません。<code>[ViewPart]</code> のメソッドは\nUI の一片で、ジェネレーターはこれを、コンポーネントの境界越しに描くのではなく、<em>呼び出した側の\n中へ</em> 展開します。</p>\n<div class=\"csharp\"><pre>\n<span class=\"keyword\">protected</span> <span class=\"keyword\">override</span> View Body =&gt;\n    Div[\n        AppHeader(<span class=\"string\">&quot;My Application&quot;</span>),\n        BodyContent()];\n\n[ViewPart]\n<span class=\"keyword\">private</span> <span class=\"keyword\">static</span> View AppHeader(<span class=\"keyword\">string</span> title) =&gt;\n    Div.Class(<span class=\"string\">&quot;app-header&quot;</span>)[\n        Span[title]];\n</pre></div>\n<p>呼び出した側の生成された <code>RenderView</code> は、ヘッダーのフレームを直接持ちます。コンポーネントの\n実体も、パラメーターも、ライフサイクルも、差分の境界もありません。その場にマークアップを書いた\nのと同じです。</p>\n<h3 id=\"コンポーネントの呼び出しに名前を付ける\">コンポーネントの呼び出しに名前を付ける<a href=\"#%E3%82%B3%E3%83%B3%E3%83%9D%E3%83%BC%E3%83%8D%E3%83%B3%E3%83%88%E3%81%AE%E5%91%BC%E3%81%B3%E5%87%BA%E3%81%97%E3%81%AB%E5%90%8D%E5%89%8D%E3%82%92%E4%BB%98%E3%81%91%E3%82%8B\" class=\"headlink\" aria-label=\"Permalink to this section\">#</a></h3>\n<p>パーツの本体はふつうの設計時の構文なので、要素と同じようにコンポーネントの呼び出しも書けます。\nそうするとコンポーネントの呼び出しに固有の名前が付き、呼び出し側からは <code>Component&lt;T&gt;()</code> と\n<code>.Param</code> が消えます。</p>\n<div class=\"csharp\"><pre>\n<span class=\"keyword\">public</span> <span class=\"keyword\">static</span> <span class=\"keyword\">class</span> Widgets\n{\n    [ViewPart]\n    <span class=\"keyword\">public</span> <span class=\"keyword\">static</span> View Badge(<span class=\"keyword\">string</span> label, <span class=\"keyword\">bool</span> compact = <span class=\"keyword\">false</span>) =&gt;\n        Component&lt;StatusBadge&gt;()\n            .Param(b =&gt; b.Label, label)\n            .Param(b =&gt; b.Compact, compact);\n}\n\n<span class=\"keyword\">protected</span> <span class=\"keyword\">override</span> View Body =&gt;\n    Div[\n        Widgets.Badge(<span class=\"string\">&quot;hello&quot;</span>),\n        Widgets.Badge(<span class=\"string\">&quot;x&quot;</span>, compact: <span class=\"keyword\">true</span>)];\n</pre></div>\n<p>呼び出し側はふつうの C# の呼び出しなので、名前付き引数と省略可能な引数が使えます。展開はあく\nまで展開です。呼び出した側の生成された <code>RenderView</code> は、呼び出しのたびに <code>StatusBadge</code> を直接\n開きます。だから描かれるツリーは、<code>Component&lt;StatusBadge&gt;()</code> を2回書いたときと同じものです。</p>\n<h3 id=\"内容を包む\">内容を包む<a href=\"#%E5%86%85%E5%AE%B9%E3%82%92%E5%8C%85%E3%82%80\" class=\"headlink\" aria-label=\"Permalink to this section\">#</a></h3>\n<p>呼び出し側が渡す内容を包むパーツは、<code>View</code> ではなく <code>SlotView</code> を返し、その内容が入る場所に\n<code>Slot</code> を書きます。呼び出し側は、要素の子を渡すのとまったく同じように、角括弧で渡します。</p>\n<div class=\"csharp\"><pre>\n<span class=\"keyword\">protected</span> <span class=\"keyword\">override</span> View Body =&gt;\n    Div[\n        Card(<span class=\"string\">&quot;Profile&quot;</span>)[P[<span class=\"string\">&quot;Body text&quot;</span>]],\n        Section.Class(<span class=\"string\">&quot;body&quot;</span>)[P[<span class=\"string\">&quot;…&quot;</span>]]];\n\n[ViewPart]\n<span class=\"keyword\">private</span> <span class=\"keyword\">static</span> SlotView Card(<span class=\"keyword\">string</span> title) =&gt;\n    Div.Class(<span class=\"string\">&quot;card&quot;</span>)[\n        H2[title],\n        Slot];\n</pre></div>\n<p>この書き方の狙いはそこにあります。切り出したパーツが、はじめからある要素と同じ見え方で読める\nこと。<code>Card(&quot;Profile&quot;)[…]</code> は <code>Section.Class(&quot;body&quot;)[…]</code> の隣に並んでも、どちらが自分のものかを\n主張しません。</p>\n<p>角括弧は省けません。そしてそれを強制しているのは C# だけです。<code>SlotView</code> から <code>View</code> への変換は\nないので、角括弧を忘れた <code>Div[Card(&quot;Profile&quot;)]</code> は、黙って空のカードを描くのではなくコンパイル\nエラーになります。同じ性質が、2つの書き方を締め出します。装飾（<code>Card(&quot;t&quot;).Class(&quot;x&quot;)</code>。該当する\n拡張メソッドがない）と、引数で渡す書き方（<code>Card(&quot;t&quot;, P[&quot;x&quot;])</code>。束縛する引数がない）です。</p>\n<p>2つ目のスロットは、ふつうの <code>View</code> の引数です。</p>\n<div class=\"csharp\"><pre>\n<span class=\"keyword\">protected</span> <span class=\"keyword\">override</span> View Body =&gt;\n    Panel(H2[<span class=\"string\">&quot;Title&quot;</span>])[\n        P[<span class=\"string\">&quot;Body text&quot;</span>]];\n\n[ViewPart]\n<span class=\"keyword\">private</span> <span class=\"keyword\">static</span> SlotView Panel(View header) =&gt;\n    Div.Class(<span class=\"string\">&quot;panel&quot;</span>)[\n        Div.Class(<span class=\"string\">&quot;panel-head&quot;</span>)[header],\n        Div.Class(<span class=\"string\">&quot;panel-body&quot;</span>)[Slot]];\n</pre></div>\n<p>名前の付いた経路が先、主な内容は角括弧の中。<code>Div.Class(&quot;card&quot;)[…]</code> や\n<code>Component&lt;T&gt;().Template(…)[…]</code> が、この API で既に取っている形です。</p>\n<p>知っておきたい規則が2つあります。<code>SlotView</code> のパーツは、<code>Slot</code> を <strong>ちょうど1回</strong> 名指しする\n必要があります。2回名指しすれば、1つの角括弧から呼び出し側の内容を2度出すことになります。一度も\n名指ししなければ、呼び出し側が渡すよう義務づけられた内容を捨てることになります。どちらも\n<strong>BCF3025</strong> です。呼び出し側の内容が来ない場所に書いた <code>Slot</code> も同じで、コンポーネント自身の\n<code>Body</code> や、<code>View</code> を返すパーツがこれにあたります。</p>\n<p>対して <code>View</code> の引数は、何度参照してもかまいません。ふつうの引数だからです。捕捉も共有もしま\nせん。参照するたびに呼び出し側の式を展開し直すので、副作用のある引数は参照の数だけ走ります。\nBlazor の <code>RenderFragment</code> を2回呼んだときと同じ振る舞いです。</p>\n<p>ただし、どちらも内容であって、内容に値はありません。式ではなくフレームになるからです。だから\nスロットは、子として <em>置く</em> ことしかできません。値が要る場所で読むと <strong>BCF1002</strong> です。<code>ForEach</code>\nのキーや、属性の値の中がそれにあたります。</p>\n<p>取捨はこれで全部です。観点ごとに並べます。</p>\n<table>\n<thead>\n<tr>\n<th></th>\n<th><code>[ViewPart]</code></th>\n<th>コンポーネント</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>状態とライフサイクル</td>\n<td>持たない。メソッドだから</td>\n<td>Blazor のコンポーネントとして自前で持つ</td>\n</tr>\n<tr>\n<td>再レンダリング</td>\n<td>自分の境界がないので、呼び出し側と一緒に</td>\n<td>自分の差分境界で、自分だけで</td>\n</tr>\n<tr>\n<td>呼び出し側のフレームの中身</td>\n<td>パーツのフレームが、その場に展開される</td>\n<td>コンポーネントを開くフレーム 1 つ</td>\n</tr>\n<tr>\n<td>引数</td>\n<td>値渡しの引数。名前付きも省略可能も使える</td>\n<td><code>[Parameter]</code> のプロパティを <code>.Param</code> で渡す</td>\n</tr>\n<tr>\n<td>別のアセンブリから</td>\n<td>使えない (BCF1002)</td>\n<td>使える</td>\n</tr>\n</tbody>\n</table>\n<p><code>[ViewPart]</code> は、ジェネレーターが展開できる宣言の約束を満たす必要があります。でなければ\n<strong>BCF1002</strong> です。メソッドは次の形でなければなりません。</p>\n<ul>\n<li>静的である</li>\n<li>ジェネリックでなく、ジェネリックでない型で宣言されている</li>\n<li>1 つの <code>return</code> へ到達する。その手前にはローカル宣言と式文を置ける。<code>Body</code> のゲッターと同じ形</li>\n<li><code>View</code> を返す。内容を取るなら <code>SlotView</code></li>\n</ul>\n<p>引数は、生成コードから名前を書ける型の、ふつうの値渡しの引数である必要があります。<code>params</code>、\n参照渡しの引数、<code>ElementView</code> の引数は、どれも拒否します。子を持たない要素を内容として渡すには、\n<code>Div[…]</code> か <code>Fragment(Div)</code> と書きます。どちらも <code>View</code> です。<code>View</code> の引数は内容のスロットな\nので、返り値の型は <code>SlotView</code> でなければなりません。<code>View</code> を返すパーツに書けば BCF1002 で、\n省略可能にはできません。</p>\n<p>拡張メンバーであってもいけません。<code>this</code> 引数も、<code>extension</code> ブロックのメンバーも同じです。\n呼び出しは、ふつうの呼び出しとして書きます（<code>AppHeader(&quot;My Application&quot;)</code>）。この API が、要素\nへの装飾でない呼び出しを、すべてそう書いているからです。流れるような書き方をすると、装飾でない\nものが、この API が装飾のために空けている位置へ入ります。しかもそのレシーバーは、どう\nやってもほかの型の値にしかなりません。それでは <code>[ViewPart]</code> は、<code>Body</code> を分ける道具ではなく、\n<em>その型</em> の API を増やす道具になってしまいます。</p>\n<p>BCF1002 は <em>呼び出し側</em> でも出ます。その条件の1つは、はっきり書いておく価値があります。</p>\n<p><strong><code>[ViewPart]</code> はアセンブリの境界を越えられません。</strong> 呼び出しを展開するには宣言のソースの構文が\n要り、ジェネレーターは自分が走っているコンパイルから宣言を集めます。IL は本体の構文を持たないの\nで、参照しているプロジェクトやパッケージにある <code>[ViewPart]</code> は、呼び出した場所で必ず BCF1002 に\nなります。展開が再帰して循環する場合と、展開する場所から見えない <code>private</code> や <code>protected</code> の\nメンバーに本体が触れている場合も、同じ診断です。</p>\n<p>そのパーツを別のプロジェクトで使いたいなら、コンポーネントにして <code>Component&lt;T&gt;()</code> から使って\nください。</p>\n<h2 id=\"次に読むもの\">次に読むもの<a href=\"#%E6%AC%A1%E3%81%AB%E8%AA%AD%E3%82%80%E3%82%82%E3%81%AE\" class=\"headlink\" aria-label=\"Permalink to this section\">#</a></h2>\n<ul>\n<li><code>If</code> とキー付き <code>ForEach</code> は<a href=\"/docs/ja/control-flow\">制御構文</a>へ。</li>\n<li>ルーティングされたページを共通の外枠で包む方法は<a href=\"/docs/ja/layouts\">レイアウト</a>へ。</li>\n<li>パラメーターを渡すもう1つの方法である <code>.Bind</code> は、\n<a href=\"/docs/ja/two-way-binding#%E3%82%B3%E3%83%B3%E3%83%9D%E3%83%BC%E3%83%8D%E3%83%B3%E3%83%88%E3%81%AE%E3%83%91%E3%83%A9%E3%83%A1%E3%83%BC%E3%82%BF%E3%83%BC%E3%82%92%E6%9D%9F%E7%B8%9B%E3%81%99%E3%82%8B\">双方向の束縛</a>へ。</li>\n</ul>\n"),
        new DocEntry(
            "layouts",
            "レイアウト",
            50,
            "ja",
            false,
            "<p>レイアウトは、ルーティングされたページを共通の外枠で包みます。ヘッダー、ナビゲーション、\nフッターといったものです。BlazorCodeFirst のレイアウトは、コンポーネントと同じように書きます。\n<code>ChromeLayoutBase</code> から派生し、設計時の UI の式を宣言し、レンダリングはソースジェネレーターに\n作らせます。</p>\n<h2 id=\"chrome-と-body\">Chrome と Body<a href=\"#chrome-%E3%81%A8-body\" class=\"headlink\" aria-label=\"Permalink to this section\">#</a></h2>\n<p><code>ChromeLayoutBase</code> は Blazor の <code>LayoutComponentBase</code> から派生しています。だから、ルーティング\nされたページを持つ <code>Body</code> パラメーターを既に備えています。レイアウト自身が描く外枠は、別の\nオーバーライドするプロパティ <code>Chrome</code> に書きます。</p>\n<div class=\"csharp\"><pre>\n<span class=\"keyword\">using</span> BlazorCodeFirst;\n<span class=\"keyword\">using</span> <span class=\"keyword\">static</span> BlazorCodeFirst.Html;\n\n<span class=\"keyword\">public</span> <span class=\"keyword\">partial</span> <span class=\"keyword\">class</span> MainLayout : ChromeLayoutBase\n{\n    <span class=\"keyword\">protected</span> <span class=\"keyword\">override</span> View Chrome =&gt;\n        Div.Class(<span class=\"string\">&quot;shell&quot;</span>)[\n            Header[H1[<span class=\"string\">&quot;My App&quot;</span>]],\n            Main.Class(<span class=\"string\">&quot;content&quot;</span>)[Body],\n            Footer[<span class=\"string\">&quot;&#169; 2026&quot;</span>]];\n}\n</pre></div>\n<p>ここの <code>Main[Body]</code> は、Razor の <code>&lt;main&gt;@Body&lt;/main&gt;</code> そのものです。ルーティングされたページを、\n要素の内容として落とし込んでいます。</p>\n<h2 id=\"body-ではなく-chrome-である理由\">Body ではなく Chrome である理由<a href=\"#body-%E3%81%A7%E3%81%AF%E3%81%AA%E3%81%8F-chrome-%E3%81%A7%E3%81%82%E3%82%8B%E7%90%86%E7%94%B1\" class=\"headlink\" aria-label=\"Permalink to this section\">#</a></h2>\n<p>Blazor は、レイアウトのルーティングされた内容を、<code>Body</code> という名前ちょうどのパラメーターで公開\nすることを求めます。そして C# は、1つの型に同じ名前のメンバーを2つ宣言できません。だから <code>Body</code>\nは Razor での意味、つまり包まれる側のページを持ったままにして、レイアウト自身の設計時の式には\n<code>Chrome</code> という別の名前を付けました。</p>\n<h2 id=\"レイアウトを入れ子にする\">レイアウトを入れ子にする<a href=\"#%E3%83%AC%E3%82%A4%E3%82%A2%E3%82%A6%E3%83%88%E3%82%92%E5%85%A5%E3%82%8C%E5%AD%90%E3%81%AB%E3%81%99%E3%82%8B\" class=\"headlink\" aria-label=\"Permalink to this section\">#</a></h2>\n<p>レイアウト自身を、別のレイアウトの中に置けます。ページに付けるのとまったく同じように、\nレイアウトの型に <code>[Layout]</code> を付けます。</p>\n<div class=\"csharp\"><pre>\n<span class=\"keyword\">using</span> BlazorCodeFirst;\n<span class=\"keyword\">using</span> Microsoft.AspNetCore.Components;\n<span class=\"keyword\">using</span> <span class=\"keyword\">static</span> BlazorCodeFirst.Html;\n\n[Layout(<span class=\"keyword\">typeof</span>(SiteLayout))]\n<span class=\"keyword\">public</span> <span class=\"keyword\">partial</span> <span class=\"keyword\">class</span> DocsLayout : ChromeLayoutBase\n{\n    <span class=\"keyword\">protected</span> <span class=\"keyword\">override</span> View Chrome =&gt; Div.Class(<span class=\"string\">&quot;docs&quot;</span>)[Aside[TableOfContents()], Main[Body]];\n}\n</pre></div>\n<p>入れ子を解決するのは Blazor で、BlazorCodeFirst ではありません。<code>LayoutView</code> がレイアウトの型\nから属性を読み、それを自分のレイアウトで包みます。BlazorCodeFirst のレイアウトは、ふつうの\n<code>LayoutComponentBase</code> の子孫です。どの段の <code>Body</code> も、その1つ下の段を持ちます。<code>SiteLayout</code> の\n<code>Body</code> は描かれた <code>DocsLayout</code> で、その <code>DocsLayout</code> の <code>Body</code> がルーティングされたページです。</p>\n<h2 id=\"renderfragment-はそのまま内容になる\">RenderFragment はそのまま内容になる<a href=\"#renderfragment-%E3%81%AF%E3%81%9D%E3%81%AE%E3%81%BE%E3%81%BE%E5%86%85%E5%AE%B9%E3%81%AB%E3%81%AA%E3%82%8B\" class=\"headlink\" aria-label=\"Permalink to this section\">#</a></h2>\n<p><code>Body</code> は BlazorCodeFirst の型ではなく、ただの Blazor の <code>RenderFragment?</code> です。それでも上の\n<code>Main[Body]</code> は、専用の構文なしにコンパイルできます。<code>View</code> が <code>RenderFragment?</code> からの暗黙の\n変換を持っているからです。おかげでフラグメントは、要素の内容が来る場所ならどこにでも書けます。\n変換元はジェネリックでない <code>RenderFragment</code> だけで、<code>RenderFragment&lt;T&gt;</code> は変換されません。\n<code>Fragment</code> や <code>Raw</code> と同じように、<code>RenderFragment</code> はキーを付けられるフレームを開きません。だから\n<code>ForEach</code> の中身の根にはできず、装飾も付けられません。</p>\n<p>同じ仕組みで、BlazorCodeFirst のコンポーネントは Razor から渡された子を描けます。\n<code>[Parameter] public RenderFragment? ChildContent</code> を持つコンポーネントは、それを <code>Body</code> と\nまったく同じように使います。</p>\n<div class=\"csharp\"><pre>\n<span class=\"keyword\">using</span> BlazorCodeFirst;\n<span class=\"keyword\">using</span> Microsoft.AspNetCore.Components;\n<span class=\"keyword\">using</span> <span class=\"keyword\">static</span> BlazorCodeFirst.Html;\n\n<span class=\"keyword\">public</span> <span class=\"keyword\">partial</span> <span class=\"keyword\">class</span> Card : BodyComponentBase\n{\n    [Parameter]\n    <span class=\"keyword\">public</span> RenderFragment? ChildContent { <span class=\"keyword\">get</span>; <span class=\"keyword\">set</span>; }\n\n    <span class=\"keyword\">protected</span> <span class=\"keyword\">override</span> View Body =&gt; Div.Class(<span class=\"string\">&quot;card&quot;</span>)[ChildContent];\n}\n</pre></div>\n<p>逆向き、つまり BlazorCodeFirst のコードから Razor や手書きのコンポーネントへ内容を渡す場合は、\n<code>Component&lt;T&gt;()</code> を使います。<a href=\"/docs/ja/components-and-reuse#%E5%AD%90%E3%81%AE%E5%86%85%E5%AE%B9%E3%82%92%E6%B8%A1%E3%81%99\">子の内容を渡す</a>を見て\nください。</p>\n<h2 id=\"読めるが書き換えられない\">読めるが、書き換えられない<a href=\"#%E8%AA%AD%E3%82%81%E3%82%8B%E3%81%8C%E6%9B%B8%E3%81%8D%E6%8F%9B%E3%81%88%E3%82%89%E3%82%8C%E3%81%AA%E3%81%84\" class=\"headlink\" aria-label=\"Permalink to this section\">#</a></h2>\n<p><code>Chrome</code> と <code>Body</code> は、どちらもコンポーネントの状態を読めます。状態を UI へ映すことが、\nそもそもの役目だからです。ただし、どちらも状態を書き換えられません。ここでの <code>Body</code> は <code>BodyComponentBase</code> の\nほうで、レイアウトのルーティングされた内容のパラメーターではありません。どちらの中で状態を\n書き換えても BCF3001 を報告します。ふつうのコンポーネントの <code>Body</code> に当てはまるのと同じ診断です。</p>\n<h2 id=\"次に読むもの\">次に読むもの<a href=\"#%E6%AC%A1%E3%81%AB%E8%AA%AD%E3%82%80%E3%82%82%E3%81%AE\" class=\"headlink\" aria-label=\"Permalink to this section\">#</a></h2>\n<ul>\n<li>コンポーネントから別のコンポーネントを呼ぶ方法は<a href=\"/docs/ja/components-and-reuse\">コンポーネントと再利用</a>へ。</li>\n<li>上で使った要素の語彙は<a href=\"/docs/ja/elements-and-decorations\">要素と装飾</a>へ。</li>\n</ul>\n"),
        new DocEntry(
            "two-way-binding",
            "双方向の束縛",
            60,
            "ja",
            false,
            "<p>双方向の束縛は、値を DOM へ書き出し、利用者の編集を自分の状態へ読み戻す、1つの装飾です。<code>.Bind</code>\nは Razor の <code>@bind</code> にあたります。必要なものが、すべて目に見える引数として並ぶ書き方になって\nいます。ジェネレーターはこれを、属性のフレームと、Blazor 自身の <code>CreateBinder</code> を運ぶイベントの\nフレームに落とします。束縛した属性が <code>value</code> か <code>checked</code> のときは、要素を正直に保つための DOM\nの再同期も加えます。生成されたコードが実行時に式ツリーをコンパイルすることはなく、リフレクション\nを使うこともありません。enum を束縛したときだけ、フレームワーク自身の変換器の中で1つだけ\nリフレクションによる参照が起きます。</p>\n<h2 id=\"要素を束縛する\">要素を束縛する<a href=\"#%E8%A6%81%E7%B4%A0%E3%82%92%E6%9D%9F%E7%B8%9B%E3%81%99%E3%82%8B\" class=\"headlink\" aria-label=\"Permalink to this section\">#</a></h2>\n<p>値を運ぶ属性、変化を知らせるイベント、そして今の値を読むラムダを名指しします。</p>\n<div class=\"csharp\"><pre>\n<span class=\"keyword\">public</span> <span class=\"keyword\">partial</span> <span class=\"keyword\">class</span> NameField : BodyComponentBase\n{\n    <span class=\"keyword\">private</span> <span class=\"keyword\">string</span> _name = <span class=\"string\">&quot;&quot;</span>;\n\n    <span class=\"keyword\">protected</span> <span class=\"keyword\">override</span> View Body =&gt;\n        Div[\n            Input.Type(<span class=\"string\">&quot;text&quot;</span>).Bind(<span class=\"string\">&quot;value&quot;</span>, <span class=\"string\">&quot;oninput&quot;</span>, () =&gt; _name),\n            P[$<span class=\"string\">&quot;Hello, {_name}&quot;</span>]];\n}\n</pre></div>\n<p>このラムダは両方向に読まれます。本体は属性の値になり、ゲッターだけのこの形では、同じ本体が、\nジェネレーターの書く代入の左辺にもなります。だから相手は代入できるものでなければなりません。\nフィールド、セッターを持つプロパティ、あるいはそのどちらかを通る経路（<code>_form.Name</code>、\n<code>Model.Items[0].Title</code>、<code>_dict[&quot;k&quot;]</code>）です。<code>() =&gt; _name.ToUpper()</code> のような計算した式は BCF3018\nを報告します。それを書きたいときのための形が、下のセッターを明示する書き方です。</p>\n<p>打鍵のたびに束縛するなら <code>&quot;oninput&quot;</code>、要素からフォーカスが外れたときに束縛するなら <code>&quot;onchange&quot;</code>\nを使います。ふつうに欲しくなるのはこの2つですが、組み合わせを縛る一覧はありません。縛るのは\n次の3つの規則だけです。</p>\n<ul>\n<li>どちらの名前も、空でないコンパイル時定数であること（BCF3011）</li>\n<li>イベントの名前が <code>on</code> で始まること（BCF3019）</li>\n<li>どちらも、同じ要素の別の装飾で既に束縛されていないこと（BCF3010）</li>\n</ul>\n<p>名前を HTML と照合する検査はありません。</p>\n<h2 id=\"名前を2つとも書く理由\">名前を2つとも書く理由<a href=\"#%E5%90%8D%E5%89%8D%E3%82%922%E3%81%A4%E3%81%A8%E3%82%82%E6%9B%B8%E3%81%8F%E7%90%86%E7%94%B1\" class=\"headlink\" aria-label=\"Permalink to this section\">#</a></h2>\n<p>Razor は属性をマークアップから推論します。<code>.razor</code> のファイルからリテラルの <code>type=&quot;checkbox&quot;</code> を\n読み取り、<code>value</code> ではなく <code>checked</code> を束縛します。この API には、読み取れるリテラルがありません。\nタグは文字列で、<code>type</code> は式です。<code>Input.Type(kind)</code> はふつうの C# の呼び出しで、その値は走って\nみるまで決まらないこともあります。だから推論を照合する相手がありません。</p>\n<p><code>value</code> を既定にすれば、避けるためにわざわざ手を尽くす価値のある失敗が起こります。チェック\nボックスが間違った属性に束縛され、それが黙って起き、知らせる診断もない、という失敗です。だから\nこの API 全体の規則は <strong>確かめられることだけを推論する</strong> になっています。要素の側は確かめられ\nないので、推論しません。代わりに短い文字列を2つ書きます。</p>\n<p>この間違いのうち、確かめ <em>られる</em> 半分は捕まえます。<code>on</code> で始まらないイベントの名前は BCF3019 を\n報告します。だから2つの引数を取り違えても、死んだ属性が増えることはなく、コンパイル時に止まり\nます。</p>\n<p><code>.Bind</code> のコンポーネント側は名前を推論します。同じ規則が、そちらでは推論を許すからです。\n<a href=\"#%E3%82%B3%E3%83%B3%E3%83%9D%E3%83%BC%E3%83%8D%E3%83%B3%E3%83%88%E3%81%AE%E3%83%91%E3%83%A9%E3%83%A1%E3%83%BC%E3%82%BF%E3%83%BC%E3%82%92%E6%9D%9F%E7%B8%9B%E3%81%99%E3%82%8B\">コンポーネントのパラメーターを束縛する</a>を見てください。</p>\n<h2 id=\"チェックボックスは-checked-を束縛する\">チェックボックスは <code>checked</code> を束縛する<a href=\"#%E3%83%81%E3%82%A7%E3%83%83%E3%82%AF%E3%83%9C%E3%83%83%E3%82%AF%E3%82%B9%E3%81%AF-checked-%E3%82%92%E6%9D%9F%E7%B8%9B%E3%81%99%E3%82%8B\" class=\"headlink\" aria-label=\"Permalink to this section\">#</a></h2>\n<p>チェックボックスは、<code>bool</code> を <code>checked</code> 属性へ、<code>onchange</code> で束縛します。</p>\n<div class=\"csharp\"><pre>\n<span class=\"keyword\">private</span> <span class=\"keyword\">bool</span> _agreed;\n\n<span class=\"keyword\">protected</span> <span class=\"keyword\">override</span> View Body =&gt;\n    Label[\n        Input.Type(<span class=\"string\">&quot;checkbox&quot;</span>).Bind(<span class=\"string\">&quot;checked&quot;</span>, <span class=\"string\">&quot;onchange&quot;</span>, () =&gt; _agreed),\n        <span class=\"string\">&quot; I agree&quot;</span>];\n</pre></div>\n<p><code>bool</code> は HTML の真偽値属性の形で、<a href=\"/docs/ja/elements-and-decorations#%E8%A3%85%E9%A3%BE\"><code>.Attr</code> が取る</a>のと同じ\nものです。<code>true</code> なら値の空な属性として出力し、<code>false</code> なら属性ごと出しません。</p>\n<h2 id=\"セッターを明示して正規化する\">セッターを明示して正規化する<a href=\"#%E3%82%BB%E3%83%83%E3%82%BF%E3%83%BC%E3%82%92%E6%98%8E%E7%A4%BA%E3%81%97%E3%81%A6%E6%AD%A3%E8%A6%8F%E5%8C%96%E3%81%99%E3%82%8B\" class=\"headlink\" aria-label=\"Permalink to this section\">#</a></h2>\n<p>4つ目の引数を渡すと、生成される代入の代わりに自分のセッターが使われます。検証や正規化、編集の\nたびに走らせたい処理は、ここに置きます。</p>\n<div class=\"csharp\"><pre>\n<span class=\"keyword\">private</span> <span class=\"keyword\">string</span> _name = <span class=\"string\">&quot;&quot;</span>;\n\n<span class=\"keyword\">protected</span> <span class=\"keyword\">override</span> View Body =&gt;\n    Input.Type(<span class=\"string\">&quot;text&quot;</span>).Bind(<span class=\"string\">&quot;value&quot;</span>, <span class=\"string\">&quot;oninput&quot;</span>, () =&gt; _name, v =&gt; _name = v.Trim());\n</pre></div>\n<p>これは Razor が <code>@bind:get</code> / <code>@bind:set</code> と <code>@bind:after</code> に分けているものを、まとめて覆います。\nセッターが書き込みそのものなので、後で走らせたかった処理も同じラムダに入ります。ただし書き込み\nは自分の仕事になります。セッターを渡した時点で、代わりに代入してくれるものはなくなります。\nメソッドグループ（<code>SetName</code>）も使え、ラムダの本体はブロックでもよく、<code>Task</code> を返せば <code>async</code> の\n形も使えます。</p>\n<div class=\"csharp\"><pre>\nInput.Type(<span class=\"string\">&quot;text&quot;</span>).Bind(<span class=\"string\">&quot;value&quot;</span>, <span class=\"string\">&quot;oninput&quot;</span>, () =&gt; _query, <span class=\"keyword\">async</span> v =&gt;\n{\n    _query = v;\n    <span class=\"keyword\">await</span> SearchAsync(v);\n});\n</pre></div>\n<p>代入できるゲッターが要るのは、ゲッターだけの形のときだけです。セッターがあれば、ゲッターは読ま\nれるだけなので、どんな式でもかまいません。</p>\n<p>正規化するセッターは、ずれを生みます。要素は打った文字を見せ、フィールドは切り詰めた値を持ち\nます。ふつうの差分は何も書きません。前回のレンダリングからレンダーツリーが変わっていないから\nです。<code>value</code> か <code>checked</code> の束縛なら、<code>.Bind</code> はその属性を DOM の再同期に登録して、このずれを\n埋めます。要素は正規化した値を見せるように直されます。頼まなくても、そうなります。</p>\n<p>この2つの名前がすべてです。Blazor のクライアントが返してくるのは、フォーム要素自身の <code>value</code>、\nチェックボックスなら <code>checked</code> で、それ以外は返ってきません。だからジェネレーターが登録する名前\nも、その2つだけです。ほかの属性への束縛は何も登録しません。カスタム要素での\n<code>.Bind(&quot;hue&quot;, &quot;onhuechange&quot;, () =&gt; _hue, Normalize)</code> が、よくある形です。セッターは変わらず走り、\n新しい値も次のレンダリングでふつうの差分によって DOM へ届きます。無いのは、上に書いた修復だけ\nです。正規化してもレンダーツリーが変わらず、要素が打った文字を見せ続ける場合のための修復です。</p>\n<p>テキスト入力を空にしたとき、セッターが受け取るのは <code>&quot;&quot;</code> で、<code>null</code> ではありません。だから引数の\n型は null 許容でない <code>string</code> です。セッターから自分の状態へ書き込むのは許されています。<code>Body</code>\nのほかの場所で状態へ書き込めば BCF3001 になるのに、です。セッターは <code>.OnClick</code> のラムダと同じ\n遅延したハンドラーで、ツリーを組み立てている最中には走らないからです。</p>\n<h2 id=\"数値日付enum\">数値、日付、enum<a href=\"#%E6%95%B0%E5%80%A4%E6%97%A5%E4%BB%98enum\" class=\"headlink\" aria-label=\"Permalink to this section\">#</a></h2>\n<p>カルチャーを書けば、どの型でも束縛できます。カルチャーは最後の引数で、省略はできません。</p>\n<div class=\"csharp\"><pre>\n<span class=\"keyword\">private</span> <span class=\"keyword\">int</span> _age;\n\n<span class=\"keyword\">protected</span> <span class=\"keyword\">override</span> View Body =&gt;\n    Input.Type(<span class=\"string\">&quot;number&quot;</span>).Bind(<span class=\"string\">&quot;value&quot;</span>, <span class=\"string\">&quot;oninput&quot;</span>, () =&gt; _age, CultureInfo.InvariantCulture);\n</pre></div>\n<p>このカルチャーが、出ていく値を書式化し、戻ってきた値を解析します。通るのは Blazor 自身の\n<code>BindConverter</code> です。数値、日付、時刻、<code>Guid</code>、enum、そしてそれらの null 許容形が、どれも動き\nます。変換がこのライブラリのものではなく、フレームワークのものだからです。</p>\n<p>既定値ではなく引数にしているのは、既定値にすれば、見えないところでカルチャーが選ばれるからです。\nRazor は要素のリテラルな <code>type</code> から選びます。この API が読まないあのリテラルで、属性の名前を\n推測しないのと同じ理由です。だから選択は、目に見える位置へ移してあります。</p>\n<h3 id=\"number-と-date-には不変カルチャーを書く\"><code>number</code> と <code>date</code> には不変カルチャーを書く<a href=\"#number-%E3%81%A8-date-%E3%81%AB%E3%81%AF%E4%B8%8D%E5%A4%89%E3%82%AB%E3%83%AB%E3%83%81%E3%83%A3%E3%83%BC%E3%82%92%E6%9B%B8%E3%81%8F\" class=\"headlink\" aria-label=\"Permalink to this section\">#</a></h3>\n<p><code>&lt;input type=&quot;number&quot;&gt;</code> と <code>&lt;input type=&quot;date&quot;&gt;</code> は、利用者のロケールではなく固定の書式で定義され\nています。ここで <code>CultureInfo.CurrentCulture</code> を使うと、小数点にコンマを使うロケールでは、要素が\n受け取れない値が出ます。</p>\n<p><strong>これは診断しません。</strong> 検査するには <code>type</code> を読む必要がありますが、ここでの <code>type</code> は式です。\nリテラルを書いたときだけ発火する規則は、同じ誤りをある書き方では捕まえ、別の書き方では見逃し\nます。この2つには <code>CultureInfo.InvariantCulture</code> を書いてください。利用者が文章として読む値には\n現在のカルチャーを使います。</p>\n<h3 id=\"解析できない値は元に戻る\">解析できない値は元に戻る<a href=\"#%E8%A7%A3%E6%9E%90%E3%81%A7%E3%81%8D%E3%81%AA%E3%81%84%E5%80%A4%E3%81%AF%E5%85%83%E3%81%AB%E6%88%BB%E3%82%8B\" class=\"headlink\" aria-label=\"Permalink to this section\">#</a></h3>\n<p>打たれた文字を変換器が読めない場合、セッターは呼ばれず、フィールドも要素も前の値に戻ります。\nこれは Blazor の挙動で、<code>.Bind</code> は上に書いた DOM の再同期を通じてそこへ届きます。</p>\n<p>意識して選ぶべき帰結が1つあります。<code>&quot;oninput&quot;</code> では巻き戻しがキーストロークごとに走ります。\n<code>int</code> の束縛に <code>4.5</code> と打つと、手元に残るのは <code>45</code> です。<code>.</code> が拒否されて消え、そのあとに <code>5</code> が\n届くからです。空文字列も解析できないので、欄を空にすることもできません。空にすると前の値がその\nまま戻ります。数値の入力では、たいていこれは望みではありません。</p>\n<div class=\"csharp\"><pre>\n<span class=\"comment\">// フォーカスが外れたときに巻き戻すので、打ちかけの数値が生き残ります。</span>\nInput.Type(<span class=\"string\">&quot;number&quot;</span>).Bind(<span class=\"string\">&quot;value&quot;</span>, <span class=\"string\">&quot;onchange&quot;</span>, () =&gt; _amount, CultureInfo.InvariantCulture);\n</pre></div>\n<p><code>&quot;oninput&quot;</code> が正しいのは、途中の値がどれも意味を持つときです。range のスライダーや、打てる文字\nなら何でも受ける text の欄がこれにあたります。</p>\n<h3 id=\"日付には書式が要る\">日付には書式が要る<a href=\"#%E6%97%A5%E4%BB%98%E3%81%AB%E3%81%AF%E6%9B%B8%E5%BC%8F%E3%81%8C%E8%A6%81%E3%82%8B\" class=\"headlink\" aria-label=\"Permalink to this section\">#</a></h3>\n<p>date の入力は <code>yyyy-MM-dd</code> を要求します。Razor と違い、この API はそれを <code>type</code> から補えません。\nカルチャーの1つ手前の引数として書きます。</p>\n<div class=\"csharp\"><pre>\n<span class=\"keyword\">private</span> DateOnly _due = <span class=\"keyword\">new</span>(<span class=\"number\">2026</span>, <span class=\"number\">8</span>, <span class=\"number\">14</span>);\n\n<span class=\"keyword\">protected</span> <span class=\"keyword\">override</span> View Body =&gt;\n    Input.Type(<span class=\"string\">&quot;date&quot;</span>).Bind(\n        <span class=\"string\">&quot;value&quot;</span>, <span class=\"string\">&quot;oninput&quot;</span>, () =&gt; _due, <span class=\"string\">&quot;yyyy-MM-dd&quot;</span>, CultureInfo.InvariantCulture);\n</pre></div>\n<p>書式を受け取るのは <code>DateTime</code> / <code>DateTimeOffset</code> / <code>DateOnly</code> / <code>TimeOnly</code> とその null 許容形だけ\nで、ほかの型は受け取りません。フレームワークが書式付きの変換器を宣言しているのが、その8つだから\nです。<code>int</code> に書けば BCF3031 です。数値を書式化したい場合は、ゲッターで書式化し、セッターを明示\nして解析してください。</p>\n<h3 id=\"トリムして配布する場合\">トリムして配布する場合<a href=\"#%E3%83%88%E3%83%AA%E3%83%A0%E3%81%97%E3%81%A6%E9%85%8D%E5%B8%83%E3%81%99%E3%82%8B%E5%A0%B4%E5%90%88\" class=\"headlink\" aria-label=\"Permalink to this section\">#</a></h3>\n<p>値型を1つでも束縛すると、Blazor の <code>BindConverter</code> が丸ごと保持されます。自分が束縛しない型の\n変換器も残ります。トリム済みの self-contained な publish で実測すると、\n<code>Microsoft.AspNetCore.Components.dll</code> のおよそ 10 KB にあたります。<code>string</code> と <code>bool</code> だけを束縛\nするアプリにこの費用はかかりません。束縛の個数ではなく、一度きりの費用です。</p>\n<h3 id=\"変換を自分で書く\">変換を自分で書く<a href=\"#%E5%A4%89%E6%8F%9B%E3%82%92%E8%87%AA%E5%88%86%E3%81%A7%E6%9B%B8%E3%81%8F\" class=\"headlink\" aria-label=\"Permalink to this section\">#</a></h3>\n<p>明示する書き方も、これまでどおり使えます。変換ではなく検証をしたいときは、そちらが向いています。</p>\n<div class=\"csharp\"><pre>\n<span class=\"keyword\">private</span> <span class=\"keyword\">decimal</span> _amount;\n\n<span class=\"keyword\">protected</span> <span class=\"keyword\">override</span> View Body =&gt;\n    Input.Type(<span class=\"string\">&quot;text&quot;</span>).Bind(\n        <span class=\"string\">&quot;value&quot;</span>, <span class=\"string\">&quot;onchange&quot;</span>,\n        () =&gt; _amount.ToString(CultureInfo.InvariantCulture),\n        v =&gt; _amount = <span class=\"keyword\">decimal</span>.TryParse(v, NumberStyles.Number, CultureInfo.InvariantCulture, <span class=\"keyword\">out</span> <span class=\"keyword\">var</span> d)\n            ? d\n            : _amount);\n</pre></div>\n<p>ここでは <code>Parse</code> ではなく <code>TryParse</code> を使ってください。セッターから投げられた例外は、フレーム\nワークが値を拒否する経路ではありません。描画そのものが失敗します。</p>\n<h2 id=\"コンポーネントのパラメーターを束縛する\">コンポーネントのパラメーターを束縛する<a href=\"#%E3%82%B3%E3%83%B3%E3%83%9D%E3%83%BC%E3%83%8D%E3%83%B3%E3%83%88%E3%81%AE%E3%83%91%E3%83%A9%E3%83%A1%E3%83%BC%E3%82%BF%E3%83%BC%E3%82%92%E6%9D%9F%E7%B8%9B%E3%81%99%E3%82%8B\" class=\"headlink\" aria-label=\"Permalink to this section\">#</a></h2>\n<p>コンポーネントでの名前は、書くのではなく導きます。そちらでは、導いた名前を確かめられるから\nです。<code>.Bind</code> はラムダでパラメーターを選び、ジェネレーターが <code>{Name}Changed</code> を、コンポーネント\nが宣言していれば <code>{Name}Expression</code> も足します。</p>\n<div class=\"csharp\"><pre>\n<span class=\"keyword\">using</span> Microsoft.AspNetCore.Components.Forms;\n\n<span class=\"keyword\">private</span> <span class=\"keyword\">readonly</span> NameModel _model = <span class=\"keyword\">new</span>();\n\n<span class=\"keyword\">protected</span> <span class=\"keyword\">override</span> View Body =&gt;\n    Component&lt;InputText&gt;().Bind(c =&gt; c.Value, () =&gt; _model.Name);\n</pre></div>\n<p>この1回の呼び出しが、<code>Value</code>、<code>ValueChanged</code>、<code>ValueExpression</code> を渡します。導いた名前はどれも\nコンポーネントの型の上で探すので、<code>{Name}Changed</code> が無かったり綴りが違ったりすれば、何も束縛\nしないのではなく BCF3020 を報告します。これが、要素側との非対称を、ちぐはぐではなく規則にして\nいます。</p>\n<p>対象を参照渡しではなくゲッターのラムダとして書くのは、<code>{Name}Expression</code> のためです。<code>EditForm</code>\nの下にあるコンポーネントは、その式から <code>FieldIdentifier</code> を解決します。この識別子が、入力を\nモデルのプロパティに結び付けます。おかげで検証のメッセージが正しいフィールドに届きます。ほかの\n書き方では、これを渡せません。</p>\n<p>周りの <code>EditForm</code> も同じ書き方です。<code>EditForm.ChildContent</code> は <code>RenderFragment&lt;EditContext&gt;</code> で、\n角括弧はこれをコンテキストを捨てて渡します。下の内容には、ほかに要るものがありません。\n<code>EditContext</code> を読む内容だけ、<code>.Template</code> で名指しします。</p>\n<div class=\"csharp\"><pre>\n<span class=\"keyword\">protected</span> <span class=\"keyword\">override</span> View Body =&gt;\n    Component&lt;EditForm&gt;()\n        .Param(form =&gt; form.Model, _model)[\n            Component&lt;NameFields&gt;().Param(fields =&gt; fields.Value, _model)];\n</pre></div>\n<p>束縛した入力は、角括弧の中に直接置いても、独立したコンポーネントに置いてもかまい\nません。上の束縛と、それが渡す <code>ValueExpression</code> は、どちらでも変わりません。<code>EditContext</code> を\n読む書き方と、<code>.Param</code> でキャッシュしたデリゲートを渡すほうがよい場面については、\n<a href=\"/docs/ja/components-and-reuse#%E3%82%B8%E3%82%A7%E3%83%8D%E3%83%AA%E3%83%83%E3%82%AF%E3%81%AA%E3%83%95%E3%83%A9%E3%82%B0%E3%83%A1%E3%83%B3%E3%83%88%E3%81%AE%E3%83%91%E3%83%A9%E3%83%A1%E3%83%BC%E3%82%BF%E3%83%BC\">ジェネリックなフラグメントのパラメーター</a>を\n見てください。</p>\n<p>セッターを明示する形と <code>async</code> のセッターは、こちらでも使えます。意味は要素のときと同じです。\n<code>TValue</code> はカルチャーも書式も取りません。値が向かう先は DOM ではなくパラメーターで、途中で書式化\nも解析もされないため、書き留めるべき選択がそもそも無いからです。</p>\n<p><code>Component&lt;T&gt;()</code> が、同じプロジェクトで宣言した <code>.razor</code> のコンポーネントを名指しできないことは\n覚えておいてください（<a href=\"/docs/ja/components-and-reuse#%E6%97%A2%E5%AD%98%E3%81%AE-razor-%E3%82%B3%E3%83%B3%E3%83%9D%E3%83%BC%E3%83%8D%E3%83%B3%E3%83%88%E3%82%84%E3%82%B5%E3%83%BC%E3%83%89%E3%83%91%E3%83%BC%E3%83%86%E3%82%A3%E3%81%AE%E3%82%B3%E3%83%B3%E3%83%9D%E3%83%BC%E3%83%8D%E3%83%B3%E3%83%88%E3%82%92%E5%91%BC%E3%81%B6\">BCF3012</a>）。\n<code>InputText</code> のようなフレームワークのコンポーネントと、手書きの C# のコンポーネントは、いつでも\n解決します。</p>\n<h2 id=\"何が検査されるか\">何が検査されるか<a href=\"#%E4%BD%95%E3%81%8C%E6%A4%9C%E6%9F%BB%E3%81%95%E3%82%8C%E3%82%8B%E3%81%8B\" class=\"headlink\" aria-label=\"Permalink to this section\">#</a></h2>\n<ul>\n<li><strong>BCF3017</strong> — ゲッターが、式本体を持つその場のラムダではない。本体がブロックのラムダや、\nメソッドグループがこれにあたります。本体は取り出せる必要があります。属性の値とバインダーの\n両方に写されるからです。セッターにこの制限はありません。渡されるだけだからです。</li>\n<li><strong>BCF3018</strong> — ゲッターだけの形で、そのゲッターの本体が代入できない。呼び出しと演算子、\nゲッターだけのプロパティ、<code>readonly</code> のフィールド、ローカル変数や <code>ForEach</code> の反復変数それ自体\nは拒否します。反復変数の <em>メンバー</em>（<code>o.Title</code>）はかまいません。そこへ書けば、元の要素が変わる\nからです。セッターを明示して書いてください。</li>\n<li><strong>BCF3019</strong> — イベントの名前が <code>on</code> で始まらない。その名前はただの属性として足され、ハンドラー\nは一度も呼ばれません。だからこれが、2つの名前を取り違えたときに捕まえる検査です。</li>\n<li><strong>BCF3020</strong> — コンポーネントの <code>{Name}Changed</code> パラメーターが無い、または\n<code>EventCallback&lt;TValue&gt;</code> ではない。</li>\n<li><strong>BCF3024</strong> — 属性の名前が <code>class</code> で、同じ要素が <code>.Class</code> か <code>.Attr(&quot;class&quot;, …)</code> も持っている。\nこれらは1つの属性に畳み込まれますが、束縛はそこに合流しないので、要素は <code>class</code> を2つ持つ\nことになります。</li>\n<li><strong>BCF3031</strong> — フレームワークが書式付きの変換器を宣言していない型に、書式が書かれている。\n受け取るのは <code>DateTime</code> / <code>DateTimeOffset</code> / <code>DateOnly</code> / <code>TimeOnly</code> とその null 許容形だけです。</li>\n</ul>\n<p>1つの要素が <code>.Bind</code> を複数持つことはできます。そのうち2つが属性の名前かイベントの名前を共有\nすれば BCF3010 で、どの装飾を2つ重ねても出るのと同じ重複です。DOM の再同期、つまり利用者が打った\n文字の上に正規化した値を置き直す修復は、<code>value</code> と <code>checked</code> にだけ効きます。ブラウザーがイベント\nと一緒に返してくるのが、その2つだけだからです。</p>\n<h2 id=\"次に読むもの\">次に読むもの<a href=\"#%E6%AC%A1%E3%81%AB%E8%AA%AD%E3%82%80%E3%82%82%E3%81%AE\" class=\"headlink\" aria-label=\"Permalink to this section\">#</a></h2>\n<ul>\n<li>これが組み立てられている、一方向の <code>.Attr</code> と <code>.On</code> は<a href=\"/docs/ja/elements-and-decorations#%E8%A3%85%E9%A3%BE\">要素と装飾</a>へ。</li>\n<li><code>.Param</code> とコンポーネント周りの残りは<a href=\"/docs/ja/components-and-reuse\">コンポーネントと再利用</a>へ。</li>\n</ul>\n"),
    ];

    /// <summary>One language's documents, in navigation order.</summary>
    public static ImmutableArray<DocEntry> ForLang(string lang) => ForLang(All, lang);

    /// <summary>The same, over a manifest given directly rather than this build's.</summary>
    public static ImmutableArray<DocEntry> ForLang(ImmutableArray<DocEntry> docs, string lang)
    {
        var builder = ImmutableArray.CreateBuilder<DocEntry>();
        foreach (var entry in docs)
        {
            if (string.Equals(entry.Lang, lang, StringComparison.Ordinal))
            {
                builder.Add(entry);
            }
        }

        return builder.ToImmutable();
    }

    /// <summary>Finds a document by language and slug. Blazor route matching is
    /// case-insensitive, so the slug lookup must be too; the language comes from the route
    /// template rather than from the reader, so it is matched exactly.</summary>
    public static DocEntry? Find(string lang, string? slug) => Find(All, lang, slug);

    /// <summary>The same, over a manifest given directly rather than this build's.</summary>
    public static DocEntry? Find(ImmutableArray<DocEntry> docs, string lang, string? slug)
    {
        if (slug is null)
        {
            return null;
        }

        foreach (var entry in docs)
        {
            if (string.Equals(entry.Lang, lang, StringComparison.Ordinal) &&
                string.Equals(entry.Slug, slug, StringComparison.OrdinalIgnoreCase))
            {
                return entry;
            }
        }

        return null;
    }
}
